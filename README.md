# 通用神经网络处理器多核调度

## H 分支当前成果（2026-09-25）

最新操作级全量运行已完成：100 案例 × A/B × 2/3/4/5 核 = **800 项**，全部取得本轮官方结果。当前成果优先阅读：

- [技术路线 H-v3.0](技术路线_H分支_v3.0.md)
- [800 项完整实验记录](实验记录_H分支_操作级800全量_v1.0.md)
- [数据统计与核验报告](数据统计_H分支_操作级800全量_v1.0.md)
- [800 项逐任务统计](results/oplist_full800.tasks.json)和[原始最终汇总](results/solve_log_oplist_full800.summary.json)
- [历史实验数据与复现索引](results/README.md)

| 核数 | A 场景：完整 100 案例 | B 场景：完整 100 案例 |
|---|---:|---:|
| 2 | 1.7208× | 2.2675× |
| 3 | 2.2534× | 3.0657× |
| 4 | 2.7109× | 3.6779× |
| 5 | 3.0787× | 4.2255× |

指标为各案例单核参照耗时除以方案官方耗时的算术平均。原历史可比 81 案例 A/5=3.4577×、B/5=4.2845×，不等于完整 100 案例均值。以下旧流程与各历史实验文档保留原始实验范围，不能视作本轮最终结论。

本次按明确上传要求归档 H 分支现有实验数据，原始结果只留本地的通用限制在本次归档范围采用例外，详见 results/README.md。

这是 2026 年研究生数学建模竞赛 A 题的可复现实验代码。当前 `main` 分支保存经过全量实验验证的主版本，核心代码位于 `solver/`，官方评估器和题目输入位于 `通用神经网络处理器下的多核调度问题附件/`。

## 目录

- `solver/`：图模型、构造式初始解、搜索器、代理评估和实验入口。
- `通用神经网络处理器下的多核调度问题附件/code/`：题目官方评估器与验证代码。
- `通用神经网络处理器下的多核调度问题附件/data/`：可复现实验输入。官方生成的超大结果和 trace 不纳入 Git。
- `技术文档.md`：算法设计、模块职责和实验结论。
- `技术路线_主分支最佳流程_v1.0.md`：当前主分支的唯一完整技术路线。
- `实验记录_主分支_fix_v1.0.md`：当前最佳流程的实验命令、结果和限制。
- `技术路线_H分支_v2.0.md`：H 分支最新流程、候选扩展和可选 MCTS 模块说明。
- `实验记录_H分支_800全量_v1.0.md`：800 项全量实验与 96 项代表集结果。
- `技术路线_H分支_无MCTS最终版_v1.0.md`：当前正式结果采用的无 MCTS 技术路线。
- `实验记录_H分支_无MCTS最新结果_v1.0.md`：不启用 MCTS 的 800 项全量结果。
- `主分支上传规范_v1.0.md`：W/Y/H 三分支的代码与文档提交要求。
- `results/`：只保留可审阅的汇总文档和指标摘要；完整原始结果保存在本地实验目录。

## 环境

- Windows 10/11
- Python 3.12
- PyTorch（仅用于 CUDA（统一计算架构）候选筛选）
- 推荐显卡：RTX 3070 8GB

安装依赖后，从项目根目录执行：

```cmd
cd /d J:\数学建模\改进 && python -m solver.run_all --cases 1-100 --cores 2,3,4,5 --scenes A,B --workers 8
```

实验入口带有断点恢复，日志写入 `results/solve_log.jsonl`。完整结果不应提交到 Git，建议使用本地磁盘或对象存储保存。

## 可复现的调度优化实验

若要启用关键路径窗口的精确顺序优化，在独立 Python 环境中安装可选依赖：

```cmd
python -m venv .venv
.venv\Scripts\python.exe -m pip install -r requirements-optimization.txt "pytest>=8,<9"
.venv\Scripts\python.exe -m pytest tests -q
.venv\Scripts\python.exe -m solver.run_all --cases 1,7,15,23,42,44,49,63,67,78,82,97 --cores 2,3,4,5 --scenes A,B --workers 8 --seed 0 --output-dir results/plans_optimized_trial --log-file results/solve_log_optimized_trial.jsonl
```

`--seed` 与算例、场景、核数共同决定每项任务的固定随机种子；任务日志记录实际种子。对于官方评估器可处理的算例，流程会用张量生命周期模型复核候选，并用官方结果选出最终方案。日志中的 `diagnostics` 给出计算/带宽下界、核空闲时间和代理关键路径；`exact_refine` 记录精确顺序优化的窗口数。不安装 OR-Tools 时，精确顺序优化会跳过，其他步骤照常运行。超过 12000 个操作的算例仍按现有超大图兜底流程处理。

已有正式评估过的方案时，可加 `--warm-start-dir results/plans_baseline_stable`。求解器会再次用官方评估器核验已有方案，并与新候选比较完成时间；日志记录 `warm_start_makespan` 及是否采用已有方案。该目录应与新输出目录分开，避免覆盖输入方案。

两次运行完成后，可用 `python -m solver.compare_optimizer_runs 旧日志.jsonl 新日志.jsonl` 对同一任务和随机种子的官方结果做成对比较；默认从 `results/summary.csv` 读取单核基准。

## 指标口径

- `makespan（总完成时间）`：官方离散事件评估器输出的周期数，越小越好。
- `speedup（加速比）`：基线 makespan 除以当前方案 makespan，越大越好。
- `improvement（改善率）`：`(基线 - 当前方案) / 基线`，越大越好。

主版本采用非回退规则：候选方案经官方评估后，若 makespan 不优于对应基线，则不进入主版本结果。

## 协作开发

三个人的工作分支固定为 `W`、`Y`、`H`，规则见 `CONTRIBUTING.md`。任何分支合并前必须提供相同算例集合的基线对比、测试结果和指标报告。
