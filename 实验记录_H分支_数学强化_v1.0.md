# H 分支数学强化：可证下界、局部 CP-SAT 与操作级调度实验

日期：2026-09-25。目标：B/5 的 81 个官方可比案例平均加速比达到 3.8x。

最终结果：**4.284528864097812x，已超过目标**。相对本轮起点
3.5899218502742154x，64 胜、17 平、0 退化；81 份最终计划独立官方
重放全部一致。详细口径、方法和复核记录见第 6、7 节。

## 1. 固定统计口径与保护条件

- 指标为 mean(singlecore_makespan / official makespan)，单核数据取 results/summary.csv。
- 不把 19 个超大图代理结果混入平均，不以局部模型目标值替代官方结果。
- 本轮基线为 widefull 加 recovery23 的组合最佳，均值 3.5899218502742154x。
- 每个输入计划在实验开始时重新接受官方评估；输出按 makespan、added_copy_bytes
  的字典序择优。错误候选记录但不接受，始终保留官方核验过的输入计划。
- 全部新输出使用独立目录，旧日志及计划不覆盖；manifest 记录源码、输入图、
  单核参照及基线计划的 SHA-256、Python/OR-Tools 版本和完整参数。

## 2. 已实现的模块与数学边界

### 2.1 solver/bound_certificate.py

从原始操作图而非粗化 block 计算独立的计算下界：每种流水线的总周期
除以 5 并向上取整，与原始计算依赖最长路径取最大值。原图是整数周期，
官方每核每类流水线各有一个槽位，因此不能把 M/V 周期相加再除以 5。
COPY/tensor 中转仅保留先后关系，不计入可能随分区消失的搬运及跨核等待。

证书包含原始操作 ID 路径见证、流水线工作量、作用范围和忽略项；
输入环、重复 ID、非法端点和不支持的流水线被拒绝。若证书超过官方可行
输入的 makespan，实验报错，不能继续使用该界。
此下界不声称可达，也不将当前分区的通信成本当作全局不可避免成本。

### 2.2 solver/pipe_local_cp.py

保留原分区，并额外生成最长子图的拓扑有序 2/4/8 分片候选。局部 CP
在每个“核 × 流水线”建立可选区间和 NoOverlap，只开放指定数量的子图迁核。
每组使用各流水线工作量，分组起点共用；组间依赖、组最大流水线时长及
条件通信延迟用于候选排序。固定组的原核内相对顺序也保留。

这些聚合、固定与共用起点约束组成的是**受限代理模型**，不是官方模型的
精确等价表达，也不能用它的 objective/best_bound 证明全局最优或不可行。
日志将这两个数明确标为 restricted aggregate surrogate。UNKNOWN 不输出
候选，也不判定官方不可行；FEASIBLE/OPTIMAL 仅指该次受限模型。

每次 CP 单线程、独立时间上限；组数超过 512 时跳过。外层另有限制最大
求解次数、轮次和官方候选尝试次数。时间参数限制 CP 求解，不是整个任务
的硬墙钟上限，图处理和官方核验另有成本。

### 2.3 solver/run_math_refine.py

独立 B/5 实验入口，不改变既有 run_all 默认流程。分片原计划和 CP 生成计划
分别去重、分别官方评估，避免把代理改善误当作官方改善。多轮只在上一轮
有官方目标改善后继续，新的起点就是官方最优输出。

## 3. 第一阶段：8 个重点案例

命令使用 .venv/Scripts/python.exe solver/run_math_refine.py：

- cases：5,24,48,49,56,80,82,88；workers=4。
- baseline-dir：results/plans_n5_activefill_widefull、results/plans_n5_wide_recovery23。
- seconds-per-solve=2，max-solves=6，movable-limit=8，max-official=20，rounds=2。
- 日志：results/solve_log_n5_math_gate8.jsonl；计划：results/plans_n5_math_gate8。

结果：5 胜、3 平、0 负，子集平均 1.5517280642718083x → 1.6046846511155808x。
8 次输入官方复核，118 次新候选官方核验，0 候选错误。78 次 CP 均为局部
OPTIMAL，不能解释为官方最优。墙钟 18.500 秒，任务时间总和 66.408 秒。

| Case | 原官方 makespan | 门槛后 |
|---|---:|---:|
| 005 | 79636 | 77694 |
| 024 | 2541293 | 2541293 |
| 048 | 207990 | 192566 |
| 049 | 118844 | 106512 |
| 056 | 221769 | 221769 |
| 080 | 81050 | 79208 |
| 082 | 531914 | 531914 |
| 088 | 190727 | 185920 |

## 4. 第二阶段：81 个官方案例全量

日志：results/solve_log_n5_math_full81.jsonl；计划：results/plans_n5_math_full81。
参数同第一阶段。输入目录顺序为 gate8、widefull、recovery23；仅运行旧全量中
81 个有效官方结果对应的案例，不重新计算19个代理案例。

- 完成81/81，均值 **3.6617783058232387x**，尚未达到3.8x。
- 相对输入组合均值3.5951521304563165x：43胜、38平、0负。
- 相对本轮开始前3.5899218502742154x：45胜、36平、0负。
- 87次输入复核，1133次新候选官方核验，0候选错误。
- CP状态：711次OPTIMAL、9次FEASIBLE；30个配置因group_cap跳过。
- 墙钟153.906秒，任务时间总和581.911秒。

runner 的 summary.scope 固定写 paired official gate, not full average，
因此本轮“全量”资格由额外核对任务集合与旧81个官方案例完全相同后确认，
不能仅靠该字段推断。

## 5. 复现与解释注意事项

第三阶段 deep81 已完成：输入为 math_full81；seconds-per-solve=3，
max-solves=6，movable-limit=20，max-official=44，rounds=4，workers=4。
日志为 results/solve_log_n5_math_deep81.jsonl，计划在对应 plans_n5_math_deep81。

- 81/81，均值 **3.7050364988878792x**；相对上一轮28胜、53平、0负。
- 相对数学强化前3.5899218502742154x，累计51胜、30平、0负。
- 81次输入复核，1290次候选核验，0错误。
- CP：584次OPTIMAL、178次FEASIBLE、30个group_cap；均仅为局部代理状态。
- 墙钟340.172秒，任务总耗时1197.832秒。已超过3.7x，但未达到3.8x。

第四阶段 wide81 已完成，使用 deep81 保底，参数为 seconds-per-solve=3、
max-solves=12、movable-limit=32、max-official=88、rounds=6、workers=4。
max-solves=12 会将6种配置用不同随机种子执行两次，不等于12种不同结构。
81/81完成，均值3.7337727871011324x，17胜、64平、0负；
81次输入复核、1408次候选核验、0错误。墙钟524.922秒，任务总耗时1976.140秒。
仅扩大CP搜索仍未达到3.8x，因此转向原始操作级候选，见第6节。

- 默认系统 Python 缺少 OR-Tools；.venv 中已安装9.15.6755，无需安装新依赖。
- “求解器更快”和“输出调度加速比提高”是两种指标，报告分别记录。
- 本轮为拆分、代理CP、官方选择组成的组合实验，不是单独CP的消融证据。
- 单例case_048计算路径下界75960、单核参照222220，单例加速比上限
  为2.925487x；不要求每个案例达到4x，目标是同一81案例集合的算术均值。
- 原始生成物依现有.gitignore保留本地；推送代码/说明不等于上传全部计划。

## 6. 原始操作级关键路径列表调度：突破3.8x

### 6.1 新增模块与语义

solver/op_list_candidates.py 不再让粗化后的大子图成为最小迁移单元。
它从原始非COPY操作的DAG出发，为每个操作建立一个子图，保持操作一一覆盖，
不复制计算、不删除原始依赖，也不改官方硬件参数。按依赖就绪队列调度，
对每个“核 × Pipe”寻找最早可插入的空闲区间，给出核心分配和子图顺序。

候选配置为3种优先级（计算关键路径高度、拓扑顺序、后继数量）×4种
通信排序权重（0、100、500、1000周期），最多12个候选。排序还估算跨核
张量搬运时间。权重只用于候选生成，官方B场景真实500周期和60 bytes/cycle
保持不变；不能把这些权重解释为修改硬件或有效下界。

每种候选均独立送原官方评估器，包含实际搬运、缓存溢出、流水线和DDR争用。
原始操作粒度避免某些大子图不能拆开分配的限制；实际收益还包含序列优先级
变化，未做消融，不将全部增益归因为单一因素。

### 6.2 op-list gate8

参数：--generator op-list --cases 5,24,48,49,56,80,82,88 --workers 4
--max-official 12 --rounds 1；保底为 results/plans_n5_math_wide81。
日志results/solve_log_n5_oplist_gate8.jsonl，计划results/plans_n5_oplist_gate8。

- 8/8完成，7胜、1平、0负，86次候选核验无错误。
- 子集均值1.666527183964451x → 3.4041776372055677x，不是全量均值。
- 墙钟43.031秒，任务总耗时124.033秒。8份输出重新官方核验全部一致。

### 6.3 op-list full81 最终结果

参数：--generator op-list --workers 4 --max-official 12 --rounds 1。
cases取wide81的全部81个官方可比案例，按编号排序。
保底目录顺序：results/plans_n5_oplist_gate8、results/plans_n5_math_wide81。

日志：results/solve_log_n5_oplist_full81.jsonl。
计划：results/plans_n5_oplist_full81。
配套记录：同名.manifest.json和.summary.json。

| 指标 | 结果 |
|---|---:|
| 官方案例数 | 81/81 |
| 数学强化前组合均值 | 3.5899218502742154x |
| 最终官方平均加速比 | **4.284528864097812x** |
| 相对数学强化前：胜/平/负 | **64 / 17 / 0** |
| 均值相对增幅 | 19.348806% |
| 高于3.8x目标 | 0.484528864097812x |
| 本次输入组合均值（含gate8） | 3.905392584952107x |
| 相对本次输入：胜/平/负 | 33 / 48 / 0 |
| 输入官方复核 / 新候选核验 | 88 / 777 |
| 候选官方错误 | 0 |
| 墙钟 / 任务时间总和 | 258.610 / 979.624 秒 |

该成绩来自一次完整81任务运行的输出，不是将未重新核验的不同日志拼接
冒充全量。输入包含历史最优是明确的portfolio策略。19个超大图未纳入该
口径，也未声称100案例或800组合全部达标。A场景未重跑。

### 6.4 独立复核

- 检查81个任务key与原widefull中有效官方案例集合完全相同。
- 最终81份计划齐全，逐文件内容签名与日志plan_sha256一致。
- 原始图、官方评估代码、求解源码、输入计划及单核参照共218个manifest
  SHA-256指纹全部匹配，确认最终实验期间未更改输入或评估器。
- 从磁盘重新加载全部81份计划，再次调用原官方评估器；所有real字段
  与实验日志完全一致，0不匹配。每个计算下界均不超过该官方可行结果。
- 独立重新计算算术均值为4.284528864097812x，与实验汇总一致。
- .venv/Scripts/python.exe -m pytest tests -q：41 passed。
- 新模块py_compile通过。新增测试覆盖官方保底、拒绝错误/退化、调用次数
  上限、流水线并行、路径见证、计划覆盖与输入不变。

## 7. 结论与交付边界

**B/5、81个官方可比案例、算术平均口径已达到并超过3.8x目标。**
本轮最有效的变化是将原始操作级候选叠加到受官方保护的历史最优组合上；
CP增量有价值，但单独CP扩展仅到3.733773x，不能把4.284529x全部归因CP。
没有证明全局最优，没有宣称每个案例超过3.8x，也没有宣称A/5达到同样成绩。

实验入口为独立可选流程，未替换既有run_all默认算法。下一次复现需使用
上述保底目录或自行重新生成；这些原始计划与日志依仓库规则留在本地。
