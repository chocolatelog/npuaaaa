# 问题一：全量复盘与持续实验总计划

所属分支：`W`（本次上传来源；原设计阶段记录保留）
文档版本：`P1-review-design-v1.1`（融合核心协作组与资源表的持续实验计划）
发布基线版本：`main-fix-v1.0.0`（已发布修复版）
工作区实验版本：`W-e09-modules-v1.0.0`（源码交接；新全量未运行）
更新时间：2026-09-25 08:35
实施状态：E09三轮机制组与事件信息采集完成；泛化、生产融合及新全量未完成。
适用范围：问题一、场景 A；不把问题二和问题三的缓存机制混入本设计。
协作说明：W、H、Y 三个分支任务相同，自选实验方向；本文不指定人员分工。

**文档约定：本文固定维护整体分析与算法设计；[《实验计划_问题一.md》](实验计划_问题一.md)是唯一实验执行入口，集中维护阶段、状态、集合和验收。两个文件都保持路径不变、内部递增版本；后续新想法融入本文对应设计及实验计划，不再拆成独立想法文档。实验事实追加主实验记录，已采纳实现同步主技术文档。**

**当前接续位置：唯一实验计划19.18，按用户要求先上传模块，由协作者按19.17接入完整入口并完成新全量。下面较早阶段建议保留为历史，不重新启动已完成实验。**

下一次接手顺序：先读《实验计划_问题一.md》的头部状态、第 3 节阶段表、第 9 节待办；再按需查本文第 5 节已有实验与第 7～10 节算法设计。实验后的持续更新规则在实验计划第 7、10 节，不从最近一条聊天或旧独立草案自行重建路线。

## 1. 本轮决策与阅读顺序

我们已有一个能够完成切图、分核、排序和官方复核的求解框架。下一步应补强“瓶颈定位后选择正确动作”的能力，而非继续堆叠更多搜索算法名称。

推荐主线：**张量边界和局部缓存决定切分成本，切分保留并行机会，完整依赖与共享带宽决定分核和顺序，最后用官方结果决定是否接受。**

按以下顺序实施：冻结证据与协议 → 构造修正隔离对照 → 末端交换与小窗口 → 局部模板缓存 → 自适应切分 → 统一事件资源表 → 依赖完整前瞻 → 临时核心协作组 → 统一预算融合 → 超大图官方覆盖 → 冻结版本与新全量。事件后缀增量重放作为有成本证据后再做的可选扩展，不抢占主线。

其中超大图的成本摸底可以与算法设计交错安排，但不能用代理成绩代替完整官方交付。下文所有新参数都是第一轮可复现的工程配置，不是已经调优的结论；所有新方法都是可检验的创新假设，不宣称学术首创。

建议阅读：第 2～5 节理解题目、现状和错误；第 6 节理解参考代码；第 7～10 节是详细算法设计，含资源表和核心协作组；第 11～13 节说明与固定实验计划的衔接及已有命令；具体任务与持续更新规则统一在《实验计划_问题一.md》维护。

## 2. 回到原题：真正的三个难点

原题三个相互耦合的决策是：**切图、核心分配、同核子图执行顺序**。缓存、通信和计算流水贯穿三者，不能把三个难点简化成三个彼此独立的指标。

### 2.1 切图：少搬运与多并行之间的冲突

每个非复制算子恰好属于一个子图，每个子图在场景 A 中形成一个独立任务。大子图有利于减少跨子图复制和重复输入，但会捆绑可并行分支，也可能增加局部缓存溢出。小子图有利于并行，却增加边界复制、任务切换和依赖等待。

因此，“越细越并行”“越粗越省通信”都只说对了一部分。拆开一条严格依赖链可能只是增加等待；把两个共享输入但本可并行的分支合并，也可能省了输入却丢掉核心并行。

合法性是完整覆盖、子图依赖商图无环、加上每核顺序边以后仍无环。**不能额外强制所有子图在某一个固定拓扑序中连续**；那只是候选生成的一种限制，不是题目规则。当前块化也是搜索空间压缩，不是题目规定的不可拆分算子组。

### 2.2 核心分配：工作量均衡不等于完成时间均衡

不能只数每核有多少个子图或算子。矩阵与向量计算、局部搬运、依赖释放时刻、共享外存竞争都影响真实完成时间。

场景 A 中，同核不同子图之间仍清空私有缓存、经外存搬运。**迁到同一个核不能自动消除子图边界字节。** 固定切分只改分核和顺序，局部构图不变时，分区新增字节和溢出字节应保持不变；收益来自等待减少、负载变化和计算/搬运重叠变化。

### 2.3 同核顺序：先做谁决定谁能被提前解锁

一个核先执行较短任务，可能提前释放其他核上的关键后继；也可能抢占共享带宽，使真正关键的任务更晚完成。合法的拓扑顺序很多，简单的就绪先到先执行不能保证总完成时间最小。

把数据依赖边和核内相邻任务顺序边合在一起检查，才能避免“各核单独合法、合起来死锁”。优化对象是提交文件里的每核任务顺序；不能直接修改官方决定的子图内部算子执行规则。

### 2.4 本文采用的固定硬件语义

| 项目 | 官方值或规则 | 设计含义 |
|---|---|---|
| L1（一级缓存） | 524288 字节 | 与统一缓冲池分别统计，不能混成一个容量 |
| UB（统一缓冲池） | 131072 字节 | 按每个任务内部生命周期统计 |
| DDR（外部内存）共享带宽 | 60 字节/周期 | 多核搬运共享，不是每核各有 60 |
| 同核相邻任务等待 | 100 周期 | 细分会增加任务切换成本 |
| 跨核任务依赖等待 | 1000 周期 | 分核不能只看当前负载 |
| 场景 A 缓存 | 每子图一个任务，任务之间清空 | 不引入跨任务同核驻留收益 |
| 二级共享缓存 | 不属于问题一 | 不引入问题三的二级缓存命中公式 |

来源：题目 Word（文字文档）及附件 `data/config.txt`（官方配置）、`code/multicore_cut_evaluate_problem_1.py`（问题一官方评估器）。算法侧的权重可以消融，官方配置不能改。

## 3. 我们现在怎样解决三个难点

### 3.1 从输入到结果的完整路径

```text
原始算子/张量图
  → 非复制算子块化、依赖与工作量统计
  → 四类构造范式 × 粗/中/细粒度 × 场景参数
  → 分层候选精化与去重，保留最多六个种子
  → 多种邻域/元启发式在共享预算内搜索
  → 归档、旧顺序微调、原官方候选复核
  → 可选：局部事件精评补候选
  → 可选：固定切分迁核/插入顺序搜索
  → 可选：拆分/合并后再微调顺序
  → 新方案原官方确认，不成功则回退
  → 保存算子映射、每核顺序、同方案指标与运行指纹
```

| 难点 | 当前方法 | 已解决的部分 | 尚未充分解决 |
|---|---|---|---|
| 切图 | HEFT（异构最早完成时间）式增长、拓扑条带、链式增长、净收益凝聚；三档粒度；边界移动与末端拆合 | 有多样化合法初始解，避免只依赖一种分区 | 粒度主要按算子数；按固定拓扑位置拆分；缺乏并行分支和溢出峰值引导 |
| 分核 | 最早完成估计、负载和通信启发式、构造精化迁核、搜索邻域 | 能利用多核并在代理层比较方案 | 前瞻不充分；共享带宽时序近似；部分候选按编号截断 |
| 顺序 | 代理生成核序列，旧相邻微调，新增固定切分迁核/插入束搜索 | 已把顺序作为独立可优化对象，合并图判环 | 末端主要为单任务动作，容易停在需要交换两任务才能改善的位置 |

### 3.2 代码模块及准确边界

以下路径均相对本项目根目录；名称是文件标识，不代表另一个独立系统。

| 文件 | 做什么、为哪个难点服务 | 当前状态 |
|---|---|---|
| `solver/model.py`（基础代价模型） | 块化、矩阵/向量工作量、边界和溢出近似、调度重放；供三类决策快速比较 | 主流程使用；不是完整官方模拟 |
| `solver/solution.py`（方案与约束） | 保存块到子图及分核关系，克隆、压缩编号、覆盖与无环检查、统一评估上下文 | 主流程使用 |
| `solver/construct.py`（基础构造） | 最早完成、拓扑条带、链式构造 | 主流程使用 |
| `solver/construct_v2.py`（净收益构造） | 合并簇及分核；容量和计算管道均衡 | 可选修正已存在，默认调用未启用修正 |
| `solver/construct_refine.py`（分层构造池） | 范式、粒度、场景及初始/迁核/边界/交换层，去重保留六种子 | 中图收缩粒度，非全图展开十二候选 |
| `solver/tabu.py`、`sa.py`、`aco.py`、`mopso.py`（四种搜索） | 禁忌、退火、蚁群、多目标粒子群式探索 | 共用预算，算法数量不等于有效多样性 |
| `solver/archive.py`（候选归档） | 保存耗时和新增搬运的非劣候选 | 主流程使用 |
| `solver/order_refine.py`（原顺序精化） | 原路径上的局部顺序微调 | 与新增末端模块并存 |
| `solver/spill_events.py`（溢出事件） | 逐访问分配、最后使用释放、逐出、写回和再次换入 | 新局部精评使用；样本字节一致不能外推全图已证明一致 |
| `solver/scene_a_event.py`（场景 A 局部精评） | 补真实子图边界复制、局部缓存和时间估计，再组合任务依赖 | 仍近似共享带宽和部分内存补边效应 |
| `solver/task_order_search.py`（固定切分调度搜索） | 迁核和任意插入；束宽 4、深度 3、最多 4000 次廉价重放、2 秒软预算 | 可选开启；已加入末端原位置双任务交换与八候选筛选，详见持续计划第14～15节 |
| `solver/partition_polish.py`（末端切分精化） | 最重三个任务按 1/3、1/2、2/3 拆分；按真实边界收益取六个合并 | 最多十二次局部精评、两次官方复核；仍较粗 |
| `solver/terminal_refine.py`（末端补候选） | 从候选/归档补充有价值方案，保留原官方保底 | 构造旁路收益成本不理想，暂不作为默认路线 |
| `solver/scene_a_fast.py`（快速官方语义副本） | 计数化复核，独立命名空间，官方源码指纹保护 | 保存方案 48+40 组完整结果一致；属于评估提速 |
| `solver/pipeline.py`（总流程） | 分配预算、衔接所有层、官方裁决、最终指标绑定和回退 | 原候选仍有 0.3% 容差，新末端严格耗时优先 |
| `solver/run_all.py`（批处理入口） | 进程池、逐任务进度、即时落盘和断点跳过 | 实验开关路径有指纹核验；基线路径不能一概称已有同等保护 |
| `solver/mamba_screen.py`（可选在线筛选） | 候选粗筛实验 | 不作为本轮已经验证的主要创新或默认学习模型 |

### 3.3 本次源码复核发现的三个结构问题

1. 构造池中 `R1`（第一精化层）按子图/核循环收集前八个迁核候选；`R2`（第二精化层）只检查前六十四条块边、最多八个变体。这容易把预算花在编号靠前的位置，而非真实瓶颈。
2. `R3`（第三精化层）函数虽然叫顺序精化，实际主要交换两个子图的核心归属，然后重新评估派生顺序。它不是完整的固定核内顺序优化。项目已有早期交换，不等于末端固定序列交换已经实现。
3. 非复制算子超过 2000 时构造池只用中粒度；总算子超过 12000 时总流程又退为简化路径并跳过常规官方验证。因而“范式 × 粒度”在较大图上并未完整生效。

这些是源码层面的确定观察，但不能直接宣称某个全量退化案例就是由其中某条造成；需要对应候选的受控重放。

## 4. 上一次全量实验到底说明了什么

### 4.1 统计来源与边界

主全量为 `results/solve_log_fixed_v1.jsonl`（修复版日志），400 个场景 A 配置：100 案例 × 四种核数。324 配置有官方值，76 配置只有代理值。上一退化版日志筛选场景 A 后配对。

原始基线取 `results/official/*_problem_1_N*_res.json`（原始问题一官方结果），单核分母取同目录 `*_singlecore_res.json`（原始单核结果）。**不把另一份 `solve_log.jsonl`（历史日志）误当用户最初的基线。**

本轮分析脚本为 `results/复盘问题一全量_v1.py`，证据为 `results/问题一全量复盘_证据_v1.json`。结果包含输入摘要、原始及单核结果摘要、逐案指标、保存方案摘要和覆盖检查。400 份方案覆盖和核序列成员检查通过；本轮没有重新官方执行全部方案，也未用这个统计脚本重新证明全部无环。

### 4.2 官方平均加速比

| 核数 | 原始基线 | 退化重构版 | 修复版 | 修复版平均并行效率 |
|---:|---:|---:|---:|---:|
| 2 | 1.740322 | 1.643841 | **1.763898** | 88.19% |
| 3 | 2.227565 | 2.117010 | **2.371323** | 79.04% |
| 4 | 2.621197 | 2.524842 | **2.889695** | 72.24% |
| 5 | 2.978966 | 2.797278 | **3.324048** | 66.48% |

每行均为相同 81 个案例的算术平均。单案加速比为单核耗时除以多核耗时，并行效率为加速比除以核数。此前表格 1.760/2.370/2.890/3.320 为旧统计值，本轮以原单核结果重算值为准。

### 4.3 与原始基线比较：整体更好，局部仍有明显缺口

| 核数 | 改善/退化/相同 | 退化超过 5% | 逐案等权耗时变化 | 总耗时变化 |
|---:|---:|---:|---:|---:|
| 2 | 37 / 39 / 5 | 9 | -1.16% | **+0.40%** |
| 3 | 46 / 28 / 7 | 12 | -4.96% | -2.87% |
| 4 | 49 / 27 / 5 | 12 | -7.22% | -4.03% |
| 5 | 49 / 29 / 3 | 9 | -7.69% | -5.56% |

合计 181 改善、123 退化、20 相同，其中 42 个退化超过 5%。2 核平均加速比上升而总耗时略上升并不矛盾，两者对大耗时案例的权重不同。今后的报告必须同时保留这些口径。

### 4.4 与退化重构版比较：修复有效，但不是逐案全胜

| 核数 | 改善/退化/相同 | 退化超过 5% | 逐案等权耗时变化 |
|---:|---:|---:|---:|
| 2 | 60 / 18 / 3 | 2 | -6.38% |
| 3 | 58 / 17 / 6 | 3 | -9.17% |
| 4 | 66 / 9 / 6 | 4 | -10.48% |
| 5 | 59 / 15 / 7 | 6 | -12.35% |

合计 59 个退化，其中 15 个超过 5%。不能把 19 个无官方值的案例当“平局”加入每核 100 的分母；旧记录中的部分平均耗时改善率存在这个口径问题。

### 4.5 典型错误与待验证原因

下表耗时变化均是修复版相对退化重构版；不是相对最初基线。

| 案例/核数 | 官方现象 | 已知结构证据 | 下一步隔离实验 |
|---|---|---|---|
| 049 / 5 核 | 143226 → 160260，慢 11.89%；新增搬运 532056 → 0 | 十一任务，最大任务含 91.84% 非复制算子；无溢出；搬运下界仅占耗时 1.59% | 固定其他部分，对最大任务做分支拆分及工作量拆分，再联合调整分核/顺序 |
| 082 / 5 核 | 624548 → 714761，慢 14.45%；新增搬运下降 | 各核任务数 11/4/0/0/0；无溢出；加速比约 1.009 | 先固定切分交换/迁核，再比较并行分支切分；核对空核期间是否确有就绪任务 |
| 063 / 4 核 | 313910 → 366270，慢 16.68%；新增搬运下降 | 三十四任务，核任务数 7/9/8/10；无溢出 | 保持切分，只改分核及顺序，检查关键任务是否被排在长队列后 |
| 007 / 4 核 | 257704 → 293980，慢 14.08%；新增搬运下降 | 十二任务，每核三个；无溢出 | 对比工作量、解锁后继和完成时刻，验证任务数均衡是否掩盖时序失衡 |
| 044 / 4 核 | 110763 → 126516，慢 14.22% | 五任务，分布 1/1/3/0；溢出占实际搬运 64.21% | 先定位局部溢出峰值再拆分，之后固定分区比较顺序，区分内存和时序影响 |
| 067 / 5 核 | 15604659 → 13701191，改善 12.20% | 溢出占搬运 77.44%，但加速比约 4.659 | 作为高溢出但已较强的保留样本，检查节省溢出是否真能降低耗时 |
| 005 / 5 核 | 修复版为单任务、加速比 1 | 四空核；原始计算图并行度指标约 18.10 | 检查构造/块化是否限制并行；不能先断言原图不可并行 |

最大任务算子占比不是计算耗时占比，原图并行度指标也不是实际可达加速比。上述指标用于定位候选，不能替代事件轨迹或受控干预。

### 4.6 全局问题分布

- 324 个官方配置中 59 个有溢出；把全部配置的字节加总后，溢出占实际搬运的 46.87%。这是字节加权结果，不能解释为“大多数图都严重溢出”。
- 69 个配置满足“实际搬运字节 / 60 / 总耗时 ≥ 0.5”。这是带宽下界接近度，不是直接测得的外存等待占比。
- 同案相邻核数共有 243 对，其中 49 对扩核后变慢；044 的 3→4 核慢 6.37%，057 的 4→5 核慢 9.11%，069 的 3→4 核慢 5.74%。需要把扩核稳定性加入验收。
- 有空核的案例数依次为 6、8、11、12。空核可能由依赖造成，也可能由算法错失并行造成，需以“空核且有合法就绪任务”的区间区分。
- 19 个仅代理案例为：003、014、016、025、028、041、047、053、054、058、062、072、076、079、084、085、087、091、092。是工程阈值和成本问题，不是题目豁免。

## 5. 后续小实验已经做到了哪一步

不要把下面的小样本进展当作新的 400 组全量结果。

| 工作 | 已有证据 | 尚不能推出 |
|---|---|---|
| 局部逐事件精评 | 固定 48 方案平均绝对百分比误差 41.105%→1.627%；另 40 方案 137.728%→2.639% | 全部候选排序更好、全部图语义一致 |
| 候选排序审计 | 历史 145 候选、48 配置，选中最优的比例 68.75%→87.50%；但平均选解损失 0.680%→1.077% | 平均预测误差下降不保证最终选解损失下降 |
| 快速三层：48 配置 | 对发布结果等权耗时 -3.97%，35 改善/2 退化/11 相同；同次保底比较 38 改善、无退化 | 对历史发布版逐案不退化、新全量已经领先 |
| 快速三层：另 40 配置 | 对发布结果 -2.05%，25 改善/4 退化/11 相同；同次保底比较 26 改善、无退化 | 065/4 核仍比发布慢 6.21%，需要局部诊断 |
| 快速复核副本 | 固定 48+40 保存方案完整官方返回对象一致；评估提速约 2.26 倍/3.59 倍 | 求解方案加速比提高同样倍数 |
| 独立构造旁路 | 48 配置有额外收益，但耗时增长，边际收益不足 | 候选越多越好、应该默认打开 |
| 净收益构造修正 | 复现簇编号错位读工作量、过期堆条目绕过最大簇大小；可选修正及回归检查已有 | 默认池已修好、修正必然提高方案性能 |

065/4 核是一个具体突破口：发布耗时 14967，快三层输出 15897。固定其十二个无子图间依赖的任务，单迁移诊断和九个拆分没有发现改善；跨核原位置双任务交换出现八个预测改善候选。预测第一的官方耗时为 15033，比 15897 降低 5.435%，距发布仅 +0.441%，新增搬运保持 55296。

这证明该实例有“单迁移卡住而双任务交换有效”的邻域缺口，但只做了少量诊断复核，不能宣称交换模块已实现或普遍有效。更不能把 MAGIS（内存图优化系统）中的内存换出动作误认成这里的跨核任务交换。

## 6. 综述与六个本地参考仓库：用了什么、还可学什么

### 6.1 资料核验范围

本轮核对了《近两年通用神经网络处理器多核调度：开源论文、可复现实现与技术路线调研》的原始文字文档，以及另一篇综述的已提取文本 `survey_doc.txt`（综述文本）；不称本轮已找到并读取两份原始综述文档。六个仓库的关键调度、分区、模拟和搜索源码均做了对应核对，但没有复现它们的论文全部实验。

综述可以提供方向，不为本题结果背书。现有模块与某论文思想相近，不等于直接移植或完整复现；本地普通有向无环图调度仓库含较早研究引用，也不能统一叫近两年成果。

### 6.2 逐仓库机制和迁移边界

| 参考项目 | 原代码解决什么问题、核心办法 | 我们已经有的相近机制 | 可迁移的新设计与禁止误用 |
|---|---|---|---|
| DAG Scheduling（有向无环图多处理器调度分析） | 等待/就绪/完成队列；所有前驱完成才调度；关键路径和任务优先级 | 基础就绪调度和关键路径重放 | 增加“执行后解锁谁”的优先级及空核原因分类；不能使用其无缓存/无共享外存模型替代官方 |
| dnn-partitioning（神经网络图分区） | 收缩分区图估延迟；在前驱闭合集上做动态规划，缓存局部代价；另有流水吞吐优化 | 合法商图、子图代价、合并/拆分 | 学前驱闭合的候选生成、局部代价缓存；不强加其特定设备连续性约束，不把流水吞吐目标当本题总完成时间 |
| DOPS（异构设备调度系统） | 最早完成调度及后继链前瞻，复制设备可用时间和缓存形成影子状态 | 最早完成构造、粗细评估 | 学有界影子状态前瞻；补齐链外前驱及带宽竞争，去掉语言模型专用缓存、预填充/解码和设备类型假设 |
| MAGIS（内存感知图优化系统） | 生命周期与峰值集合定位；变换、模拟、去重、预算控制；可换出和重计算 | 溢出评估、多个变换邻域、归档和预算 | 学“峰值涉及哪些张量，就在哪里动分区”，及变换/评估闭环；本题不能任意重计算算子或改写计算图 |
| Tessel（流水并行调度系统） | 给定放置后，用依赖、设备不重叠、内存约束优化调度与空泡 | 新增固定切分顺序搜索 | 学局部顺序窗口与交替优化；不移植微批次重复调度，固定时长约束求解也不能证明官方模型全局最优 |
| ONNXim（神经网络加速器模拟器） | 核心、片上存储、外存和互连的事件资源状态；搬运与计算独立推进 | 局部事件模型、官方语义快速副本 | 学竞争事件和失效缓存管理；不能为赛题增加其双缓冲槽数、片上网络或存储模拟器硬件假设 |

综述另提及 LATTICE/DAN-Scheduler（内存压力调度路线）、NPUMeter（性能建模路线）等，本地无完整对应源码，本文只把它们作为综述启发。

### 6.3 可复查的本地源码入口

公共根目录为 `J:/数学建模/参考模型/`。下面给出实际双层目录，行号仅作本次定位参考。

1. `dag-scheduling-analysis-master/dag-scheduling-analysis-master/src/sched.py`：约 116～176 行，就绪与完成事件、全前驱检查。
2. `dnn-partitioning-main/dnn-partitioning-main/latency-greedy/greedy.cpp`：约 457 行，收缩图延迟；`throughput-dp/dp.cpp`：约 1177、1445 行，闭合集代价缓存与动态规划。
3. `DOPS-micro26_pieak_final/DOPS-micro26_pieak_final/src/scheduler/scheduler_heft.py`：最早完成构造；`scheduler_bifocal.py`：约 473、508、630 行，后继链、影子模拟、窗口枚举。约 520 行明确不计竞争及链外依赖，必须改造。
4. `MAGIS-main/MAGIS-main/python/magis/simulator.py`：约 11、74、107 行，生命周期、峰值、内存变换候选；`optimizer.py`：约 245～312 行，去重、评估、搜索预算。
5. `Tessel-main/Tessel-main/tessel/schedule/solver.py`：约 81～119 行，设备、依赖与内存约束；约 246、300 行，步数和空泡求解器。
6. `ONNXim-master/ONNXim-master/src/Core.cc`：约 32～57 行，发射条件和缓冲管理；`Sram.cc`、`Dram.cc`、`Interconnect.cc`：存储和互连资源。

### 6.4 对旧调研建议的更正

- 旧报告“已等价实现、比论文闭环更强”等说法缺乏逐项证明，今后改为“思想对应、部分实现、样本验证”。
- 不再推荐场景 A 的跨子图同核生命周期复用，也不把合并后的字节节省归因于单纯迁核。
- 峰值超容量字节不是总溢出量；要统计多次写回与换入。
- `max(计算时间, 搬运字节/带宽)` 是理想重叠下的下界或粗近似，不是可靠上界。
- 不照搬没有实验依据的 0.45/0.25 等优先级权重；先用可解释排序、非劣候选和官方复核。
- 保底只保证相对本次已完成的基线不退化，不保证随机/墙钟搜索相对另一轮历史结果不退化。
- 旧代理字段可能绑定另一个方案，不能用旧代理/官方比值直接估计模型误差；必须重放同一保存方案。

## 7. 设计一：以张量、并行性和缓存压力共同决定粒度

### 7.1 为什么选择这条路线

方案甲是继续增加粗/中/细变体，容易扩大评估成本；方案乙是直接训练分区策略，当前标签和排序可靠性不足；**推荐方案丙：保留现有四范式，按真实瓶颈产生少量、彼此不同的分区候选。** 先改善生成动作的质量，再考虑学习筛选。

### 7.2 统一分区描述与边界字节

为每个任务缓存：矩阵工作量、向量工作量、局部精评时间、输入/输出张量集合、两池溢出事件、峰值时刻活跃张量、前驱和后继。

对张量 t，记大小为 b(t)，生产者所在子图集合为 P(t)，消费者所在子图集合为 C(t)。对每个子图 g 统计官方构图规则需要的输入和输出复制指示量 I(t,g)、O(t,g)：

`边界复制总量 = Σ_t b(t) × Σ_g [I(t,g) + O(t,g)]`。

同一子图内多个消费者共享一次输入，跨不同子图才分别输入；生产者仍有外部消费者时，不能因合并了其中一个消费者就删掉输出。原始输入、最终输出、无消费张量和特殊生产关系全部以官方构图规则为准。实现优先复用已存在的局部图推导，不另写一套简化规则代替它。

这样从“按边计通信”升级为“按张量跨越子图的关系计复制”，适合共享输入、一对多分支的图。

### 7.3 粒度生成：按工作与依赖，不只按算子数量

1. 保留当前三档作为对照；新支路对每核目标任务数取 2、4、8，对应目标工作量约为全图工作量除以核数及该档任务数。
2. 工作量使用矩阵/向量两个分量与局部时间，不能直接拿算子个数代替。以 `max(矩阵工作量, 向量工作量)` 作廉价增长尺度，同时保存两个分量。
3. 沿真实依赖或共享张量关系增长。达到目标尺度后产生一个候选；有强张量复用但会跨阈值时，另外保留一个继续增长的候选，而不是强行只有唯一边界。
4. 每范式最多保留两种明显不同的非劣粒度，初始全池最多十二种；中图不直接恢复完整笛卡尔乘积，而在现有中粒度之外优先补一个诊断需要的粗或细方向。
5. 最大子图算子数仍作成本保护，容量超限作溢出预警，不把“超过缓存”一律判非法，因为官方允许溢出处理。
6. 拟合优先级先用确定性键：预测完成时间、预计新增搬运、结构差异、稳定编号。昂贵精评前保留时间好、字节少、并行性好三类候选，避免单权重把一类提前删光。

### 7.4 三种具体分区动作

**动作一：工作量/溢出位置拆分。** 对当前关键路径上的重任务和溢出最多任务取并集，最多三个任务。保留现有三个工作量分位点，并在两池主要溢出事件前后各生成一个合法前驱闭合集边界。前缀只要求在该任务内部前驱闭合；拆分后还必须检查整个商图。候选过多时按时间下界、实际边界字节和预计溢出排序。

**动作二：并行分支拆分。** 对目标任务内部图做就绪层分析，在最宽的就绪层选择两个按剩余路径长度排序的不同分支种子。二分后继闭包作为对照；实际新增候选采用可达种子集合分区，分成零掩码前缀、各独占分支和多种子共享后缀，避免把汇合点绑进一条分支造成串行。具体边界预算与分核枚举以持续计划第16节为准，逐候选检查整个商图。该候选不要求在全图固定拓扑序连续。独立分支可以真正分核，严格链拆分则不会被误称提高并行。若块化把整个可拆分区域锁死，记录“块化受限”，另建限定局部的细块视图，而不修改全图默认块化。

**动作三：张量收益合并及边界移动。** 合并对象包括依赖相邻任务和共享大输入的消费者任务。每次按张量复制差重新算节省，再评估是否丢掉并行或增加溢出。边界移动只考虑关键链边界和主要峰值张量涉及的最多八个块；跨核边界也可以生成，不局限当前同核边界。每次完整覆盖和合并图判环，不把最先遇到的八条边当最有价值的八条。

### 7.5 耦合验证与接受规则

分区候选必须重建受影响任务的局部图和溢出，随后执行固定次数的分核/顺序微调；不能沿用原分区的局部时间。合并使任务数减少不算成功，减少复制也不算成功，以末端官方总完成时间为准。

首轮每配置最多十二个新切分精评，其中至少四个名额留给拆分、四个留给合并/边界移动，其余四个按诊断补充；某类无合法候选再释放名额。每候选最多四百次廉价顺序重放，最后按不同分区选两份全局复核。参数与当前末端规模接近，先避免用更多预算掩盖算法改善。

针对 049、005、082 验证并行分支；针对 044、067 验证峰值与复制权衡。非诊断样本也必须运行，避免只针对已知案例编号设分支。

## 8. 设计二：固定切分下的复合分核与顺序邻域

这是下一轮最先落地的功能，因为边界清晰、可以用保存方案隔离验证，065 已提供正向证据。

### 8.1 在哪里改

修改 `solver/task_order_search.py`（末端调度搜索），增加原位置双任务交换和短窗口重排；`solver/pipeline.py`（总流程）只接日志、开关及回退。先在已保存的完整快速三层最终方案上离线验证；接入端到端时先作为完整三层之后的独立末端步骤，保留原三层结果，再单独消融是否值得融合进原迁移阶段。不要同时更改默认构造、代理权重和搜索时长，否则难以归因。

早期构造层已经有核心归属交换。这里补的是**最终保存切分上的、保留其他核顺序的两个明确位置互换**，两者搜索对象和发生阶段不同。

### 8.2 双任务交换的完整流程

```text
输入：合法分区、当前每核顺序、同方案局部时间、官方保底
计算数据依赖 + 核顺序的完成时间和关键链
源任务：关键链按局部时间取前八个
补充：最晚完成核心的长任务，去重后源集合最多十二个
伙伴：其他核各选长任务与空泡相邻任务，去重后每源最多十二个
交换两任务原来位置；其他位置保持不动
检查覆盖；完整合并图判环；非法直接拒绝
廉价重放；完整序列签名去重；保留不同结构候选
与单迁移、核内插入一起做束搜索，父状态始终保留
局部/全局精评候选；官方耗时更小才接受，耗时相同再比新增搬运
任何评估错误或固定切分字节变化都拒绝并记录，返回原保底
```

如果总任务不超过二十，直接枚举所有跨核任务对，避免小图因筛选漏掉改善；大图才用上述至多 144 对的源/伙伴规则。全局最终判环允许拒绝间接依赖冲突，不能仅检查两任务间是否存在直接边。

束宽仍为 4，深度仍为 3，廉价重放总额仍为 4000。单迁移/插入先分 2000、交换分 1500、短窗口分 500；无候选的剩余额度顺次转给另外两类。2 秒软预算仍保留并单独报告是否触发，基准诊断另用固定计数以避免机器负载改变搜索轨迹。

### 8.3 短窗口顺序搜索

在关键核最显著的依赖等待前后，取最多六个相邻任务；固定窗口外顺序。递归枚举满足数据依赖的窗口拓扑序，最多考察六百个序列，再受上述总预算约束。每个序列仍做完整合并图判环；窗口外路径可能施加间接先后关系。

候选评分以重放完成时间优先，稳定序列摘要打破平局，不使用未经校准的多个惩罚权重。保留两个不同动作来源的复核名额；若同一动作占满而另一动作有预测改善，至少保留后一类一个名额，用于控制排序误差。

### 8.4 必须验证的行为

- 十二独立任务的固定案例可找到 065 已诊断的交换候选，但不能把具体任务编号写成生产规则。
- 链式依赖、跨核回边使合并图成环的交换被拒绝。
- 固定切分的原始/分区/溢出搬运统计不变。
- 无预测改善、超时、官方报错均返回原方案且保留错误原因。
- 单迁移已最优而交换可改善的小型人工案例能覆盖邻域能力；等成本或更差交换不替换保底。

## 9. 设计三：共享带宽事件状态与依赖完整的前瞻分核

### 9.1 先补状态，再加前瞻

当前局部精评把固定任务时间按依赖/核顺序组合，再用总字节除带宽限制整体时间。这个模型不知道两段搬运究竟什么时候重叠。直接在它上面增加前瞻，可能只是更努力优化错误时序。

因此分两步：先建立可重放的搬运/计算事件状态，再增加短窗口。拟新增 `solver/scene_a_replay.py`（共享带宽重放）与 `solver/lookahead_place.py`（前瞻分核），名字为设计接口，目前没有对应实现或命令行开关。

### 9.2 最小资源状态

状态至少包括当前时刻、各核当前任务与等待计时、各任务内部操作依赖计数、各计算/搬运管道可用状态、活动外存请求及剩余服务量、就绪队列、已完成任务时间。

局部模板必须来自包含官方内存补边的步骤输出，不能只把整个任务变成一个固定时长块。模板可通过现有官方语义构图/快速副本提取；官方文件保持原样。每个任务模板缓存键包含图摘要、精确成员及边界标识、官方源码与配置摘要、相关编号和并列决策顺序。未经证明不能归一化编号后复用，因为编号可能影响官方并列排序。

外存活动请求数为 q 时，按官方规则给每个请求相同服务份额。若剩余工作用独占周期表示，推进 Δ 时间则每个请求减少 Δ/q；若用字节表示则减少 60Δ/q。取整、最短周期、事件同时发生的处理顺序按官方实现对齐，不能随意把字节/60近似当精确结果。

下一事件是最近的计算完成、外存请求完成、等待结束或依赖解锁；推进后处理全部同刻事件，再重新计算活动请求的份额。不设置未经题目给出的“两条请求上限”，也不照搬参考模拟器的双缓冲槽数。

### 9.3 前瞻窗口的确切定义

借鉴 DOPS（异构设备调度系统）的影子状态，但不照搬其忽略链外依赖的假设。

1. 从真正可调度的就绪任务中选择当前任务 s；排序先看剩余关键路径，再看能解锁的后继工作量，最后稳定编号。
2. 对 s 的全部 N 个核心位置复制当前资源状态。向后看最多两个关键后继，即含当前任务的决策深度为三。
3. 窗口必须加入被选后继的所有尚未完成前驱，并递归补齐未提交前驱；已提交但未完成前驱继续在影子事件队列里执行。闭包总任务上限八个，超过时缩短前瞻直到只剩当前任务，不能忽略链外前驱。
4. s 尝试所有核心；后续两个有分核选择的任务各只保留最早完成估计最好的两个核心，因此主要分支至多 N×4。闭包中的其他任务采用确定性最早完成策略，不继续指数扩张。
5. 已经活动的核任务和外存请求继续推进；不能清空背景竞争。对窗口外尚未提交任务使用固定的参考就绪调度作为背景补全，所有分支用同一规则；若预算只允许局部估计，显式标记近似并缩短窗口。
6. 用窗口结束状态加剩余关键路径估计比较候选；这是排序启发式，不宣称完整真实完成时间或可证最优。若做完整参考补全，则直接比较补全后的模拟总完成时间。
7. 只提交 s 的核心与位置，后继分配仅作为影子假设；下一次调度根据新状态重新计算。

每构造候选最多二千次影子分支评估，超过后返回依赖完整的普通最早完成构造。先只对两个多样化种子启用，不向所有范式和每步邻域展开。场景 A 中任何分核选择都不得减去跨任务同核复制字节。

### 9.4 验收顺序

先在保存方案上对齐官方返回对象，尤其共享请求重叠、同时完成和内存补边；再比较候选排序的前二名召回及选解损失；最后才比较前瞻构造带来的端到端性能。若资源模型尚不可靠，保留它作诊断，不进入默认搜索。

### 9.5 融入用户新想法：事件驱动资源表

资源表作为上面共享带宽重放、前瞻及第 10.4 节核心协作组的公共状态基础。借鉴路由表的状态查询、变化通知与结果复用；第一版采用资源模块维护状态、单一事件循环统一提交更新，不让二至五个模拟核心分别开线程互相广播。所有表描述的是同一个模拟时刻，不能用过期资源状态抢任务。

| 表或缓存 | 必需字段 | 谁触发更新 | 禁止的错误复用 |
|---|---|---|---|
| 核心表 | 当前任务、队列指针、上一任务结束、各管道活动操作与队列、任务内存状态、版本 | 任务/管道开始与完成 | 把预计空闲时刻当成不会变化的预约；跨任务保留私有缓存 |
| 任务表 | 分配核心、未完成前驱数、依赖等待、剩余操作数、状态、模板标识 | 前驱完成、操作完成、切分改变 | 前驱数归零就立即执行，忽略核队首及 100/1000 周期等待 |
| 共享外存表 | 当前时间、上次结算时间、活动请求及剩余独占服务量、池版本、预计完成事件 | 请求进入/完成 | 新请求分到过去的带宽；每核都按独占带宽估算 |
| 区域协作表 | 区域成员、未完成任务、参与核心、释放状态、不同分区/核心集合的候选摘要 | 区域任务完成、候选变化 | 把区域多核时间当成不受组外负载影响的常数 |
| 局部模板缓存 | 原图/官方源码/配置摘要、成员和边界、编号顺序、局部依赖/溢出结果 | 首次精评或模板变化 | 只按子图成员命中，忽略边界编号变化造成的并列顺序差异 |

任务状态至少区分依赖未完成、等待未结束、具备启动条件、执行中、完成。固定方案重放只激活合法队首任务；构造阶段若要换顺序，生成新的完整方案并判环，不能在评估器中偷偷跳过队首。

事件处理流程：取下一个有效事件 → 按旧活动集合结算到事件时刻的外存服务 → 按官方顺序处理同刻完成/释放/依赖变化 → 更新状态与版本 → 检查任务与操作的发射条件 → 重算受影响事件。所有同刻零延迟变化按官方顺序处理至稳定。

请求变化会影响所有活动共享请求，继而改变其他核心及后继预测。维护任务到后继、张量到子图、请求到预测事件的反向关联；不能把“局部更新”理解为只更新当前核心。事件带候选分支身份及资源版本，旧版本完成事件无效；作废时必须补上新事件，避免漏唤醒。各候选独立修改自己的状态副本，禁止污染别的候选。

现有代码已有每核可用时间、就绪堆、整方案缓存和剩余操作计数。本次先在 E04 补局部模板缓存，再于 E06 统一全局事件资源表，不重新包装已有变量就宣称算法创新。先对固定方案验证评估耗时，再对同预算搜索验证方案质量；两种提速分开报告。

### 9.6 局部复用与可选增量重放的边界

只改分核/核顺序时复用完整摘要相同的局部模板，但第一版仍重放全局时间；共享带宽会把局部变化传播到其他核心。改切分则重建受影响模板，未受影响项也必须校验编号、边界和顺序摘要。

模板构图目前会生成新操作/张量编号。成员相同不保证编号相同，第一版允许先构图取得准确摘要，再复用后续昂贵局部处理，不能为了命中率擅自规范化编号。内存缓存设有界容量并记录峰值，性能测量包括摘要和缓存查询开销。

事件后缀重放安排为 E12 可选扩展：只有证明候选改变之前的事件前缀一致，才能从该前缀的完整检查点恢复。检查点必须包含全部活动外存请求、剩余服务量、任务/管道队列、计数及依赖等待；无法证明时从头重放。模拟状态检查点与跨进程实验断点分开，不把前者已有设计说成后者已实现。

## 10. 将三种设计融合成有预算的交替改进

### 10.1 瓶颈选择动作，不把诊断标签当定论

| 诊断信号 | 第一优先动作 | 第二优先动作 | 排除误判的对照 |
|---|---|---|---|
| 一个任务很长，空核时仍有潜在独立分支 | 分支/工作量拆分 | 拆分后的前瞻分核 | 固定切分仅调度，确认收益是否必须改变分区 |
| 关键核排队、任务有就绪空泡、无明显溢出 | 交换/插入/小窗口 | 前瞻分核 | 固定边界字节，比较官方时间 |
| 局部反复换入/写回集中 | 峰值附近拆分或边界移动 | 同输入消费者合并 | 重新计所有复制，不能只看少了多少溢出 |
| 同一输入被多个小任务重复读 | 张量收益合并 | 调整其时间位置 | 检查合并后丢失的并行与新增溢出 |
| 多段外存搬运同时拥挤 | 时序/顺序改变 | 降低边界复制 | 用全局事件重放确认，不只看总字节 |

事件诊断对每段空闲先区分“没有就绪任务”和“有就绪任务但当前核顺序/资源限制不能执行”。多种阻塞同时存在时保存多个标志；除非另定义互斥归因规则，不能把它们相加宣称等于总耗时。

### 10.2 保底与挑战支路

完整执行原基线路径，保存方案及官方值。新支路从这个方案或不同结构候选出发，执行：局部调度 → 定向分区 → 分区后的调度修复。每一轮完整方案经过全局复核后才更新保底，最多两轮。

先保留一份独立保底，防止“第一层局部更好，导致后续搜索反而更差”的路径效应。构造修正改变种子时，也要同时保留原种子支路做隔离比较，不能仅凭构造分数提前替换全部默认种子。

候选接受键为 `(官方总完成时间, 官方新增搬运字节)`。本设计建议新路线都严格使用该键；原候选的 0.3% 容差若要统一，必须单独做消融，不能声称本轮已经修改。

### 10.3 分层评估与预算

四级链路为：廉价结构重放 → 真实局部图/溢出精评 → 快速官方语义全局复核 → 原官方最终确认。中间层不可靠或异常时立即回退。

首轮沿用每层最多两次全局复核；整个新支路最多六次，原官方最终确认另记一次。报告每层候选生成数、去重数、非法数、精评数、复核数、官方胜出数、耗时、改善量。不能只列“耗时预算 2 秒”，因为最后一次调用和官方复核可能使实际总时间超过软预算。

评估器质量用“前二候选是否包含官方最好解”和“最终选解比候选池最好解慢多少”衡量，平均预测误差作为辅助。不同结构或两个代理分歧很大的候选保留少量探索名额；不要把新精评预测稍差当作绝对禁止复核。

实验分两条公平轨道：一条固定保存方案、固定候选次数验证模块能力；另一条统一端到端时间/评估预算验证部署价值。前者保底可严格单调，后者要报告原路径是否被预算挤占。不得把额外算力换来的全部收益归因于算法。

### 10.4 融入用户新想法：临时核心协作组

上层把计算图划为可研究的区域，决定优先处理哪个区域、尝试给它几个核心；下层把区域分成合法子图，安排到各核队列。一个官方子图仍只绑定一个核心，单个原始算子不允许自行改写成几个算子。所谓临时大核是调度层的合作，不合并缓存、计算管道和外存带宽。

区域选择：从保底方案取最多两个位于关键路径且内部存在并行分支的重区域。第一版区域等于一个完整已有子图，或若干完整已有子图的并集；保留全部区域外前驱/后继。严格链和单个巨算子作为反例，不按“工作量大”直接分配更多核心。

对于每个区域，尝试一至 N 个核心的实际子集；最多五核只有 31 个非空子集，可先廉价评估。每种数量同时生成分支保持型和工作量/溢出位置平衡型分区，复用第 7 节合法切分动作。保留区域外任务相对顺序，在候选核队列中寻找合法插入位置；不为组核强加全组空等或虚构起始时刻。

核心二先完成自己的分支时，可以在其后安排其他区域的合法任务；其他成员继续当前区域。汇合只遵守原图已有依赖，不增加人为全组屏障。求解器在模拟中按完成事件调整安排，最终仍输出静态队列，不能依赖官方执行一个额外在线组核控制器。

每个候选都查询 E06 资源表，保留组外活动请求和任务的竞争。评分是“保底整图预计完成时间减候选整图预计完成时间”；区域本身更快但整图更慢的候选不算改善。下层回传各核心数量的完整计划、复制/溢出和关键后继释放信息，上层不采用简单的单核时间除核心数。

首轮两区域轮流分配最多二千次廉价重放；每区域每核心数保留两种结构，最多二十份，再筛至十二份局部精评，最终两个不同结构候选全局复核。选择不同核心数需保留多样性，不能全部名额都被预测最佳核数占用。第 11 节独立实验预算和融合总预算分别执行，不把所有上限直接相加。

拟新增 `solver/region_group_refine.py`（区域协作精化模块），输入完整图、保底计划与官方基线、资源状态/评估回调，返回计划、同方案指标和审计。模块目前未创建。区域标识只写审计，不写官方提交字段。

必须比较：普通拆分加自由分核、固定两核组、自适应核心数量、自适应数量加逐核释放。各组候选数相同，判断收益是否来自联合动作而不是更多预算。重点 049、082、005；044、067 检查内存带宽代价，065 的交换证据不能直接当作组核证据。

### 10.5 参考代码在融合方案中的作用

| 参考来源 | 融合位置 | 必须保留的题目约束 |
|---|---|---|
| 普通有向无环图调度代码 | E03 的关键任务选择、E06 的就绪事件和前驱计数 | 全部前驱、核队首及等待条件完整 |
| 神经网络分区代码 | E04 局部缓存、E05 合法闭合集、E08 区域多候选 | 不强加单一拓扑序连续；目标为本题总完成时间 |
| MAGIS（内存感知图优化） | E05 峰值张量引导动作、E09 变换—评估—预算闭环 | 不重计算原算子，不改写原计算图 |
| ONNXim（加速器模拟） | E06 计算/搬运资源状态、事件推进 | 官方缓存、带宽、管道及等待语义不变 |
| DOPS（异构设备调度） | E07 影子状态前瞻、E08 区域核心候选 | 补齐链外依赖和共享竞争，不引入设备专用缓存 |
| Tessel（流水调度） | E03 小窗口顺序、E08 区域内顺序、E09 交替优化 | 不移植微批次重复调度，不把固定时长最优称为官方最优 |

两个新想法因此并入原有三个难点的解决链条：资源表是计算和决策基础，协作组是分区与分核的联合动作，独立顺序精化负责组内与组间队列。它们不是另起一套与原路线无关的系统。

## 11. 分阶段实施：统一由实验计划维护

详细阶段、状态、输入集合、修改文件、实验步骤、预算、消融、验收和失败后的决定，统一见 [《实验计划_问题一.md》](实验计划_问题一.md)。本节不再维护第二份可能分歧的任务清单。

确定顺序为 E01 实验协议 → E02 构造修正对照 → E03 交换/窗口 → E04 局部模板缓存 → E05 自适应切分 → E06 事件资源表 → E07 前瞻 → E08 核心协作组 → E09 预算融合 → E10 大图覆盖 → E11 冻结验证/全量。E00 历史复盘完成，E12 事件后缀增量重放后置保留。

这延续原来的“正确性与复合邻域优先、自适应切分随后、全局事件与前瞻再后”的主线。新增局部缓存置于昂贵切分前，统一资源表置于前瞻和组核前；两项用户新想法因此成为原计划的组成部分。

旧版第 11 节的阶段 1 对应 E01；2A 对应 E02；2B 对应 E03；3 对应 E05；4 拆成 E04/E06/E07；5 拆成 E09/E11；6 对应 E10。E08 是新增的联合区域动作，E12 是可选成本优化。这个映射用于保留计划连续性，不把已写设计当作已做实验。

当前已有 88 配置开发输入、6 个额外诊断案例；新计划固定另 20 个保留验证案例，各核均有原官方可比值。清单与抽取规则写在实验计划第 4 节，后续不随结果挑选。历史数据已分析，保留验证只对下一轮调参隔离，不宣称从未见过数据。

全量统计继续严格区分官方、代理、超时和失败；超过 5% 的未解释退化必须诊断。发布评定包括各核平均加速比、逐案及总耗时、求解成本、指标绑定和官方覆盖；不把平均变好当逐案不退化。具体执行门槛及三种基线按实验计划第 4、5 节。

## 12. 可直接交给实施模型的任务约定

目标：按《实验计划_问题一.md》的当前阶段顺序改进问题一，不改变题目、数据、官方参数及输出格式。先阅读本地两份主文档、本文、相关测试、保存方案和对应源码；不得把本文的设计状态写成已实现。

每一阶段开始前，向用户说明将改哪些文件、具体邻域/状态/公式和验证方法，按用户项目规则确认设计后再改求解代码。本轮只提交设计和复盘，不因以前授权过自主迭代就自动开始新阶段。

每项改动必须保留一个关闭开关的原路径、明确候选预算和出错回退。新增功能全部进入运行指纹；代码或参数变化新建实验日志和计划目录，不接着旧版本日志混跑。每完成一个任务立刻保存计划和指标，显示“已完成/总数、阶段、当前案例、失败数”。未完成项重启后继续，已有文件不删除。

先在人工反例与固定保存方案验证机制，再在开发集合比较端到端效果。不得只增加镜像实现的测试；重点检查合并图循环、共享张量重复计数、反复溢出、带宽竞争、同时事件、超时回退及指标绑定。

每轮更新本分支两个上传文档：技术路线说明当前技术/实现理由/下一步计划；实验记录追加命令、版本、参数、集合、所有指标、退化分析和是否采用。文档头写分支、代码版本、更新时间到分钟。本文与《实验计划_问题一.md》在本地持续维护；不擅自改变团队的两份上传文档规范。实验结束必须同步计划阶段与下一步，不另写新的计划文件。

本项目当前没有神经网络训练。现有离散调度与官方事件模拟在处理器上运行；不能为满足形式要求把它称为已使用 GPU（图形处理器）。若未来尝试学习候选排序，另立实验并说明哪些张量批量计算真正使用 RTX 3070（英伟达显卡）8GB 显存；模型下载先检查 `J:/models`，需要下载时遵守 `J:/nlp/models` 目录约定。本轮不下载模型、不新增训练依赖。

新主线优先使用现有 Python（编程语言）环境与标准库，不引入参考仓库的整套旧依赖。若后续用约束求解器或学习框架，先独立记录解释器、库版本和兼容性，验证后再纳入可复现环境。

## 13. 本轮复盘与已有实验入口的命令

以下每条都是可在 CMD（命令提示符）中执行的单行命令。第一条只做本轮统计；后面的求解命令是已有功能的复现实验入口，本轮没有执行。尚未实现的交换、前瞻与新分区功能不提供虚构开关。

### 13.1 打开命令提示符，重算历史全量证据

读取既有日志、单核/多核官方结果和保存方案，显示 400 份计划检查进度，重建本轮统计文件。统计是约数秒的可重复计算，不运行求解器；中断后重新运行即可。

```cmd
cd /d J:\数学建模\改进 && python -X utf8 results\复盘问题一全量_v1.py
```

### 13.2 在开始任何新算法实验前运行现有检查

验证当前工作区测试；本轮只修改分析与文档，没有借此宣称已重新通过全部求解器检查。

```cmd
cd /d J:\数学建模\改进 && python -m pytest tests -q
```

### 13.3 可选：复现当前快速三层开发样本

以九个诊断案例、四种核数重新运行已有功能，使用独立日志和计划目录。会消耗求解时间；不是本文尚待实现的下一版。相同代码、数据和参数下重复同命令可续跑已完成记录之后的任务；变更代码需换新实验名。

```cmd
cd /d J:\数学建模\改进 && python -X utf8 solver\run_all.py --cases 5,7,44,49,57,63,65,67,82 --cores 2,3,4,5 --scenes A --workers 4 --event-rerank --task-order-refine --partition-polish --fast-candidate-eval --log-file results\solve_log_review_replay_v1.jsonl --output-dir results\plans_review_replay_v1
```

### 13.4 对比上一条实验与发布全量的官方交集

生成配对耗时、胜负和退化统计。脚本只比较两份日志的共同官方任务，不把少量案例冒充全量。

```cmd
cd /d J:\数学建模\改进 && python -X utf8 solver\compare_runs_audit.py --base results\solve_log_fixed_v1.jsonl --new results\solve_log_review_replay_v1.jsonl --out results\review_replay_comparison_v1.json
```

下一版新增接口实施完成后，必须再提供从检查到开发集、独立验证、全量和对比报告的整套单行命令。现在先固定设计，避免把尚不存在的参数当成可执行方案。

## 14. 本轮证据索引与最终设计边界

- `results/问题一全量复盘_证据_v1.json`：本轮逐案证据、哈希摘要和统计。
- `results/fast_three_stage_analysis_v1.json`：已完成四十八配置的同次与历史对照。
- `results/fast_three_stage_validation_analysis_v1.json`：另四十配置的同次与历史对照。
- `results/diagnostic_case065_N4_comparison_v1.json`：065 原方案与新方案诊断。
- `results/diagnostic_case065_N4_swaps_v1.json`：065 双任务交换候选及部分官方验证。
- `技术路线_W分支_v1.0.md`：发布与本地工作区的整体实现路线。
- `实验记录_W分支_v1.0.md`：按时间追溯实验的主记录。

最终验证假设为：张量与生命周期改善切分，事件资源表减少重复评估并提高状态可靠性，依赖完整前瞻改善分核，临时核心协作组增强区域联合决策，复合邻域与统一预算提升同预算搜索效果。当前只能支持分阶段实验，不能承诺提升比例。执行顺序和采纳决定统一以《实验计划_问题一.md》为准。


## 14. 连续实验设计修订（2026-09-25 03:57）

E05分支88对照、峰值及累计热点诊断、四来源融合首轮已完成开发证据。新融合虽然对输入9胜1平，却损失独立分支7组优势；保存候选定位到成员分核代表与粗筛丢失，旧依赖代理亦未稳定改善。因此不继续围绕十组调参，四来源保持可选、统一融合改进回到E09。E06按原顺序实施状态模型，解决共享外存传播、依赖等待及候选预测不足；没有将E05保留验证/生产融合标为完成。

E06首子项只实现共享外存表和原语义从头重放，已有分数服务、加入/退役、预测失效和候选隔离反例。固定88方案原官方/旧计数副本/新资源表完整核验中。下一步核心/任务前驱计数、队首唤醒、版本与原因接口，随后才允许E07查询；不提前做E12后缀恢复。新增细节和命令统一见实验计划17节与实验记录36节，不再开另一份计划文档。


连续修订 2026-09-25 04:43：E06外存表和任务核心表各88完整等价、缓存协同34组256候选等价，含初始化成本下降38.2743%；只采纳为可选评估接口，完整快照/区域表/后缀恢复未完成。E07深度1/3八配置无新增收益，定位到065独立任务无后继、窗口为空，正在单变量补就绪前沿；不通过增加实验规模掩盖动作空间未激活，也不把固定方案评估提速说成加速比提高。问题三完整三组对照、大图官方校准、完整GPU（显卡）代理评分仍未完成。


连续修订 2026-09-25 04:52：E07就绪前沿与数据前驱完成边界分别完成065两配置消融，没有找到好于保底的未来叶子。新增成本与内部退化已记录，不能只看最终零退化。下一步为原输入三根的合法插入全覆盖审计，区分动作没有潜力与粗筛漏选；仅额外诊断，不扩大正式预算、不从弱基线替换当前比较。E08区域协作仍按原设计排在E07机制检查之后，E05生产融合仍回E09。


连续修订 2026-09-25 05:27：E07开发窗口/边界/保留当前动作及覆盖诊断完成，性能收益未证实，暂不采纳。E08固定切分原子区域已有真实四核联合收益：067原16269017，联合16245921；只动0为16443762，只动18为16253281。联合动作使区域自身更晚结束而整图更早，后续区域代价必须含组外影响，不能只最小化区域完成。参考代码启发仍是Tessel（流水调度）的依赖/设备约束和普通分区的合法结构菜单，不能移植硬件假设。

下一步具体算法已汇入唯一执行计划19.2：最多两原完整任务区域、两种内部结构×宽度、所有物理核心子集、保留组外核序的完整列表构造、2000廉价/20池/12资源/2官方。以E05分支拆分恢复005/082内部并行，再验证核心集合与次序的联合选择；E07前瞻继续关闭、融合预算在E09处理。固定切分两轮共145份保存计划合法；跨轮次复用候选与官方成本分开，一次重复官方4.3973秒明确记录，不删原始证据。问题三、大图官方与完整GPU（显卡）评分仍保持未完成。


连续修订 2026-09-25 05:46：E08从双任务原子动作推进到固定E05内部切分的完整区域队列接口。六个较强E05基线1胜5平，082两核900周期收益来自长分支与共享前缀同核、短分支迁出，未减少字节。51原预算候选外补齐32，共83菜单，未发现额外潜力；不因多数持平否定方向，也不再重复相同菜单。下一步按唯一计划19.4扩展结构菜单，保持完整依赖、组外顺序、真实核心集合和全图目标。19.2的两范式20结构联合生成仍未实现，不能因接口测试通过就标E08整体完成。


## 最新实施决策：粒度和核心集合需要独立控制（2026-09-25 06:13）

两范式结构菜单十配置已完成但与独立分支互补，未替换发布默认。067均衡二分改善来自单核细粒度队列而非实际多核，说明结构粒度不应直接绑定请求核心数。下一轮固定结构菜单，仅在每结构枚举1～N全部核心子集，以全图完成时间裁决，不按区域结束时间裁决；旧完整输入、同计划复用和20/12/2预算保留。新增集合覆盖旧集合属于生成层验收，不代表有限筛选预算性能必然提高。

优先级：①解耦对照及旧候选覆盖；②通用分支结构多样性，解决每宽度一个代表漏掉其他层；③在保存候选上定位粗分、队列构造及预算损失；④E09共同预算融合与保留集，之后才新全量。各阶段必须单变量、区分成本和代理/官方，不以单轮失败否决整个组件。实施唯一入口仍为实验计划19.6，事实见实验记录38.7；问题三、大图官方及完整显卡评分保持未完成。


最新实施同步（2026-09-25 06:29）：核心集合解耦与受限层间分支多样性均已通过十配置回归/合法性/恢复，但没有新增官方收益，保持可选、不切发布默认。多样性按工作量加权同组关系分歧补结构，保留旧代表且总量20；评分预算仍12资源/2官方。三轮完整方案可跨清单验证复用并保留来源。005旧好结构还受同层种子选择限制；049四核已有同分区但核序不足。现在按实验计划19.8做独立限额潜力审计，再决定种子组合/顺序精化/E09共同预算顺序，详细事实见实验记录38.10。问题三、大图官方、完整显卡评分未完成。


最新实施同步（2026-09-25 06:48）：seed_combinations（同层种子组合）菜单在保留原结构后填补20结构内剩余名额；两核五配置对上轮2胜3平，005恢复92850、049恢复156634，未全面超过旧独立分支。143回归通过，65计划合法；一次遗漏旧E05证据导致的重复官方1.5459秒已公开计费，现有官方注册表完成兼容核验并导入旧证据。完整实验事实与CMD（命令提示符）命令见记录38.12。下一步按唯一实验计划19.10在冻结成员切分上接现有迁移/交换顺序精化，先建设覆盖菜单、E05官方和58份诊断的证据索引，后做额外12资源/2官方的强保底组件消融；不把额外预算伪装为原菜单预算。默认主流水线未替换，问题三、大图官方、完整显卡评分未完成。


最新实现与下一阶段（2026-09-25 07:22）：新增分级证据索引、冻结菜单的局部时长近似/精确搬运统计、迁移与交换交错搜索、显卡批特征核验、额外12评分/2官方及候选内恢复。151回归通过，十配置对当前强菜单2胜8平；067两核32146123、049四核147111。067胜者来自原来较慢的切分，交换任务36与58后获得收益，说明不能只对当前一个最佳切分重排；交换本身不改变搬运。额外阶段78新资源/3新官方，不能视为原菜单同预算。事实详见原实验记录38.14，完整算法入口为solver/menu_order_polish.py（菜单顺序精化）和solver/experiment_menu_order.py（实验入口）。

后续坚持原E09约定：6次新增全局复核、调度/切分/区域协作各2份、最终1次官方；先冻结候选和六份入围摘要，再查真值，完整计划去重与分级证据复用。机制组、开发88、预留80和端到端共同墙钟逐级验证，具体执行步骤已写实验计划19.11。当前没有未完成运行需要恢复，不重新跑旧目录，不推送；问题三、大图官方、完整显卡评分尚未完成。


### E09共同预算机制组交接（2026-09-25 07:47）

六次逻辑全图评分、最多一次官方已经在十个机制配置跑通：60评分中12新增/48复用，3新增官方；生成/候选/配置恢复与空续跑通过。相对E0310胜，相对强菜单＋顺序1胜5平4负，不切默认。044四核94919和067两核32155407恢复旧工作量组件，不计新发现。四个退化配置的好候选已存在而被混合口径粗筛排除，下一项仅在原冻结池统一局部事件近似及核序重放口径，配额和6+1不变。完整事实见实验记录38.15；唯一实施计划见实验计划19.13。88开发、80保留、全量、问题三三组对照、19大图官方及完整显卡事件评分均未因此完成。


### E09最新验收位置（2026-09-25 08:14）

十配置完成统一近似与总复制同分键两项单变量，最终相对原强菜单＋顺序2胜7平1负；082两核首次官方696861，067四核仍退化42429周期。157回归通过，实际显卡特征一致，分批及空续跑通过；四份缺失的完整资源事件已独立采集，0新增官方。当前新400全量、88扩展、80保留及默认替换均未进行。完整事实见实验记录38.16～38.18，后续仅按实验计划19.16从已有事件分析资源竞争；问题三、大图官方、完整显卡评分仍未完成。
