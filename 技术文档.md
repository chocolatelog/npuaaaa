# 通用神经网络处理器多核调度问题 —— 技术文档

版本：`main-online-shared-v1.1.0`  
所属分支：`main`  
更新时间：2026-09-25 11:25  
当前状态：问题一在线共同预算流程已完成 400 组端到端验收；324 组官方、76 组代理严格分开。该文档维护项目整体实现，主分支详细算法路线见 [技术路线_主分支最佳流程_v1.1.md](技术路线_主分支最佳流程_v1.1.md)，逐次实验事实见 [实验记录_主分支_main-online-shared-v1.0.md](实验记录_主分支_main-online-shared-v1.0.md)。

> 求解 2026 年研究生数模竞赛 A 题（华为）"通用神经网络处理器下的多核调度问题"
> 算法代码：`solver/`　|　结果数据：`results/`　|　官方评估器：附件 `code/`

---

## 目录

1. [问题定义与总体思路](#1-问题定义与总体思路)
2. [系统架构](#2-系统架构)
3. [图模型与预处理](#3-图模型与预处理)
4. [快速代理评估器](#4-快速代理评估器)
5. [构造式初始解](#5-构造式初始解)
6. [元启发式算法](#6-元启发式算法)
7. [多目标优化框架](#7-多目标优化框架)
8. [Mamba 式 SSM 候选粗筛层](#8-mamba-式-ssm-候选粗筛层)
9. [方案合法性与真值校验](#9-方案合法性与真值校验)
10. [工程实现与运行配置](#10-工程实现与运行配置)
11. [实验结果](#11-实验结果)
12. [局限与改进方向](#12-局限与改进方向)

---

## 1. 问题定义与总体思路

### 1.1 输入 / 输出

**输入**：DAG 计算图（`tensors` / `ops` / `edges`，Op–Tensor 二部图）+ 核数 N（2~5）+ 固定硬件配置（`config.txt`，不得修改）：

| 参数 | 值 | 含义 |
|---|---|---|
| L1 | 524288 B | Cube 私有缓存 |
| UB | 131072 B | Vector 私有缓存 |
| DDR 带宽 | 60 B/cycle | 全核共享（读+写） |
| 场景A跨核/同核等待 | 1000 / 100 cycles | 每个 Task |
| 场景B跨核同步 | 500 cycles | 每对 COPY_OUT→COPY_IN |
| L2 Cache | 1 MB @ 250 B/cycle | 只读 FIFO，带宽独立（问题3） |

**输出**（方案 JSON，仅两个字段）：

```json
{
  "node_to_subgraph": {"<非COPY op id 十进制字符串>": sgid, ...},
  "core_schedules":   [[sgid, ...], ...]        // 外层下标=核id，内层=执行顺序
}
```

### 1.2 三重耦合决策

切图（子图归属）× 分核（子图→核）× 排序（核内顺序）相互耦合：
切多→并行度高但跨子图搬运与 Task 等待多；切少→通信省但缓存压力大（spill）且并行度低。

### 1.3 求解思路

```
官方评估器（秒级~分钟级，只能最终验证）
        ▲ 真值校验（≤1.2万op用例，最终从~5个候选中择优）
        │
快速代理评估器（毫秒级，搜索内循环）
   · 新增搬运量：与官方逐字节一致
   · Makespan：事件仿真 + cp + spill + 带宽下界
        ▲
构造池（9个初始解）→ 禁忌搜索 → 模拟退火 → 蚁群 → MOPSO
        └──────── 共享 Pareto 归档（双目标）────────┘
        └── SA 内环可叠加 Mamba 式 SSM 粗筛层（省 40% 完整仿真）
```

核心原则：**搜索空间中的任何分配方案都先修复为合法方案**（商图无环），
**每核顺序由事件仿真确定性派生**（拓扑一致），从而把不可行解排除在搜索之外。

---

## 2. 系统架构

### 2.1 模块清单（`solver/`）

| 文件 | 职责 | 关键类/函数 |
|---|---|---|
| `model.py` | 图模型、块粗化、精确边界流量、Makespan 代理、方案编码 | `Model`, `_tarjan_scc` |
| `construct.py` | 构造式初始解（HEFT/条带/链式） | `heft_construct`, `strip_construct`, `chain_construct` |
| `solution.py` | 解状态、邻域动作、环修复、筛选集成 | `Sol`, `Context` |
| `tabu.py` | 禁忌搜索 | `tabu_search` |
| `sa.py` | 模拟退火（含 SSM 锦标赛） | `sa_search` |
| `aco.py` | 蚁群算法 | `aco_search` |
| `mopso.py` | 多目标粒子群 | `mopso_search`, `decode` |
| `archive.py` | Pareto 归档 | `ParetoArchive` |
| `mamba_screen.py` | Mamba 式 SSM 粗筛器 | `MambaScreen` |
| `pipeline.py` | 单任务求解流水线 + 官方真值校验 | `solve_case`, `real_evaluate` |
| `run_all.py` | 并行全量求解入口 | `solve_task` |
| `evaluate_official.py` | 官方评估器批量评估（子进程） | `run_one` |
| `aggregate.py` | 汇总 CSV/曲线/附录 | `main` |
| `eval_metrics.py` | 按官方三指标评估本模型 | — |
| `ab_test.py` / `ab_budget.py` | SSM 粗筛层 A/B 对照 | — |

### 2.2 数据流

```
data/case_XXX.json ──run_all.py(并行8)──> solve_case() ──> results/plans/{case}_{scene}_N{n}.json
                                                │
solve_log.jsonl（每任务的估计值/真值/日志）<──┘
plans ──evaluate_official.py(并行)──> 官方评估器CLI ──> results/official/*_res.json
official ──aggregate.py──> summary.csv / summary.md / appendix.md / plots/*.png
```

---

## 3. 图模型与预处理

### 3.1 op 级 DAG（收缩 COPY 节点）

将 Op–Tensor 二部图转为 op 依赖图：tensor 的 eligible 生产者 → eligible 消费者
连边；COPY_IN/COPY_OUT 通过 DFS 路径收缩（与官方 `derive_multicore_plan` 一致）。
同时保留 tensor 级信息（生产者/消费者全集、size、pos、has_COPY_OUT）供流量精确计算。

### 3.2 路径式块粗化

**目的**：op 数最多 3.5 万，直接在 op 级搜索维度过高；以"块"为搜索原子。

**算法**（`Model._build_blocks`）：按拓扑序遍历 op，优先追加到
**前驱恰好是该块尾**的块（路径式增长，防止交错），其次归入任意有容量的
前驱块，否则开新块。块容量 `cap = clamp(n_eligible // 400, 24, 240)`——
保证子图可贴合 L1/UB 容量（粒度过大无法做出 cache-fitting 的切分）。

### 3.3 SCC 缩聚（合法性关键）

> **关键发现**：DAG 的任意划分，其子图商图**未必无环**——子图沿路径交错
> （A→B→B→A）时商图成环，这正是题目要求"子图依赖图必须为 DAG"的原因。

块构建后对块商图跑迭代式 Tarjan SCC，非平凡 SCC 内的块合并为一，
直至无环（缩聚一次即达不动点，循环上限 8 轮防御）。搜索中每次评估前
同样对解做 SCC 合并修复（`Context.repair`），**保证产出的方案恒合法**。

### 3.4 tensor 分组去重

按 `(生产块集合, 消费块集合, has_out, pos)` 签名聚合 tensor 字节数，
把逐 tensor 的边界计算压缩为逐"组"计算——链式图上组数远小于 tensor 数，
评估成本从 O(#tensors) 降至 O(#groups)。

---

## 4. 快速代理评估器

搜索内循环的唯一评估器（`Model.evaluate`），单次 2~20ms，包含四部分：

### 4.1 新增搬运量（与官方逐字节一致）

复刻官方边界 COPY 插入规则，扣除原图固有搬运（合成 COPY 替换原 COPY）：

- **场景 A**（每子图一个 Task）：
  - 输入边界：消费子图无本地生产者 → 插 COPY_IN，`+size`；
  - 输出边界：生产子图存在外部消费者或原图 COPY_OUT → 插 COPY_OUT，`+size`。
- **场景 B**（每核合并一个 Task）：
  - 图输入：每个消费核各读一次 `+size`；
  - 图输出：每个生产核各写一次 `+size`；
  - 跨核：每个（源核,目标核）对插 COPY_OUT+COPY_IN，`+2·size`；
  - 同核：零字节（L1/UB 直接复用，`C_inner < C_cross` 的来源）。

**验证**：同一方案下代理与官方评估器的 `partition_added_copy_bytes` 完全相等
（如 case_044: 2265056 B = 2265056 B）。

### 4.2 Makespan 估计

1. **子图时长** `d(s) = max( Σwork_M, Σwork_V, in_bytes/60, out_bytes/60, cp(s) )`
   ——四条 Pipe 并行的下界 + 子图内部串行关键路径
   （块级最长路，块权 = `work_M + work_V + 出入流量/60`，捕获深链串行性）。
2. **事件仿真派生每核顺序**：就绪子图按最早可开始时间出堆、追加到其核——
   顺序天然满足拓扑一致性；等待模型：场景A `max(核上一Task结束+100, 跨核前驱+1000)`，
   场景B `max(核空闲, 跨核前驱+500)`。
3. **spill 惩罚**：L1/UB 双容量池（CONV/MATMUL 走 L1 512K，向量 op 走 UB 128K），
   每池取两个信号的较大者：
   `over = max( 区间驻留峰值, 完全内部张量总字节 ) − 0.85·cap`
   （前者捕获宽图并发驻留，后者捕获深链 LRU 反复换入换出）；
   `spill_bytes += 2·over`，`spill_time += 2·over/60` 计入 Makespan。
4. **全局带宽下界**：`Makespan ≥ 全部 DDR 搬运字节 / 60`（多核共享带宽的硬约束，
   对多核方案是关键的排序信号）。

### 4.3 精度实测（648 个真值校验样本）

| 指标 | 精度 |
|---|---|
| Makespan 排序相关（Spearman） | **0.953** |
| Makespan 绝对 MAPE | ~72%（中位高估 44%，源于 spill/带宽保守 + 真值择优的选择偏差） |
| 新增搬运（切分部分） | **逐字节精确** |
| 新增搬运（含 spill 估计）排序相关 | 0.547（最薄弱环节） |

### 4.4 评估缓存

以 `(tuple(sg_of_block), tuple(core_of_sg), scene, N)` 为键的 LRU（8192 条），
SA/TS 回访邻域时大幅命中。

---

## 5. 构造式初始解

| 构造器 | 思路 | 适用 |
|---|---|---|
| `heft_construct` | 块按递减 upward rank 处理，最早完成+通信局部性选核，每核序列按 `max_sg_ops ∈ {80,160,320,640,1280}` 切子图 | 通用（主力） |
| `strip_construct` | 拓扑序切 contiguous 条带（2N/4N/8N 条），条带贪心分核 | 深图 |
| `chain_construct` | 块链独占 + LPT 均衡分核 | 宽并行图 |
| 整图基线 | 全部 op 一个子图（单核串行） | 兜底对照 |

构造池共 9 个初始解全部入 Pareto 归档；**池前三名也进入最终真值校验候选集**
（代理模型在"单子图 vs 多子图"上的排序偶有偏差，由真值裁决）。

---

## 6. 元启发式算法

共享解表示与邻域（`Sol` + `Context`）：

### 6.1 邻域动作（5 种）

| 动作 | 说明 | 触发权重 |
|---|---|---|
| `move_block` | 块移入另一子图 | 40% |
| `move_core` | 子图换核 | 15% |
| `merge` | 两子图合并（自适应子图数） | 20% |
| `split` | 子图按拓扑中位一分为二 | 15% |
| `swap` | 交换两块归属 | 10% |

定向变体 `targeted_move`：优先选**跨子图高流量边**上的块移动（通信驱动）。
所有动作后经 `repair()`（SCC 合并）保证商图无环。

### 6.2 禁忌搜索（主力，89% 任务上带来改进，平均 8.7%）

- 每代采样 12 邻居，取最优可行（禁忌属性=动作对象，任期 `0.6·√块数`）；
- 渴望准则：劣于历史最优的禁忌解不放行，优于则放行；
- 按完整仿真次数或墙钟双预算停止。

### 6.3 模拟退火（30% 任务再改进 1.5%）

- 初温 `0.15·f_cur`，几何降温 0.995，`exp(-Δ/T)` 接受劣解；
- 50% 概率使用通信定向邻域；收敛后从最优解扰动重启；
- **SSM 粗筛层的宿主**（见第 8 节）。

### 6.4 蚁群（3% 直接改进；归档多样性贡献）

- 信息素 `τ_attach[块][0..6]`（0=新建簇，1..6=并入最近开簇）、`τ_core[块][核]`；
- 蚂蚁沿块拓扑序构造：`P ∝ τ^α · η^β`，η 为通信亲和度/负载均衡启发式；
- 每代前 1/3 精英按 `1/(1+rank)` 沉积，蒸发率 0.15，信息素下限 0.05。

### 6.5 MOPSO（17% 任务再改进 4.1%）

- **随机键编码**：粒子 `x ∈ [0,1]^(2·nb+1)`——每块 `cut`（切分得分）与
  `pref`（核偏好）两基因 + 全局基因 `g`（目标子图数 K∈[Kmin,Kmax]）；
- **解码**：拓扑序上 `cut` 得分最大的 K−1 个位置为切点 → **凸切分**
  （商图必无环）；子图沿拓扑序分核：最早完成 + `pref` 票数偏置；
- 速度更新 `v = w·v + c1·r1·(pbest−x) + c2·r2·(leader−x)`（w=0.5, c1=c2=1.2，限幅 ±0.25）；
- 领袖从**基因空间局部 Pareto 前沿**按拥挤距离轮盘赌选取（前沿容量 24）。

### 6.6 各算法实测贡献（800 任务归因）

| 阶段 | 带来改进的任务 | 平均幅度 |
|---|---|---|
| 禁忌搜索 | **89%** | **8.7%** |
| 模拟退火 | 30% | 1.5% |
| MOPSO | 17% | 4.1% |
| 蚁群 | 3% | 3.2% |

---

## 7. 多目标优化框架

- **目标向量**：`(估计 Makespan, 估计新增搬运字节)`，均最小化；
- **标量化**（SA/TS/ACO 引导与最终选择）：`f = mk + w·added/60`，`w=0.2`
  （把字节折算为 60 B/cycle 带宽下的周期数，保持 Makespan 主导）；
- **共享 ParetoArchive**（容量 48，支配过滤 + 拥挤距离剪枝）：四个算法全部供解，
  MOPSO 直接在其上选领袖；
- **最终决策**：`archive.best_by_scalar(0.2)` 与搜索最优、构造池前三一起
  构成候选集，≤1.2 万 op 用例由官方评估器真值裁决。

---

## 8. Mamba 式 SSM 候选粗筛层

### 8.1 动机

完整代理仿真虽是毫秒级，但 SA/TS 每秒需要数百次评估；大图上单次可达
20ms+。用更廉价的**学习型代理的代理**先粗筛扰动候选，可省下大量完整仿真。

### 8.2 结构（`mamba_screen.py`）

选择性状态空间递推（Mamba/S6 风格，离散化 ZOH 简化）：

```
a(x) = σ(A₀ + Uₐ·x)          选择性衰减门（S=32 维，输入依赖）
b(x) = tanh(U_b·x)            选择性输入门
h_t = a(x) ⊙ h_{t-1} + b(x)   隐状态递推
ŷ   = w · [h_t ; x]           读出（预测该扰动的归一化改进量）
```

- 特征 `x`：16 维廉价移动特征（动作类型 one-hot、块工作量/出入流量占比、
  目标子图亲和度/规模、核负载不均衡、同核标志、当前适应度对数、搜索阶段），
  O(块度数) 计算，无完整重算；
- 门控投影 `Uₐ/U_b` 为固定随机投影（selective 但免反向传播）；
  读出层 `w` 由 **NLMS 在线学习**——每次完整仿真得到真值标签
  `y = (f_cur − f_new)/|f_cur|` 后即时更新，无离线训练/冷启动；
- 隐状态仅在**真改进**时 `commit`（编码"什么样的移动在当前轨迹上有效"）。

### 8.3 接入方式：锦标赛式软筛选（仅 SA）

```
每步: 生成 4 个候选扰动 → 全部 SSM 预测(数百乘加/个)
      → 仅对预测最优者做完整仿真 (15% 概率换随机者→漏筛率估计+训练补充)
      → Metropolis 接受/拒绝始终基于真值适应度
```

**设计取舍（实验驱动）**：
- ✗ 硬跳过（只放行预测改进者）→ SA 丧失跨势垒移动，部分用例 +15~34%，弃用；
- ✗ TS 锦标赛 → 每代真值评估变少 + 迭代加速使禁忌任期相对变短，部分用例劣化，弃用；
- ✓ 仅 SA 锦标赛 → 零风险纯增益。

### 8.4 A/B 实验结果

12 代表用例 × 场景 A/B × 4 核，同种子，隔离实验（构造→TS→SA）：

| 配置 | 平均 Makespan | 结论 |
|---|---|---|
| 筛选 1500 次仿真 vs 基线 1500 次 | **−0.87%** | 4 优 / 20 平 / **0 负** |
| 筛选 900 次 vs 基线 1500 次 | +1.27% | 17/24 退化 <1% |

即：**同等质量削减 ~40% 完整仿真**，或**同预算探索 ~4× 扰动**
（SA 内 75% 候选跳过完整仿真；困难用例获益显著：case_044 场景A −17.9%）。
漏筛率（ε 探测估计）10~20%；生产默认关闭（`use_screen=False`）。

### 8.5 spill 校准与单调改进协议（自主优化轮次）

1. **NNLS 校准**（`calibrate_spill.py`）：以 1300 项官方评估的 spill 真值
   拟合四路信号 → 系数 `[2.31·L1p, 0.86·L1w, 0, 0, 0]`，
   spill 排序相关 场景A 0.335→**0.681**、场景B 0.569→**0.747**（留出 R²=0.50）。
2. **活体验证**：校准模型端到端不稳定（中位≈0，均值+4.3%）→
   不设默认，作多样性种子（`solve_case(..., spill_calibrated=True)`）。
3. **单调改进协议**（`improve_runs.py`）：校准模型+新种子重解 800 任务，
   小图 in-run 真值 / 大图官方评估择优，仅保留严格更优者并重生成官方结果：
   **291/800 任务替换**，P1 加速比 +2.2%~+4.9%、P2/P3 +0.5%~+2.5%，
   结构上保证只升不降。

### 8.6 技术升级轮（v3，综述对标后的实现）

| 项 | 实验结论 |
|---|---|
| op 级 LRU spill 仿真 | 排序相关 0.295，低于启发式+NNLS 0.75 → **回退** |
| overlap 因子 | 排序不变、偏差 +2% → 默认关 |
| 大用例逐案真值在线校正 | 已实现（>1.2万op 自动触发） |
| EFT 容量装箱 + M/V 互补分核 | 互有胜负 → 进构造池交真值择优 |
| L2 双池代理（场景 C） | 全链路通过，P3 搜索不再盲跑 |
| v3 keep-best 全量轮 | **159/800 替换**，最终加速比 P1 2.967 / P2 3.251 / P3 3.355（5核） |

教训：NetBenefit 凝聚在块号/拓扑交错的图上无法保证拓扑凸（深图劣化 2.4×），
heft 路径式增长的天然凸性是深图正确基元；"更精确的仿真"不等于"更好的代理"。
详见 `results/report.md` §6.1 与 `results/survey_gap_analysis.md`。

---

## 9. 方案合法性与真值校验

### 9.1 合法性保证链

1. 块商图 SCC 缩聚（预处理）→ 块 DAG 无环；
2. 每次评估前 `Context.repair()` 对解做 SCC 合并 → 子图商图无环；
3. 每核顺序由事件仿真派生 → 拓扑一致，不违反依赖；
4. `node_to_subgraph` 恰好覆盖全部非 COPY op（`plan_from` 全量生成）。

结果：**1300 项官方评估全部通过**，无非法方案。

### 9.2 真值校验（`real_evaluate`）

进程内 import 官方评估器（`evaluate_scene_a/b`、`evaluate_problem_3`），
对最终候选集（≤5 个）按 `real_mk + 0.2·real_added/60` 择优：
- ≤ 6000 op：5 候选全验；6000~12000 op：3 候选；>12000 op：代理值择优。

---

## 10. 工程实现与运行配置

### 10.1 求解预算（按规模自适应）

| op 数 | 时间预算 | 块容量 | 真值校验 |
|---|---|---|---|
| ≤1500 | 12 s | 24 | ≤5 候选 |
| ≤5000 | 15 s | 24 | ≤5 候选 |
| ≤12000 | 20 s | ~30 | ≤3 候选 |
| >12000 | 28 s | ≤240 | 无（代理择优） |

### 10.2 运行命令

```bash
cd solver
python run_all.py --cases 1-100 --cores 2,3,4,5 --scenes A,B --workers 8
python evaluate_official.py --workers 6          # 1300 项官方评估（可断点续跑）
python aggregate.py                               # summary.csv / curves / appendix
python eval_metrics.py                            # 三指标评估
python ab_test.py && python ab_budget.py          # SSM 粗筛层 A/B
```

### 10.3 复杂度

单次代理评估 O(nb + E_block + #tg_groups)；SCC 修复 O(E_block)；
20 核机器上 800 任务 + 1300 官方评估端到端约 2.5 小时（8~10 进程并行）。

---

## 11. 实验结果

### 11.1 官方平均加速比（100 用例，单核基准，三轮单调改进后最终版）

| 核数 | 问题1（场景A） | 问题2（场景B） | 问题3（场景B+L2） | P2 并行效率 |
|---|---|---|---|---|
| 2 | 1.735 | 1.729 | 1.745 | 86% |
| 3 | 2.241 | 2.351 | 2.386 | 78% |
| 4 | 2.623 | 2.851 | 2.924 | 71% |
| 5 | 2.989 | 3.261 | 3.366 | 65% |

（首版：P1 1.675/2.150/2.529/2.812、P2 1.704/2.315/2.802/3.162、
P3 1.721/2.354/2.874/3.267；提升来自 §8.5 校准轮、§8.6 技术升级轮
与综述方案轮（词典序选优、Tessel 顺序搜索、FIFO 复用距离），
四轮累计 551/800 任务被真值更优方案替换。）

- 4 核 P2：中位 3.08×，最大 4.48×；加速比中位高于均值 8~13%；
- 实际 Makespan 为粗下界 `max(单核/N, DDR字节/60)` 的 1.40×(P1)/1.22×(P2)（中位），
  剩余可挖空间约 10~25%；P1 有 22 用例已被带宽下界主导（加核无益）；
- ~10% 用例加速比≈1：深度上千的纯串行链，图结构固有。

### 11.2 新增搬运量（次指标）

| 配置 | added/原始流量 中位 | 切分贡献均值 | spill 贡献均值 |
|---|---|---|---|
| P1 N=4 | 1.34× | 22.3 MB | 4.2 MB |
| P2 N=4 | **0.87×** | 10.1 MB | 11.2 MB |

场景 B 同核复用把切分流量压半以下；代价是合并 Task 驻留压力使 spill 上升，
但总额仍低于场景 A——正确利用了 `C_inner < C_cross`。

### 11.3 问题3 只读 Cache

命中率均值 9.6%（2核）→17.5%（5核），最大 ~82%；命中率与 L2 加速比正相关
（r=0.641）；L2 平均收益 +0.9%→+3.7%（核数越多越大）。命中率不高的原因：
模型优先同核复用（零字节），从源头减少了可命中 L2 的访问。

### 11.4 交付物

- `results/plans/`：800 个方案（100 用例 × 场景 A/B × 2~5 核）
- `results/official/`：1300 个官方评估结果 JSON（已裁剪至关键字段，6.1MB）
- `results/summary.csv|md`、`results/appendix.md`（逐用例附录）、`results/plots/*.png`
- `results/report.md`（方法+结果）、`results/evaluation.md`（三指标评估）
- `results/ab_test.json`、`results/ab_test_900.json`（SSM 层 A/B 数据）
- `results/official_stub/` + `results/baseline_comparison.md`（原始代码基线对比）

### 11.5 与原始代码基线对比（附件 stub 生成器）

基线为附件自带 `stub_multicore_cut_and_schedule.py`（随机合法方案，README
注明仅演示格式、非性能基线），1200 项评估全部用同一官方评估器完成
（详见 `results/baseline_comparison.md`）：

| 问题 | Makespan 提升（2→5 核） | 加速比（基线→本文, 5核） | 新增搬运降幅 |
|---|---|---|---|
| 问题 1（场景 A） | **1.81× → 3.10×** | 0.96 → **2.95** | 73~76% |
| 问题 2（场景 B） | **1.55× → 2.62×** | 1.25 → **3.24** | 72~76% |
| 问题 3（场景 B+L2） | **1.56× → 2.66×** | 1.26 → **3.35** | 72~76% |

- 基线随机切分在问题 1 下平均加速比 **<1**（多核反而更慢：切分通信+等待
  吞噬并行收益）；问题 2/3 仅靠核数堆到 1.1~1.3×；
- 问题 3 Cache 命中率两者相当（基线 12~17% vs 本文 10~18%）：随机切分
  制造更多重复读取使命中率略高，但其 Makespan 差 1.6~2.7 倍——
  本文策略以同核复用从源头减少重复读取，L2 只兜底，命中率接近而
  绝对性能大幅领先。

---

## 12. 局限与改进方向

| 项 | 现状 | 改进 |
|---|---|---|
| spill 估计 | 双信号近似，added 排序相关仅 0.547 | op 级 LRU 仿真精化，改善大图（>1.2万op 无真值兜底）方案质量 |
| 带宽受限用例 | P1 中 22 个已触下界，p90 added 达原始流量 16~22× | 按张量亲和度聚簇压缩大张量切割 |
| 场景 B spill | 均值 10~15 MB 偏高 | 分核阶段加每核容量预算，以少量跨核通信换 spill |
| 深串行用例 | ~10% 加速比≈1（结构固有） | 算子级流水切分，预期收益有限 |
| SSM 粗筛 | 仅 SA、在线学习精度有限 | 批间 Spearman 监控 + 特征加入子图级增量流量 |
| Makespan 代理 | 绝对值中位高估 44%（排序 0.953） | 校准回归/分位数映射，主要影响 >1.2 万 op 用例 |

---

*文档生成时间：2026-09-23；代码与数据以 `solver/`、`results/` 目录为准。*
