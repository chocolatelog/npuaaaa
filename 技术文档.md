# 通用神经网络处理器多核调度问题 —— 技术文档

> 求解 2026 年研究生数模竞赛 A 题（华为）"通用神经网络处理器下的多核调度问题"
> 算法代码：`solver/`　|　结果数据：`results/`　|　官方评估器：附件 `code/`
> 版本：四轮单调改进后的最终版（2026-09-24）

---

## 目录

1. [问题定义与总体思路](#1-问题定义与总体思路)
2. [系统架构](#2-系统架构)
3. [图模型与预处理](#3-图模型与预处理)
4. [快速代理评估器](#4-快速代理评估器)
5. [构造式初始解](#5-构造式初始解)
6. [元启发式算法](#6-元启发式算法)
7. [多目标优化框架与真值闭环](#7-多目标优化框架与真值闭环)
8. [迭代优化历史（含实验否决记录）](#8-迭代优化历史含实验否决记录)
9. [方案合法性与最终选择](#9-方案合法性与最终选择)
10. [工程实现与运行配置](#10-工程实现与运行配置)
11. [实验结果](#11-实验结果)
12. [局限与改进方向](#12-局限与改进方向)

---

## 1. 问题定义与总体思路

### 1.1 输入 / 输出

**输入**：DAG 计算图（`tensors` / `ops` / `edges`）+ 核数 N（2~5）+ 固定配置（`config.txt`，不得修改）：

| 参数 | 值 | 含义 |
|---|---|---|
| L1 | 524288 B | Cube 私有缓存 |
| UB | 131072 B | Vector 私有缓存 |
| DDR 带宽 | 60 B/cycle | 全核共享（读+写） |
| 场景A跨核/同核等待 | 1000 / 100 cycles | 每个 Task |
| 场景B跨核同步 | 500 cycles | 每对 COPY_OUT→COPY_IN |
| L2 Cache | 1 MB @ 250 B/cycle | 只读 FIFO，带宽独立（问题3） |

**输出**（方案 JSON，仅两个字段）：

```json
{
  "node_to_subgraph": {"<非COPY op id 十进制字符串>": sgid, ...},
  "core_schedules":   [[sgid, ...], ...]
}
```

### 1.2 三重耦合决策

切图（op→子图）× 分核（子图→核）× 排序（核内顺序）。切多→并行度高但跨子图
搬运与 Task 等待多；切少→通信省但缓存压力（spill）大且并行度低。

**合法性关键结论**：DAG 任意划分的子图商图**未必无环**（子图沿路径交错时成环），
方案必须保证商图为 DAG——求解器在块级与解级均做 Tarjan SCC 缩聚修复。

### 1.3 求解思路（Cheap-to-Expensive 分层）

```
官方评估器（秒~分钟级）＝ Judge
   ▲ 真值校验/逐案校正（≤1.2万op 进程内评估，候选择优）
   │
快速代理评估器（毫秒级）＝ 搜索内循环
   · 新增搬运量：与官方逐字节一致
   · Makespan：事件仿真 + 串行关键路径 + spill + 带宽下界
   ▲
构造池（12+ 初始解）→ 禁忌搜索 → 模拟退火 → 蚁群 → MOPSO
   └──── 共享 Pareto 归档（双目标）────┘
   └── Tessel 式顺序精修（相邻独立子图交换）
   └── SA 内环可选 Mamba 式 SSM 粗筛（省 40% 完整仿真）
```

---

## 2. 系统架构

### 2.1 模块清单（`solver/`）

| 文件 | 职责 |
|---|---|
| `model.py` | 图模型、路径式块粗化、SCC 缩聚、精确边界流量、Makespan 代理、场景 C L2 复用距离、固定顺序仿真 |
| `construct.py` | 构造式初始解（HEFT/条带/链式） |
| `construct_v2.py` | NetBenefit 凝聚 + EFT 容量装箱 + M/V 互补分核（`eft_place_core`） |
| `solution.py` | 解状态、邻域动作、SCC 环修复、筛选集成、fitness 偏置 |
| `tabu.py` / `sa.py` / `aco.py` / `mopso.py` | 四种元启发式 |
| `archive.py` | Pareto 归档（支配过滤 + 拥挤距离） |
| `mamba_screen.py` | Mamba(S6) 式选择性 SSM 候选粗筛层 |
| `order_refine.py` | Tessel 式核内顺序精修（相邻独立子图交换爬山） |
| `pipeline.py` | 单任务求解流水线：构造→四算法→顺序精修→真值校验 |
| `run_all.py` | 并行全量求解入口 |
| `improve_runs.py` | keep-best 单调改进协议（多种子轮次） |
| `calibrate_spill.py` | spill 估计器 NNLS 离线校准 |
| `evaluate_official.py` / `stub_baseline.py` | 官方评估器批量评估 / 原始代码基线生成+评估 |
| `compare_baseline.py` | 三指标基线对比统计 |
| `aggregate.py` / `eval_metrics.py` | 汇总 CSV/曲线/附录 / 三指标评估 |
| `ab_test.py` / `ab_budget.py` / `validate_*.py` | SSM 粗筛 A/B、各轮次验证 |

### 2.2 数据流

```
data/case_XXX.json ──run_all.py(并行)──> solve_case() ──> results/plans/
                                              │
solve_log.jsonl（估计/真值/日志）<──────────┘
plans ──evaluate_official.py──> 官方评估器 ──> results/official/
improve_runs.py：候选重解 → 真值比较 → 仅保留严格更优 → 重生成官方结果
official ──aggregate.py──> summary.csv/md / appendix.md / plots/
```

---

## 3. 图模型与预处理

### 3.1 op 级 DAG（收缩 COPY 节点）

Op–Tensor 二部图 → op 依赖图（tensor 生产者→消费者连边），COPY 节点经 DFS
路径收缩（与官方 `derive_multicore_plan` 语义一致）。

### 3.2 路径式块粗化

按拓扑序遍历 op，**优先追加到"前驱恰为该块尾"的块**（路径式增长，防交错成环），
块容量 `cap = clamp(n_eligible // 400, 24, 240)`——保证子图可贴合 L1/UB 容量。

> **教训**（v3 轮实验）：NetBenefit 凝聚式合并（MAGIS 风格）在块号与拓扑序
> 交错的图上无法保证拓扑凸——区间夹块使簇商图成环，深图劣化 2.4×。
> 路径式增长的天然凸性是深图的正确基元。

### 3.3 SCC 缩聚

块商图跑迭代式 Tarjan SCC，非平凡 SCC 合并直至无环；搜索中每次评估前对解做
同样修复（`Context.repair`）——**产出方案恒合法**。

### 3.4 tensor 分组

按 `(生产块集, 消费块集, has_out, pos)` 签名聚合字节，评估成本从 O(#tensors)
降至 O(#groups)。

---

## 4. 快速代理评估器

### 4.1 新增搬运量（与官方逐字节一致）

- **场景 A**：输入边界（无本地生产者→COPY_IN）+ 输出边界（有外部消费者或
  原图 COPY_OUT→COPY_OUT）；
- **场景 B**：图输入每消费核一读 + 图输出每生产核一写 + 跨核 (源,目标) 对
  COPY_OUT+COPY_IN；同核零字节（`C_inner < C_cross` 来源）；
- 扣除原图固有搬运（合成 COPY 替换原 COPY）；spill 估计加入 added。

**验证**：同一方案下与官方 `partition_added_copy_bytes` 逐字节相等。

### 4.2 Makespan 估计

1. **子图时长** `d(s) = max(Σwork_M, Σwork_V, in/60, out/60, cp(s))`——
   cp 为子图内部串行关键路径（块权 = work_M + work_V + 出入流量/60）；
2. **事件仿真派生每核顺序**（最早就绪优先，拓扑合法），叠加场景 A 等待
   （100/1000）或场景 B 同步（500）；
3. **spill 惩罚**：L1/UB 双池，每池取"区间驻留峰值超限"与"内部总字节超限"
   之大者；支持 NNLS 校准系数（见 §8.1）；
4. **全局带宽下界**：`Makespan ≥ 全部 DDR 字节 / 60`；
5. **固定顺序仿真**（`_simulate_fixed_orders`）：给定每核顺序轮询发射，
   支持 Tessel 式顺序精修（§8.7）。

### 4.3 场景 C（问题 3）：L2 复用距离 + 双池

两遍仿真：第一遍得各子图结束时序 → 对每个跨核共享输入张量按 FIFO 语义
计算**复用距离**（两次读之间填充的 distinct 字节，< 1MB 判命中）→ 命中字节
从 `sg_in_b` 扣除后第二遍仿真 → 双池带宽下界
`max(ddr_served/60, l2_served/250)`。

### 4.4 精度实测（648 真值样本 + 100 用例全量）

| 指标 | 精度 |
|---|---|
| Makespan 排序相关 | **0.968**（P2 N=4 全量） |
| 新增搬运（切分部分） | 逐字节精确 |
| spill 信号排序相关（NNLS 校准后） | 0.68(A) / 0.75(B) |

**已否决的替代设计**（数据驱动）：op 级 LRU spill 仿真（排序 0.295，低估
官方级联重读）与搬运/计算 overlap 折减因子（排序无变化）——均保留开关、默认关。

### 4.5 评估缓存

`(tuple(sg), tuple(core), scene, N)` 键 LRU（8192 条）；`orders_override`
路径绕过缓存。

---

## 5. 构造式初始解

| 构造器 | 思路 |
|---|---|
| `heft_construct`（5 种粒度） | 块按递减 upward rank，最早完成 + 通信局部性选核 |
| `strip_construct`（3 种） | 拓扑条带贪心分核 |
| `chain_construct` | 块链独占 + LPT 均衡 |
| `eft_place_core`（v2，2 种粒度） | **EFT 容量装箱 + M/V 互补打分**：每核 L1/UB 驻留预算（超限重罚）、双管道失衡度、输入通信计入 EFT |
| 整图基线 | 单核串行兜底 |

构造池共 12+ 初始解全部入 Pareto 归档，池前三名进最终真值校验候选集。

---

## 6. 元启发式算法

### 6.1 邻域动作（5 种）

`move_block`（40%，含通信边界定向变体）/ `move_core`（15%）/ `merge`（20%）/
`split`（15%，拓扑中位拆分）/ `swap`（10%）。所有动作后经 SCC 修复保证合法。

### 6.2 四算法要点与实测贡献

| 算法 | 要点 | 贡献（800 任务归因） |
|---|---|---|
| 禁忌搜索 | 12 邻居采样、属性禁忌（任期 0.6√块数）、渴望准则 | **89% 任务 / 8.7%**（主力） |
| 模拟退火 | 初温 0.15f、降温 0.995、定向邻域、重启 | 30% / 1.5% |
| MOPSO | 随机键（每块 cut/pref + 全局 K 基因）→凸切分；基因空间 Pareto 前沿 + 拥挤距离领袖 | 17% / 4.1% |
| 蚁群 | (块,簇归属/核) 信息素 + 精英沉积 | 3% / 3.2% |

### 6.3 Mamba 式 SSM 粗筛层（SA 可选）

选择性递推 `h_t = a(x)⊙h_{t-1} + b(x)`（固定随机投影门控）+ NLMS 在线读出，
锦标赛式软筛选：每步 4 候选全部预测、仅预测最优者（ε=15% 概率换随机者）
做完整仿真，接受/拒绝始终基于真值。A/B（等仿真预算 1500 次）：
**平均 −0.87%，4 优/20 平/0 负**；60% 预算下 +1.27%（即同质量省 40% 仿真）。
生产默认关闭。

---

## 7. 多目标优化框架与真值闭环

- **目标向量** `(Makespan, 新增搬运)`，共享 ParetoArchive（容量 48）；
- **标量化**（搜索引导）：`f = mk·bias + 0.2·added/60`（bias 为大用例逐案
  校正乘子）；
- **真值闭环**：≤1.2 万 op 任务对最终 ~5 个候选做进程内官方评估，
  **词典序选优**——Makespan 严格优先（±0.3% 内并列才比 added）；
  >1.2 万 op 任务搜索中期对 top-2 做逐案真值校正（比值作 fitness 偏置再精搜）；
- **keep-best 单调改进协议**（`improve_runs.py`）：新种子/新模型重解全部任务，
  与现有方案真值比较，**仅保留严格更优者**并重生成官方结果——结果只升不降。

---

## 8. 迭代优化历史（含实验否决记录）

### 8.1 spill 校准轮（v2）
1300 项官方真值 NNLS 拟合四路信号 → 系数 `[2.31·L1p + 0.86·L1w]`，
spill 排序相关 0.335→0.681(A)、0.569→0.747(B)。活体验证端到端不稳 →
**不设默认，作多样性种子**。

### 8.2~8.4 三轮 keep-best（v2/v3/v4）

| 轮 | 配置 | 替换任务 | 平均改善 |
|---|---|---|---|
| v2 | 校准模型 + 新种子 | 291/800 | −4.9% |
| v3 | + EFT 分核 + 真值校正 | 159/800 | −2.9% |
| v4 | + 词典序选优 + Tessel 顺序精修 + L2 复用距离 | 101/800 | −3.0% |

四轮累计 **551/800 任务**被真值更优方案替换，零退化。

### 8.5 v3/v4 技术升级实验全记录

| 项 | 实验结论 | 决定 |
|---|---|---|
| op 级 LRU spill 仿真 | 排序 0.295 < 启发式 0.75 | 回退（默认关） |
| overlap 折减因子 | 排序不变（0.968）、偏差 +2% | 默认关 |
| NetBenefit 凝聚切分 | 非凸环，深图劣化 2.4× | 弃用 |
| EFT 容量装箱 + M/V 分核 | 互有胜负（−4%~+20%） | 进构造池 |
| 大用例逐案真值校正 | — | 保留 |
| L2 双池代理 + 复用距离 | 全链路通过；触发率低与图结构一致 | 保留 |
| Tessel 顺序精修 | case_011/B −10%；多数用例已局部最优 | 保留 |
| 词典序选优 | 消除 added 折算噪声翻盘 | 保留 |

### 8.6 文献对标（两篇综述）

与 2024-2026 主线（白盒约束感知 + 代理分层 + 局部搜索）完全同向：
MAGIS 的 Mutator-Simulator-Optimizer 闭环、DOPS 的 HEFT+分层精评、
LATTICE 的内存压力模型均已等价实现；分析存档
`survey_gap_analysis.md` / `survey2_gap_analysis.md`。
本系统独有的赛题定制点：**场景 C 的 FIFO 复用距离调度**
（综述确认无开源系统实现本题完整硬件模型）。

---

## 9. 方案合法性与最终选择

### 9.1 合法性保证链

块商图 SCC 缩聚（预处理）→ 解级 `repair()` → 每核顺序仿真派生或
顺序精修（仅交换无直接依赖的相邻对）→ 全量覆盖生成 `node_to_subgraph`。
**1300+1200 项官方评估全部通过，零非法方案。**

### 9.2 真值校验与词典序

候选 = 搜索最优 ∪ 归档标量最优 ∪ 构造池前三；≤6000 op 全验（5 候选）、
6000~12000 op 验 3 个；词典序 `lexi_better(mk, added, tol=0.3%)` 择优。

---

## 10. 工程实现与运行配置

### 10.1 自适应预算

| op 数 | 时间预算 | 真值校验 |
|---|---|---|
| ≤1500 / ≤5000 | 12 / 15 s | ≤5 候选 |
| ≤12000 | 20 s | ≤3 候选 |
| >12000 | 28 s | 无（逐案校正补偿） |

### 10.2 运行命令

```bash
cd solver
python run_all.py --cases 1-100 --cores 2,3,4,5 --scenes A,B --workers 8
python evaluate_official.py --workers 6          # 官方评估（可断点续跑）
python improve_runs.py --phase vN --workers 10   # keep-best 轮（可中断续跑）
python aggregate.py && python eval_metrics.py    # 汇总 + 三指标评估
python stub_baseline.py && python compare_baseline.py   # 基线对比
python ab_test.py                                # SSM 粗筛 A/B
```

---

## 11. 实验结果

### 11.1 官方平均加速比（100 用例，单核基准，三轮 keep-best 后最终版）

| 核数 | 问题1（场景A） | 问题2（场景B） | 问题3（场景B+L2） | P2 并行效率 |
|---|---|---|---|---|
| 2 | 1.777 | 1.729 | 1.769 | 89% |
| 3 | 2.337 | 2.351 | 2.401 | 78% |
| 4 | 2.861 | 2.851 | 2.969 | 72% |
| 5 | 3.264 | 3.261 | 3.425 | 65% |

（首版：P1 1.675/2.150/2.529/2.812、P2 1.704/2.315/2.802/3.162、
P3 1.721/2.354/2.874/3.267；P1 第五轮采纳 W 分支构造池机制（104 任务替换→3.133）；第六轮 M0 下界攻击
（lbattack 第 5 范式 + 图类路由 + K 扫描，74 任务替换→3.159）+ M1 配置对齐；
第七轮 w3（粒度表修复：W 表不适配 100 例口径致中图细档缺失；
小图 K 网格种子 + 真值扫描裁决），95 任务替换→P1 3.264；四轮 keep-best 累计 551/800 任务真值替换 + P3 逐案裁决（C 方案胜 85 例），5 核相对首版 P1 +6.3% / P2 +6.2% / P3 +9.0%。）

- 4 核 P2：中位 3.08×，最大 4.48×；~10% 深串行图（如 case_016 深度 2441）
  加速比≈1，属图结构固有；
- 实际 Makespan 为粗下界 `max(单核/N, DDR字节/60)` 的 1.40×(P1)/1.22×(P2)
  （中位）；P1 有 22 用例已被带宽下界主导。

### 11.2 新增搬运量（次指标）

| 配置 | added/原始流量 中位 | 切分贡献均值 | spill 贡献均值 |
|---|---|---|---|
| P1 N=4 | 1.34× | 22.3 MB | 4.2 MB |
| P2 N=4 | **0.87×** | 10.1 MB | 11.2 MB |

场景 B 同核复用将切分流量压半以下，总额仍低于场景 A——正确利用
`C_inner < C_cross`；代价是 spill 升至 10~15 MB。

### 11.3 问题 3 只读 Cache

命中率均值 9.8%（2核）→17.4%（5核），最大 ~82%；命中率与 L2 加速比正相关
（r=0.641）；L2 收益 +0.9%→+3.4%。命中率不高是策略副作用：同核复用从源头
消除重复读（零字节），L2 只兜底跨核部分。

### 11.4 与原始代码基线对比（附件 stub 生成器，1200 项官方评估）

| 问题 | Makespan 提升（2→5核） | 加速比（基线→本文, 5核） | 新增搬运降幅 |
|---|---|---|---|
| 问题 1 | **1.84× → 3.14×** | 0.96（负优化）→ **2.99** | 74~77% |
| 问题 2 | **1.57× → 2.63×** | 1.25 → **3.26** | 72~77% |
| 问题 3 | **1.57× → 2.67×** | 1.26 → **3.37** | 72~77% |

Cache 命中率两者相当（±2pt）：基线靠随机切分刷命中率但 Makespan 差
1.6~2.7 倍；本文同核复用从源头消除重复读。详见 `results/baseline_comparison.md`。

### 11.5 交付物

- `results/plans/`：800 方案（100 用例 × A/B × 2~5核）
- `results/official/`：1300 官方评估结果（关键字段版）
- `results/official_stub/` + `baseline_comparison.md`：基线对比
- `results/summary.csv|md`、`appendix.md`、`plots/`
- `results/report.md`（四轮优化全记录）、`evaluation.md`（三指标评估）
- `survey_gap_analysis.md` / `survey2_gap_analysis.md`（文献对标）
- `improve_decisions.json`（各轮 keep-best 决策日志）

---

## 12. 局限与改进方向

| 项 | 现状 | 改进 |
|---|---|---|
| spill 估计 | NNLS 校准后排序 0.68/0.75 | 官方级联重读建模；对 >1.2万op 大用例价值最大 |
| 带宽受限用例 | P1 中 22 个触下界；p90 added 达原始 16~22× | 大张量亲和聚簇，避免横切巨型张量 |
| 场景 B spill | 均值 10~15 MB | 每核容量预算收紧 + 以通信换 spill |
| 深串行用例 | ~10% 加速比≈1（结构固有） | 算子级流水切分，预期有限 |
| 离线动作排序器 | 在线 Mamba-SSM 已可用 | 800 任务 × 数千评估数据已备，XGBoost + TopKRecall 验收 |
| 消融体系 | 数据已齐 | B1/B2/B3 分层消融、L2 三态消融、λ 扫描 Pareto、oracle 上界（纯分析） |

---

*文档版本：四轮 keep-best 后最终版（2026-09-24）；以 `solver/`、`results/` 为准。*
