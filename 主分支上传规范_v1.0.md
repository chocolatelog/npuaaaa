# 主分支上传规范

版本：`DOC-PROTOCOL-v1.0`

更新时间：2026-09-24 17:21

## 1. 分支职责

| 分支 | 责任 |
|---|---|
| `main` | 当前可复现、经过验证的最佳流程；只接收有完整证据的改动 |
| `W` | 完整求解器实验分支，记录 W 本轮采用的方案和结果 |
| `Y` | 完整求解器实验分支，记录 Y 本轮采用的方案和结果 |
| `H` | 完整求解器实验分支，记录 H 本轮采用的方案和结果 |

W、Y、H 没有固定的代码模块所有权。三条分支可以修改同一模块，最终按统一实验结果竞争合并；表中的分支名称只表示实验方案的来源。

三个人的分支都必须各自维护两份文档：

1. `技术路线_<分支>_vX.Y.md`：当前代码做了什么，算法细节是什么，下一步准备做什么；
2. `实验记录_<分支>_vX.Y.md`：每次实验的命令、参数、数据范围、结果、异常和结论。

主分支还必须维护：

- `技术路线_主分支最佳流程_vX.Y.md`；
- `实验记录_主分支_<版本>.md`；
- 本上传规范。

说明：每个分支作为成果上传或提交时，实际成果文档只包含前面列出的两份，即技术路线文档和实验记录文档。本文件是仓库级协作规范，不计入个人分支的两份成果文档；主分支技术路线中统一记录 W、Y、H 的后续改进计划。

## 2. 每次提交必填信息

技术路线文档开头必须包含：

```text
版本号
更新时间
目标分支
基线提交或版本
负责模块
```

实验记录文档每条实验必须包含：

```text
实验编号
更新时间
代码版本
实验命令
数据范围
核数、场景、时间预算、随机种子
设备和显存信息
测试结果
官方可比结果与代理结果的分开统计
异常和下一步
```

## 3. 代码提交前检查

在分支根目录执行：

```cmd
cd /d J:\数学建模\改进 && python -m pytest tests -q
```

检查以下内容：

1. 方案覆盖完整，每个节点只出现一次；
2. 商图无环；
3. 每核顺序满足直接依赖；
4. 官方评估器能运行；
5. 没有把代理结果混入官方平均值；
6. RTX 3070 8GB 下没有显存溢出；
7. 实验支持断点续跑；
8. 随机种子和配置可重复；
9. 未删除任何旧结果或数据文件；
10. 文档更新时间和版本号已更新。

## 4. 实验记录要求

每次实验都必须保留：

- 一行完整的单行命令；
- 代码版本和父提交；
- 运行开始/结束时间；
- 任务完成数和失败数；
- 平均值、中位数、退化案例数；
- 大图代理结果和官方结果的分栏；
- 主要日志文件路径；
- 对比基线版本；
- 是否建议合并。

不得只上传一张平均加速比表而不写实验命令和数据口径。

## 5. 文件管理

本次 H 分支 2026-09-25 归档例外：根据明确的全部文档、实验数据和记录上传要求，允许发布现有 results 中的方案 JSON、实验 JSONL、配置指纹和汇总。范围见 results/README.md；不包含环境、缓存、密钥、数据库和完整 trace，也不改变 main 合并门槛。以下为一般情况下的默认规则。

2026-09-26 补充授权归档：上传当前 H 技术路线与第三问 C400 实验记录，同时保留支撑结果的方案、日志、指纹、汇总及探针摘要。原始证据不改写，历史结果不覆盖。

允许提交：源代码、测试、中文技术路线、中文实验记录、指标摘要、复现说明。

禁止提交：原始大规模 `results` 日志、断点数据库、Python 缓存、本地环境、密钥、完整 trace 和临时文件。`.gitignore` 已配置这些规则；若确需提交小型摘要，必须使用明确的 `summary` 或 `report` 文件名。

## 6. 分支提交流程

在个人分支完成代码和两份文档后执行：

```cmd
cd /d J:\数学建模\改进 && git status --short && python -m pytest tests -q && git add solver tests 技术路线_<分支>_vX.Y.md 实验记录_<分支>_vX.Y.md && git commit -m "<分支>: 中文描述改动和验证" && git push -u origin <分支>
```

提交后在协作说明中提供：分支名、提交号、技术路线文档路径、实验记录文档路径、基线版本和是否建议合并。

## 7. 合并主分支流程

主分支维护者先读取个人分支的两份文档，再执行同一算例集合的对照实验。只有满足以下条件才能合并：

1. 测试全部通过；
2. 官方方案全部合法；
3. 平均 makespan 不劣于当前主分支；
4. 退化案例数量不增加，或有明确的收益解释；
5. 运行时间和显存占用在预算内；
6. 主分支技术路线和实验记录已更新。

主分支合并命令为：

```cmd
cd /d J:\数学建模\改进 && git checkout main && git pull --ff-only origin main && git merge --no-ff <分支> && python -m pytest tests -q && git push origin main
```

如果实验不满足门槛，保留个人分支，不回退或删除已有代码和结果。

## 8. Y/H 文档补齐计划

当前主分支已为 W 分支建立技术路线和实验记录样例。Y、H 分支各自补齐两份文档即可，文档应记录各自实际选择的算法方向，不预设模块责任。两者均从当前 `main-fix-v1.0.0` 读取基线，不得引用过期的 `refined_v2` 作为唯一基线。
