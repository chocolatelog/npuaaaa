# 多核调度模型补充

## 1. 计算图、商图与 DAG 偏序集

删除 `COPY_IN/COPY_OUT` 节点并沿 COPY 路径收缩后，得到操作依赖图

\[
G=(V,E),
\]

其中 (V) 是需要调度的计算操作，((u,v)\in E) 表示操作 (v) 必须在
操作 (u) 产生的数据之后执行。由题目保证，(G) 是 DAG。

设子图划分函数为

\[
g:V\rightarrow\{1,2,\ldots,S\},
\]

第 (s) 个子图记为 (V_s=\{v\in V:g(v)=s\})。由操作图诱导出的商图为

\[
Q=(\mathcal S,E_Q),\qquad
(a,b)\in E_Q\Longleftrightarrow
\exists (u,v)\in E,\;g(u)=a,\;g(v)=b,\;a\ne b.
\]

商图上的可达关系定义偏序

\[
a\prec b \Longleftrightarrow
\text{商图中存在从 }a	ext{ 到 }b	ext{ 的有向路径}.
\]

因此，每个核心上的子图排列必须是该偏序集的线性扩展；若商图出现环，
则不存在合法的子图执行顺序。代码中的 `group_graph` 正是在检查这个条件，
并生成一个拓扑线性扩展。

## 2. 多目标函数

对一个候选方案 (x=(g,c,\pi))，采用以下最小化目标向量：

\[
\mathbf f(x)=
\left(f_1,f_2,f_3,f_4,f_5\right),
\]

其中：

* (f_1)：Pipe 负载与总计算量得到的 makespan 代理；
* (f_2=B_{\rm cross}/60+\tau N_{\rm cross})，表示跨核搬运和同步等待，
  其中 \(\tau=1000\)（问题1）或 \(500\)（问题2/3）；
* (f_3=B_{\rm cut}/60)，表示子图边界搬运量；
* (f_4=L_{\max}-\bar L)，表示核心负载不均衡；
* (f_5)：问题3中的 Cache 压力，Cache 未启用时权重置零。

这些量先按图规模归一化，再用于搜索。最终排名仍以官方评估器返回的
makespan、搬运量和 Cache 统计为准，代理目标只负责缩小搜索空间。

## 3. 帕累托支配与帕累托最优

由于多个目标相互冲突，不能只用一个目标判断所有方案。对两个方案 (x,y)，
若

\[
f_i(x)\le f_i(y),\quad \forall i,
\]

且至少存在一个目标 (j) 满足

\[
f_j(x)<f_j(y),
\]

则称 (x) 支配 (y)，记为 (x\prec_D y)。不被任何可行方案支配的方案
构成帕累托最优解集（Pareto front）。ACO 每轮把非支配方案放入档案，
而不是只保留一个线性加权最优解。

## 4. 加权切比雪夫标量化

为便于蚁群构造和局部搜索，使用加权切比雪夫标量化替换原来的简单加权和。
令 (z^*=(z_1^*,\ldots,z_m^*)) 为理想点，(s_i) 为归一化尺度，
\(\lambda_i\ge0\)，则

\[
F_{\rm TCH}(x)=
\max_i\left\{\lambda_i
\frac{|f_i(x)-z_i^*|}{s_i}\right\}
 +\rho\sum_i\lambda_i
\frac{|f_i(x)-z_i^*|}{s_i}.
\]

第一项控制最差目标，第二项用于打破近似并列解。当前默认权重为：

|场景|makespan|跨核同步|切边搬运|不均衡|Cache|
|---|---:|---:|---:|---:|---:|
|问题1|0.55|0.25|0.10|0.10|0.00|
|问题2|0.45|0.35|0.10|0.10|0.00|
|问题3|0.40|0.20|0.05|0.05|0.30|

可用 `--lambdas a,b,c,d,e` 覆盖默认权重。

## 5. HV 超体积指标

对归一化后的非支配目标点集 (P)，给定被所有点支配的参考点 (r)，
超体积定义为

\[
HV(P)=\operatorname{vol}\left(\bigcup_{p\in P}[p_1,r_1]\times\cdots\times[p_m,r_m]\right).
\]

HV 越大，说明解集同时具有更好的收敛性和多样性。代码用 `pymoo` 计算：

```python
from pymoo.indicators.hv import HV
hv = HV(ref_point=reference)(normalized_pareto_points)
```

运行方式：

```bash
python pareto_metrics.py /tmp/case019_pareto.json
```

## 6. 参数灵敏度分析

采用单因素控制变量法，固定随机种子、样例、核心数和迭代次数，每次只改变一个
参数：

* \(\lambda\)：目标权重向量；
* \(\alpha\)：信息素指数；
* \(\beta\)：启发式指数；
* \(\rho_e\)：信息素蒸发系数。

记录标量化值、各目标分量、非支配解数量和 HV。运行方式：

```bash
python sensitivity_analysis.py \
  "/path/to/case_019.json" -n 4 --scene A \
  --ants 2 --iterations 2 --local-rounds 1 \
  -o /tmp/case019_sensitivity.json
```

正式实验时，将快速参数替换为 `ants=4, iterations=8, local-rounds=2`，
并在全部代表性样例上统计均值和标准差。
