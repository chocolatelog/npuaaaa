# 问题一实验分析

## 1. 实验目的与数据口径

本实验验证问题一多核调度方案在官方评估器下的合法性、完成时间和数据搬运代价。所有最终指标均来自官方评估器，不使用代理模型估计值替代真值。

测试维度为 100 个案例 x 4 种核数（2、3、4、5 核），共 400 个组合。当前本地结果文件 `/tmp/problem1_global_official_20260924/results.json` 已包含 400/400 组记录，400 组均为 `ok=true`。由于仓库规定不提交大规模原始结果，该 JSON 保留在本地，不提交到 Git。

评估指标：

- `makespan`：总完成时间，越小越好；
- `added_copy_bytes`：新增 COPY 搬运字节数，越小越好；
- `scheduled_copy_bytes`：调度后 COPY 搬运字节数，越小越好；
- `speedup = singlecore_makespan / makespan`：相对单核基线的加速比，越大越好。

方案的严格选择键为 `(makespan, added_copy_bytes, scheduled_copy_bytes)`，因此 makespan 优先，只有完成时间相同时才比较搬运量。

## 2. 400 组官方全覆盖结果

下表是当前本地全覆盖批次的总体统计。该批次采用 `--quick`，每个案例使用一个确定性的 `critical` 候选，因此用于全覆盖合法性和基准测量；高质量多候选搜索结果另行报告。

| 核数 | 组合数 | 平均 makespan | 中位 makespan | 平均新增搬运(B) | 总运行时间(s) | 平均每组(s) |
|---:|---:|---:|---:|---:|---:|---:|
| 2 | 100 | 2,376,347 | 352,870 | 13,604,615 | 718.59 | 7.19 |
| 3 | 100 | 2,215,448 | 302,191 | 14,289,172 | 683.12 | 6.83 |
| 4 | 100 | 1,968,135 | 240,606 | 14,773,581 | 635.13 | 6.35 |
| 5 | 100 | 1,053,145 | 216,090 | 15,021,272 | 751.86 | 7.52 |

均值明显高于中位数，说明少数超大图（例如 `case_014`、`case_076`、`case_091`）对平均值有强烈影响。报告中应同时给出均值和中位数，不能只报平均值。

## 3. 可获得单核基线的对比

并非全部 100 个案例都提供可直接读取的 `singlecore_log.txt`。在本地全覆盖结果中，只有 11 个案例含可用基线，因此下面的 speedup 统计只对这 11 个案例计算，不能外推为全部 100 案例。

| 核数 | 基线样本数 | 平均 speedup | 中位 speedup | 几何平均 speedup | speedup < 1.1 | speedup < 1 |
|---:|---:|---:|---:|---:|---:|---:|
| 2 | 11 | 1.563 | 1.953 | 1.486 | 4 | 2 |
| 3 | 11 | 1.726 | 1.490 | 1.571 | 4 | 3 |
| 4 | 11 | 2.480 | 2.556 | 2.098 | 4 | 3 |
| 5 | 11 | 2.889 | 3.034 | 2.321 | 4 | 4 |

`speedup < 1` 并不意味着程序非法，而是说明该案例的依赖链、共享带宽或新增搬运抵消了并行收益。这类案例必须作为退化案例单独分析，不能从平均值中隐藏。

## 4. 代表案例分析

### 4.1 可扩展案例：case_001

| 核数 | makespan | speedup | added_copy_bytes |
|---:|---:|---:|---:|
| 2 | 117,436 | 1.985 | 3,456 |
| 3 | 79,201 | 2.943 | 5,760 |
| 4 | 59,868 | 3.894 | 8,064 |
| 5 | 48,544 | 4.802 | 10,368 |

该案例近似宽并行结构，核数增加后 makespan 明显下降，新增搬运仍很小，说明划分没有引入严重通信开销。

### 4.2 通信/划分困难案例：case_002

| 核数 | makespan | speedup | added_copy_bytes |
|---:|---:|---:|---:|
| 2 | 198,284 | 1.321 | 27,648 |
| 3 | 175,749 | 1.490 | 55,296 |
| 4 | 132,669 | 1.974 | 58,368 |
| 5 | 133,445 | 1.963 | 76,800 |

5 核相较 4 核没有继续改善，说明更细切分后受到跨核等待或关键路径限制。高预算 tabu 实验曾将 4 核 makespan 降至 106,210、5 核降至 93,738，说明搜索预算对困难案例有实际作用。

### 4.3 深串行案例：case_016

该案例 2~5 核 makespan 均约 7.72M，新增搬运随核数增加，speedup 接近 1。其主要瓶颈是 DAG 关键路径，而不是核数不足。对此类案例继续增加子图数量通常只会增加同步和搬运，不能产生有效并行度。

### 4.4 带宽/规模主导案例：case_014

| 核数 | makespan | added_copy_bytes | 单组耗时(s) |
|---:|---:|---:|---:|
| 2 | 8,853,447 | 82,954,920 | 112.02 |
| 3 | 5,959,924 | 88,283,304 | 113.38 |
| 4 | 4,556,102 | 91,969,704 | 110.34 |
| 5 | 3,691,596 | 93,164,088 | 110.17 |

该案例随核数增加仍有收益，但官方评估器耗时约 110 秒/组，说明全局实验必须采用断点续跑。新增搬运也随切分增加，不能只追求并行核数。

### 4.5 几乎无收益案例：case_043

| 核数 | makespan | speedup | added_copy_bytes |
|---:|---:|---:|---:|
| 2 | 676,060 | 1.002 | 916,544 |
| 3 | 677,040 | 1.001 | 1,249,430 |
| 4 | 675,090 | 1.004 | 1,345,658 |
| 5 | 678,455 | 0.999 | 2,158,164 |

这是典型退化结构：增加核数没有降低关键完成时间，反而增加搬运。该案例应作为算法的反例和边界条件写入论文，而不是删除。

## 5. 与主分支已有结果的对比

主分支 `results/summary.csv` 是另一套已经完成的多候选官方结果。其问题一 100 案例汇总如下：

| 核数 | 主分支平均 makespan | 主分支中位 makespan | 主分支平均 speedup | 主分支中位 speedup | 主分支几何平均 speedup | 平均新增搬运(B) |
|---:|---:|---:|---:|---:|---:|---:|
| 2 | 1,669,219 | 294,060 | 1.735 | 1.886 | 1.689 | 24,169,250 |
| 3 | 1,245,807 | 250,857 | 2.241 | 2.416 | 2.109 | 24,388,882 |
| 4 | 1,049,400 | 228,344 | 2.623 | 2.777 | 2.393 | 24,820,347 |
| 5 | 932,207 | 206,520 | 2.989 | 3.065 | 2.666 | 25,025,923 |

主分支结果的候选池更大、采用了官方真值筛选，因此平均 makespan 低于当前 `--quick` 单候选全覆盖批次；但平均新增搬运也更高。两者不是同一实验条件，不能直接据此宣称某个算法全面优于另一个算法。正式对比必须固定候选数量、随机种子、评估器版本、时间预算和样本集合。

## 6. 合法性与工程指标

当前本地 400 组官方评估均通过，未发现商图依赖环、非 COPY 操作遗漏、子图未出现在任何核调度中或 JSON 格式错误。

大图运行时间是主要工程瓶颈。最慢案例集中在 `case_014`、`case_076`、`case_091` 等；因此使用 `--resume --max-seconds 300` 分批运行，并逐条写入 `results.json`。

## 7. 结论与后续

1. 方案已经完成问题一 100x4 的官方合法性全覆盖。
2. 并行收益高度依赖 DAG 结构；宽图收益明显，深链和带宽主导图收益有限。
3. 单看 makespan 会忽略通信代价，最终表格必须同时报告 `added_copy_bytes`。
4. 主分支多候选结果优于当前 quick 全覆盖结果，说明继续增加候选和局部搜索预算有价值，但代价是运行时间和搬运量增加。
5. 最终论文应报告均值、中位数、几何平均、退化案例数、最慢案例和固定复现实验命令，并明确“候选预算内 best-known”，不写“已证明全局最优”。

复现实验命令：

```bash
python -u global_test.py \
  --data-dir "/Users/bailansen/中文题目/A题/通用神经网络处理器下的多核调度问题  附件/data" \
  --output-dir /tmp/problem1_global_official_20260924 \
  --cores 2 3 4 5 --quick --resume --max-seconds 300
```
