# 问题一实验分析

## 1. 评估指标

官方评估器输出的主要指标为：

- `makespan`：总完成时间，越小越好；
- `added_copy_bytes`：方案新增的数据搬运量，越小越好；
- `scheduled_copy_bytes`：调度后 COPY 搬运量，越小越好。

相对单核基线的加速比定义为：

```text
speedup = singlecore_makespan / multicore_makespan
```

方案比较按 `(makespan, added_copy_bytes, scheduled_copy_bytes)` 字典序进行。

## 2. 已完成官方验证

截至 2026-09-24，问题一批量测试已完成 55 组（部分案例 × 2/3/4/5 核）：

| 核数 | 平均 makespan | 平均 added_copy_bytes | 可比较基线案例 | 平均 speedup | 几何平均 speedup |
|---:|---:|---:|---:|---:|---:|
| 2 | 808647 | 6122153 | 3 | 1.767 | 1.736 |
| 3 | 592091 | 6651573 | 3 | 2.140 | 2.058 |
| 4 | 457547 | 6883550 | 3 | 3.275 | 3.122 |
| 5 | 129784 | 371741 | 3 | 3.901 | 3.597 |

结果文件：`/tmp/problem1_global_official_20260924/results.json`。该文件是本地断点结果，不提交到仓库；重新实验时使用相同命令和 `--resume` 继续覆盖剩余案例。

## 3. 合法性

已完成的 55 组均通过官方评估器，未发现：

- 商图依赖环；
- 非 COPY 操作遗漏；
- 子图调度缺失；
- 输出 JSON 格式错误。

## 4. 结果解释

加速比随核数总体上升，但并不保证严格单调。原因包括共享 DDR 带宽、跨核等待、子图边界搬运和原图依赖链。深串行案例的并行收益受 DAG 关键路径限制，不能仅通过增加核数解决。

当前统计中只有部分案例带有单核日志，因此平均 speedup 只对存在单核基线的案例计算；没有基线的案例仍保留官方 `makespan` 和搬运指标，但不强行填充加速比。

## 5. 后续实验

1. 使用 `--resume` 完成 100 个案例 × 4 种核数的全覆盖；
2. 对低加速比案例增加禁忌搜索预算；
3. 固定随机种子，比较贪心、HEFT、禁忌、蚁群和混合方案；
4. 报告均值、中位数、几何均值、p90、退化案例数和运行时间；
5. 仅将通过官方评估器的方案纳入最终表格。
