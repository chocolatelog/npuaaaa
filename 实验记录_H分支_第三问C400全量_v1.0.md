# H 分支第三问 C：400 配置全量实验记录

实验编号：H-PROBLEM3-FULL400-20260926；版本：v1.0；更新时间：2026-09-26（Asia/Shanghai）；目标分支：H。
代码父提交：33feb6141dab5570d65423739d9f377d8559be59，加运行前 C 适配工作区改动。源码、输入、B 方案及参照数据精确字节指纹保存在原始 manifest。

## 1. 范围、输入与设备

- 完整 case_001–case_100 × 场景 C × 核数 2/3/4/5，共 400 个唯一配置。
- 输入计划：results/plans_oplist_full800 中对应 B 方案；均重新调用官方第三问评估器。
- 单核参照：results/summary.csv；A/B 参考日志仅用于既有接口参数，本次没有同场景 C 历史配对记录。
- 6 workers，最多 6 个确定性候选，无 seed 参数；评估/生成/候选预算分别为 360/120/300 秒。
- manifest 记录 Python 3.12.7、64 位 Windows 构建。本轮未记录 CPU/GPU 型号或显存峰值，不用推荐设备代替实际遥测。
- 正式运行本地时间约 2026-09-26 00:36:08 至 01:07:05（UTC+08:00，按日志创建及最后写入时间）；wall_seconds=1856.484，即约 30 分 56.5 秒。文件时间是辅助证据，实测墙钟以汇总字段为准。

## 2. 正式命令

从仓库根目录执行的参数如下。重跑时必须换 output-dir 和 log-file 名称，原入口拒绝覆盖。

```powershell
python solver/run_oplist_full.py --cases 1-100 --scenes C --cores 2,3,4,5 --baseline-dir results/plans_oplist_full800 --reference-log results/solve_log_oplist_full800.jsonl --singlecore results/summary.csv --output-dir results/plans_problem3_full400 --log-file results/solve_log_problem3_full400.jsonl --workers 6 --max-candidates 6 --evaluation-seconds 360 --generation-seconds 120 --candidate-seconds 300
```

## 3. 最终结果

400/400 均为 official；未验证/失败配置 0；大图官方配置 76。每组以下数据均为完整 100 案例，不是 81 案例子集。

| 核数 | 原 B 场景平均 | 同一 B 方案在 C 下重评 | C 最终平均 | 改善/持平/退化 |
|---|---:|---:|---:|---:|
| 2 | 2.267487× | 2.272547× | 2.286691× | 4/96/0 |
| 3 | 3.065740× | 3.125541× | 3.164093× | 6/94/0 |
| 4 | 3.677874× | 3.776263× | 3.858122× | 6/94/0 |
| 5 | 4.225545× | 4.362701× | 4.468436× | 7/93/0 |

原 B 成绩与 C 保底分属不同评估模型；23/377/0 比较的是 C 最终与同一模型下的 C 保底，不是 C 最终对原 B 分数。

全部 400 配置混合平均加速比 3.4443356492764767×，C 保底为 3.38426281890979×。混合均值只作概览，5 核成绩应使用 4.468436482050492×。

指标为各案例单核参照耗时 / 官方多核耗时的算术平均，不是平均耗时之比。日志已有 speedup 和 baseline_speedup 字段，可直接统计；也可和单核参照复算，不需要等待完整任务才计算阶段均值。

| 核数 | 最终加速比中位数 | 官方 makespan 均值（cycles） | 官方 makespan 中位数（cycles） |
|---|---:|---:|---:|
| 2 | 2.202699× | 1470480.54 | 207787.5 |
| 3 | 3.096659× | 1007543.41 | 142252.0 |
| 4 | 3.940743× | 754006.14 | 122934.5 |
| 5 | 4.712208× | 631727.18 | 110455.5 |

## 4. Cache 命中率

| 核数 | C 保底平均 | C 最终平均 | 单例最高 | 零命中案例数 |
|---|---:|---:|---:|---:|
| 2 | 9.7494% | 9.8219% | 49.0931% | 18 |
| 3 | 16.4700% | 16.6667% | 49.2331% | 18 |
| 4 | 17.8839% | 17.9194% | 55.7913% | 21 |
| 5 | 22.6650% | 23.1639% | 68.1650% | 15 |

每例 hit_bytes/(hit_bytes+miss_bytes) 后等权平均，零访问按桥接规则为 0。400 项平均 16.892961428734973%；5 核提高约 0.498874 个百分点。

当前日志保存 cache_hit_rate，没有保留逐项 hit_bytes 和 miss_bytes，因此不能从此归档严格复算全局字节加权命中率，也不能由 scheduled_copy_bytes 推算实际 DDR/L2 分池流量。命中率不是本轮择优的第一目标。

## 5. 过程、异常与解释

- 正式运行：400 次输入官方重评，1830 次候选官方调用，候选错误 0、生成器错误 0、候选预算耗尽 0。
- 最终 makespan：23 改善、377 持平、0 退化。接受规则实际为 (makespan, added_copy_bytes) 字典序，可接受耗时持平但搬运量下降。
- 2026-09-25 早期 probe：case_001–005、5 核，通用候选未优于有效 B 方案的 C 重评结果；case_003 因 verify_cap=12000 被跳过并返回 None，并非已证实超时。
- 后续 probe_iter1 仅覆盖 case_001、002、5 核，同样无耗时改善。两个小样本不能当成缓存感知优化的收益证据。
- 正式启动前经历缺少 C 参考键、缺少 _C_ 基线文件以及路径推导 NameError；修复基线回退和路径推导后才成功启动。此处保留失败事实，不将此前启动声明当作真实运行时间。
- _cache_affinity_traffic 仅是未接入评分的占位函数；本轮真实候选仍是 height/depth/fanout 与 delay0/100。不可写成缓存窗口/亲和性专项优化已完成，更未完成逐层消融。
- 原始 manifest 的 scope 与 target_scope 沿用 A/B 字样；config、expected_keys 和日志任务实际全为 C。保留原文件，并在本记录更正口径。
- 普通 C 评估为进程内调用，超时预算不保证强制中断；原运行器不支持断点续跑。

## 6. 证据路径

- results/plans_problem3_full400/：400 份最终 C 方案。
- results/solve_log_problem3_full400.jsonl：逐任务输入、候选、最终官方值和计划签名。
- results/solve_log_problem3_full400.summary.json：原始最终汇总。
- results/solve_log_problem3_full400.manifest.json：原始启动参数和文件指纹，不修饰历史字段。
- results/problem3_probe/summary.json 与 summary.csv：早期五例探针原始输出。
- results/problem3_probe_iter1/summary.json 与 summary.csv：后续两例探针原始输出。

## 7. 发布核验与结论

发布前核验结果：

- 400 个唯一 C 任务，覆盖 100 案例 × 4 核数，无缺失或重复；每项 official。
- 400 份计划规范化 SHA-256 全部匹配日志；总大小 50594625 字节。
- manifest 的 552 个原始文件全部存在，本机原始字节哈希全部匹配；官方评估器无 Git 差异。
- 各核 speedup、cache_hit_rate、胜平负与原始日志/汇总一致；单核参照独立复算未发现差异。
- 使用项目 .venv 运行原有测试 60 passed，新增 C 基线回退、不退化和统计口径测试后 65 passed。
- 测试命令：.venv/Scripts/python.exe -m pytest tests -q -rs --basetemp results/test_tmp_c400_<fresh_id>。临时目录不提交。最初默认 Python 的 pytest 临时目录权限错误已通过新建专用临时路径规避，最终测试在项目环境完成。
- 归档只核验现有证据，没有在发布时重跑独立 400 项官方模拟；跨机器检出换行和绝对路径变化会影响原始字节哈希，计划使用规范化 JSON 哈希比较。

结论：已获得完整 C400 官方记录，5 核平均 4.468436×、平均字节命中率 23.1639%，在本轮 C 保底上无耗时退化。只发布 H 分支，不自动合并 main；后续专项优化需独立运行与消融证据。
