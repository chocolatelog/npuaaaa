# 构造池重构实现说明

## 已实现内容

- `solver/construct_refine.py`：统一管理 HEFT、STRIP、CHAIN、NETBENEFIT 四类范式，以及 coarse、balanced、fine 三种粒度。
- `solver/pipeline.py`：主流程改用分层构造池，保留 `build_construct_pool` 兼容入口。
- `solver/construct.py`：CHAIN 支持按粒度限制子图算子规模，原有构造函数仍保留。
- `solver/validate_construct_pool.py`：构造池专项验证入口，使用进度条和 JSONL 断点续跑。
- `tests/test_construct_refine.py`：粒度计算、12 个 R0 候选和兼容入口回归测试。

候选元数据包含范式、粒度、场景、精化层、父候选、结构签名、目标算子数和目标条带数。精确结构重复会被去重，但在 R0 和 R1 阶段会保留四类范式的代表，避免 NETBENEFIT 因结构相同而丢失。

## 本次验证

执行了 1～30 号案例、4 核、A/B/C 三场景，共 90 个构造池任务：

- 小图、中图、大图各 30 个任务。
- 每个任务最终保留 6 个种子。
- 最终种子层级统计：R0 237 个、R1 126 个、R2 141 个、R3 36 个。
- 最终种子范式统计：HEFT 128 个、STRIP 176 个、CHAIN 124 个、NETBENEFIT 112 个。
- 未发现候选数量不足或断点记录缺失。

结果文件：`results/construct_validation.jsonl`。

## 运行命令

在项目根目录 `J:\数学建模\改进` 执行：

```cmd
python -m pytest tests/test_construct_refine.py -q
```

运行构造池验证（默认 1～30 号案例、4 核、A/B/C）：

```cmd
python solver/validate_construct_pool.py --start 1 --end 30 --cores 4 --scenes A,B,C --output results/construct_validation.jsonl
```

命令会跳过已经写入 JSONL 的案例、场景和核数组合，可以在中断后继续运行。

运行单案例官方评估：

```cmd
python -c "import sys; sys.path.insert(0,'solver'); from model import load_graph; from pipeline import solve_case; g=load_graph('通用神经网络处理器下的多核调度问题附件/data/case_001.json'); r=solve_case(g,2,'A',time_budget=3.0,seed=7,block_ops_cap=24,verify_k=1); print(r['est']); print(r['real'])"
```

## 说明

本轮专项验证只验证构造池和代理评价，不代表全量官方指标已经更新。完整性能结论仍需使用 `run_all.py` 在指定案例、核数和时间预算下运行，并用官方评估器复核。
