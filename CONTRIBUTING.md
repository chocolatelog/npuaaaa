# 协作开发规范

## 分支

- `main`：可复现、可提交的主版本，只接受经过指标验证的合并。
- `W`：成员 W 的实验和代码分支。
- `Y`：成员 Y 的实验和代码分支。
- `H`：成员 H 的实验和代码分支。

三条个人分支都从 `main` 创建。每个人只在自己的分支上提交，不直接向 `main` 推送。提交信息使用中文或英文均可，但要说明改动目标和验证方式。

## 合并门槛

合并到 `main` 前必须满足：

1. `python -m pytest` 全部通过；
2. 官方评估器无依赖环、非法调度和格式错误；
3. 在固定算例集合上，`makespan（总完成时间）` 平均值不劣于主版本；
4. 退化算例数量不能增加；
5. 提交实验命令、随机种子、设备参数和结果文件；
6. 说明显存占用，RTX 3070 8GB 下不能出现显存溢出。

### 指标优先级

合并决策按以下顺序：

1. 合法性和测试通过；
2. 平均 makespan 更小；
3. 退化案例更少；
4. 中位 speedup（加速比）更高；
5. 在平均 makespan 接近时，再比较新增搬运字节和运行时间。

如果一个版本只改善个别案例但导致平均 makespan 变差，保持当前 `main`。如果指标相同，选择代码更简单、运行更快、显存更低的版本。

## 提交和合并流程

```cmd
git checkout -b W && git add . && git commit -m "W: 描述本轮调度改进"
git push -u origin W
```

提交前在项目根目录运行：

```cmd
python -m pytest
```

合并由维护者在完成指标对比后执行：

```cmd
git checkout main && git pull --ff-only origin main && git merge --no-ff W && git push origin main
```

禁止提交 `results/` 下的原始大规模实验结果、断点数据库、日志、Python 缓存和本地环境目录。指标摘要和复现实验说明可以提交。

2026-09-25 H 分支归档例外：依据本次明确上传全部技术文档、实验数据和记录的要求，允许归档现有 results 下的方案 JSON、实验 JSONL、manifest、summary 和研究记录。此例外不包含密钥、环境、缓存、数据库、官方完整 trace，不改变 main 分支的合并门槛。范围和复现索引见 results/README.md。

2026-09-26 H 补充：根据上传当前技术路线和实验记录的明确要求，追加第三问 C400 的文档、400 份方案、原始 JSONL/manifest/summary 及两次早期探针摘要，用于证据核对与复现。保护范围与 main 合并门槛不变。
