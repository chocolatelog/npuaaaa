# 问题一：临时核心协作组与分层调度设计

所属分支：`W`（本次上传来源；原设计阶段记录保留）
文档版本：`P1-core-group-v0.1`（问题一核心协作组设计初版）
代码基线版本：`main-fix-v1.0.0`（发布修复版；本地实验扩展另见主技术文档）
更新时间：2026-09-25 08:35
状态：新方向设计，尚未实现、尚未实验；本轮未修改求解代码。
关联文档：[问题一全量复盘与实验方向大纲](问题一_全量复盘与实验方向大纲_v1.0.md)

> 历史草案状态：已合并，不再独立维护。合并时间：2026-09-25 00:27。算法设计已进入[整体大纲](问题一_全量复盘与实验方向大纲_v1.0.md)第 10.4 节，实施顺序、状态和验收统一见[固定实验计划](实验计划_问题一.md)。以下内容保留供追溯，存在顺序差异时以固定实验计划为准。

## 1. 对用户想法的判断

可以在调度策略中引入“几个核心暂时组成协作组，共同处理一个计算区域，完成后重新分配”的机制。建议称为临时核心协作组，避免把它误解成硬件真的融合。

题目不支持把一个官方子图绑定多个核心，也没有把单个原始算子拆成几个新算子的输出接口。因此，可实施方式是：先把计算区域划分成多个合法子图，再将这些子图联合安排到若干核心上。

协作组只存在于求解器内部，最后仍输出原有算子到子图映射，以及每核子图顺序。新增的是生成和搜索方案的策略，不是新的硬件能力。

## 2. 三个层次不能混淆

| 层次 | 定义 | 是否提交给官方 |
|---|---|---|
| 计算区域 | 例如一个分叉后再汇合的计算片段；可含多个最终子图 | 否，只是求解器分析对象 |
| 子图任务 | 某组原始非复制算子，固定由一个核心执行 | 是 |
| 核心协作组 | 在一段调度安排中参与该区域的核心集合 | 否，通过各核任务队列间接体现 |

一个重区域可以用三核处理，但必须落实为多个子图各自分核。一个运行中的子图不能被抢占拆开或半途迁移，缓存也不因分组而合并。

问题一每个子图独立清空缓存。同组两核仍是两个独立核心，两池容量不共享；所有组仍共同争用固定 60 字节/周期的外存带宽。

## 3. 分组如何形成、缩小与解散

假设一个区域包含三个可并行分支，最后进入同一个汇合算子。

第一阶段：为三个分支分别生成子图，安排给核心一、二、三；核心四处理其他区域。

第二阶段：核心二的分支先完成，若有其他合法任务可执行，就在其队列中安排这些任务。核心二无须陪同其他两个核心一起空等。

第三阶段：三个分支都完成后，汇合任务在某一个核心执行，其他核心继续处理别处。组的并行宽度自然缩小。

所谓“解散”，是各核后续任务改为来自其他区域，不需要增加官方不存在的解散指令。禁止为了表现分组而增加虚构的全组同步屏障。

本题提交的是静态队列。求解器可以在模拟中按任务完成事件动态决定下一步分配，最后将这些决定整理成固定队列；提交后官方按依赖和队列推进，并不运行我们额外编写的在线调度器。

因此，求解器也不能强制一个本来就绪的队首任务等到任意指定时间才执行。如果策略依赖精确预约起始时刻，必须确认这种效果能由合法核序列表达，否则放弃该候选。

## 4. 什么区域适合多核协作

| 结构/瓶颈 | 判断 | 主要原因 |
|---|---|---|
| 多条独立而较长的计算分支 | 优先尝试 | 有可利用的算子级并行 |
| 当前一个大子图包住多个独立分支 | 优先尝试拆分协作 | 可能恢复被过度聚合压掉的并行 |
| 一条很长的依赖链 | 通常少核 | 计算量大也不代表能并行 |
| 一个原始算子占绝大多数耗时 | 当前题目接口下收益受限 | 不能把该算子改写成几个并行算子 |
| 共享外存搬运已占主要时间 | 谨慎 | 更多核心不增加总带宽，可能加剧竞争 |
| 分支很短、跨任务等待占比高 | 谨慎 | 拆分增加的 100/1000 周期等待可能超过收益 |

需统计区域内矩阵/向量工作量、依赖关键路径、就绪宽度、共享输入、边界复制和局部溢出。就绪宽度只反映结构机会，不是已证实可达加速比。

不能直接把区域单核耗时除以核心数作为多核耗时，也不能把多个核心的缓存容量相加后声称任务能放下。

## 5. 推荐算法：区域切分与核心数量联合选择

### 5.1 区域来源

第一版从已有保底方案中选最多两个区域：关键路径上局部时间较长且包含分支的任务，以及影响最终完成时间的分叉—汇合片段。

第一版只支持区域等于一个已有子图，或若干已有完整子图的并集；暂不截取其他任务的一半。分叉—汇合区域包含内部路径上的节点，并检查外部连接；必须保留全部外部依赖，不以“它属于其他组”为理由忽略。

单任务内部若只有一条强依赖链，则仅保留单核对照。块化阻止必要分支拆分时，先记录为受限案例；局部细块化另做独立消融，不能偷偷改变全图搜索空间。

### 5.2 核心子集与核心数量

目前最多五核，可以枚举一至五核的所有非空核心子集，最多 31 个，作为廉价选择层。具体核心子集不能只按核心编号选择，应考虑现有队列可用时间与组外关键任务。

虽然核心硬件相同，不同核心已有的任务、前驱所在位置和依赖等待不同，因此选哪几个核心也有影响。

先固定保底计划，保留区域外任务的相对顺序。对区域任务试不同核心子集和插入位置，允许在组内任务之间穿插已经就绪的外部任务；不为协作组独占预留一整段空闲时间。

### 5.3 每种核心数量对应什么切分

不能先固定唯一切分，再简单增加核心数。第一版对每个数量生成两种分区：

1. 分支保持型：同一独立分支内部尽量保留连续依赖链，不为凑核心数强行拆链；公共前缀和汇合部分另作任务候选。
2. 工作量均衡型：对过重分支只在已有可并行结构或明显溢出位置增加切分，使用矩阵/向量工作量与局部时间估计，不按算子数量平均分。

两个候选都重新生成真实边界复制，检查算子完整覆盖、分区商图无环以及核序列合并图无环。第一版拒绝不合法候选，不通过虚构依赖或修改原图来修复。

### 5.4 应比较整图收益

对每个区域候选生成完整可执行计划，再比较整图预计完成时间。定义：

`预计整体收益 = 保底计划的预计完成时间 - 候选完整计划的预计完成时间`。

注意，多给一个核心可能让区域更快，却把别处的关键任务挤到后面。所以不能只比较区域结束时刻，也不能忽略组外搬运请求。

下表仅用于说明决策，不是实验结果：

| 区域使用核心数 | 区域完成耗时 | 整图完成耗时 | 选择 |
|---:|---:|---:|---|
| 1 | 30000 | 40000 | 单核对照 |
| 2 | 18000 | 28000 | 本例最佳 |
| 3 | 15000 | 32000 | 区域更快，但影响其他任务，整图更慢 |

多个区域都需要核心时，第一版按“当前整图瓶颈区域”依次提出候选，每次接受后重新计算全局状态，不预先固定永久分组。第二版才考虑两个区域同时改变核心数，避免第一版搜索规模失控。

### 5.5 预算与接受

每区域每种核心数先通过廉价完整重放选两种不同切分/核心子集组合；两区域最多二十份。随后按预计收益排序，但为不同核心数保留多样性，合计最多十二份局部精评；最后两份不同结构方案进入全局复核。

廉价完整计划重放总次数最多二千，按区域轮流分配候选次数，避免第一个区域耗尽全部额度。任务插入位置枚举也计入此额度，超限返回当前保底。

局部精评只重算受影响分区；共享带宽和依赖等待必须在完整计划上重新评估。使用快速官方语义副本复核时，新最终方案仍由原官方确认。

只在官方总完成时间更小，或时间相同而新增搬运更少时接受。所有异常、非法、超预算或无改善路径都保留原方案。预算为初始实验设置，不是已调优结论。

## 6. 如何体现真正的分层

上层决定：哪个计算区域需要优先处理，尝试给它多少个核心，哪些其他区域会受到影响。

下层决定：区域拆成哪些合法子图、子图分别在哪个核心、排在该核什么位置。

底层验证：局部缓存与复制、全局共享带宽、完整依赖、原官方总完成时间。

上层不能把区域简单当作固定时长的黑盒。下层应回传不同核心数对应的分区、时间、字节、外部依赖释放信息，上层据此做选择。

这相当于将单任务调整扩展为“一个区域内多任务的联合分区和分核动作”。它不扩大题目允许的最终方案集合，但有机会在有限预算内更有效地搜索原来就合法的好方案。

## 7. 与当前路线的关系

当前自适应切分计划解决“怎么拆”，复合邻域解决“怎么移动与排序”。核心协作组将两者联合成一个更大的候选动作，因此应作为自适应切分之后的独立挑战模块验证，不直接替换整个构造池。

第一轮从完整快速三层的保存方案出发，加入分组候选；先验证固定输入下的改善，再接端到端。比较对象必须包括同预算的普通拆分加自由分核，判断收益究竟来自分层组织，还是仅仅多试了几个候选。

重点诊断 049、082、005：它们有大任务或核心闲置线索，但需要先确认内部可并行结构。044 和 067 用于观察内存及带宽代价；另加严格链与单大算子反例，验证不会无意义组大核。

065 的已有证据主要支持固定切分交换，不能拿它直接证明区域多核协作有效。

## 8. 实施接口与验证设计

拟新增 `solver/region_group_refine.py`（区域协作组精化），输入完整图、保底计划、官方基线及统一评估回调，输出改进计划、对应指标和审计记录。文件尚未创建，不提供虚构命令行参数。

内部区域记录包括：原始算子集合、受影响原子图、外部前驱/后继、候选核心集合、最终子图映射和每核序列。区域标识仅写入实验审计，不写入官方提交文件。

每个候选记录：区域来源、核心数量与具体集合、拆分类型、局部预测、整图预测、分区/溢出字节、组外任务延迟、官方结果、求解耗时及拒绝原因。

消融顺序：原方案；普通拆分加自由分核；固定两核组；自适应核心数量；自适应数量加完成后逐核释放。最后一种释放体现在核序列安排，不增加运行时指令。所有组使用相同候选评估预算。

必须验证：严格依赖链不因组核得到虚假加速；单算子不被拆写；多核缓存和带宽不被错误相加；所有外部依赖保留；无不必要的组屏障；完整方案无环；局部更快但全局更慢的方案不被接受。

每阶段实验保留逐任务进度和版本指纹，可从已完成配置续跑。实施前向用户说明具体改动并按项目约定确认；目前仅记录设计，不执行新实验。

## 9. 官方源码依据

附件目录：`J:/数学建模/改进/通用神经网络处理器下的多核调度问题附件/code/`。

- `stub_multicore_cut_and_schedule.py`（分区方案解析）：约 143～185 行，验证原始算子覆盖，建立子图到单个核心的映射。
- `multicore_cut_evaluate_problem_1.py`（问题一评估）：约 183～185 行，每任务保存一个核心编号；约 295～300 行，激活任务时占用相应核心。
- 同文件约 318～342 行，只有任务前驱完成且同核/跨核等待满足时，才激活该核队首任务；不能附加自定义的任意启动时刻。
- `evaluation_validation.py`（合法性检查）的任务顺序检查，将子图依赖边和核内相邻顺序边合并判环。

本设计希望验证的是“按计算区域联合选择切分与临时核心集合”能否提高搜索效率和最终质量。既不预先承诺收益，也不宣称该思想本身为首次提出。
