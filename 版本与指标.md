# 版本与指标记录

## 当前版本（Y 分支 · w6 融合版）

Y 分支在本轮完成：以 W 分支 E09 模块集（事件模型、束搜索顺序精化、
分区/区域/预算后处理）为主干，叠加三项独立验证增益（场景 A heft 的
M/V 互补配对、W2 H=2 后继前瞻、小图 K 网格真值候选），并保留
keep-best 单调改进协议与 repro 可复现清单体系。

## 已验证结果（100 用例全量，官方评估器，固定 config.txt）

平均加速比（单核基准）：

| 核数 | 问题 1（场景 A） | 问题 2（场景 B） | 问题 3（场景 B + L2） |
|---|---|---|---|
| 2 | 1.796 | 1.729 | 1.769 |
| 3 | 2.421 | 2.351 | 2.401 |
| 4 | 2.940 | 2.851 | 2.969 |
| 5 | **3.382**（中位 3.84，最大 5.27） | 3.261（中位 3.70） | 3.425（中位 3.98，最大 5.50） |

三指标摘要（N=5）：

- 指标 2 额外搬运：P1 22.9MB（中位 0.78MB，/原始 中位 1.04×）；
  P2 21.3MB（中位 0.57MB，0.87×）；P3 20.8MB（中位 0.52MB，0.85×）；
- 指标 3 Cache 命中率（问题 3）：均值 18.48%（中位 15.50%，最大 85.3%），
  L2 加速比 1.0556（随核数单调增长）。

完整逐用例数据见 `results/summary.csv` 与 `results/appendix.md`。

## 可复现命令

```bash
cd solver
python run_all.py --cases 1-100 --cores 2,3,4,5 --scenes A,B --workers 8
python evaluate_official.py --workers 6
python aggregate.py
```

随机种子由 `solver/repro.py` 的稳定哈希生成（crc32），同环境重跑产出
字节级一致的方案（已验证）。

## 评审要求核对

- 变更说明：见 `技术文档.md` §8 迭代历史与 `results/final_plan.md`；
- 测试：官方评估器 2500+ 项全部成功，无非法方案；
- 固定随机种子：repro.stable_seed（crc32）；
- 基线/新版本指标：`results/baseline_comparison.md`（对比 stub 基线）；
- 退化算例：keep-best 协议保证无退化（`results/improve_decisions.json`）；
- GPU：未使用（纯 CPU 求解）。
