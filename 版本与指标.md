# 版本与指标记录

## 当前主版本

当前目录作为首个 `main` 主版本上传。它包含最新版 `solver/`、官方评估器代码、可复现实验输入、中文技术文档和指标摘要。

## 已验证结果

第一问第二轮保护结果位于本地 `results/q1_full_v2_guarded`，覆盖 2 至 5 核、100 个算例。平均相对同核基线加速比约为：2 核 1.09 倍、3 核 1.11 倍、4 核 1.09 倍、5 核 1.09 倍。完整结果因体积较大不进入 Git；必要时按本文件中的实验命令重新生成。

## 评审要求

新分支必须提交：

- 变更说明；
- 测试命令和输出；
- 固定随机种子；
- 基线和新版本的 makespan、speedup、改善率；
- 退化算例清单；
- GPU 型号和显存占用。

指标不超过当前主版本时，不合并到 `main`。具体规则见 `CONTRIBUTING.md`。
