# 问题一：事件驱动资源表与增量评估设计

所属分支：`W`（本次上传来源；原设计阶段记录保留）
文档版本：`P1-resource-table-v0.1`（问题一资源表设计初版）
代码基线版本：`main-fix-v1.0.0`（发布修复版；本地实验扩展另见主技术文档）
更新时间：2026-09-25 08:35
状态：方案设计，尚未实现新的统一资源表；未修改求解代码或运行新实验。
关联文档：[临时核心协作组设计](问题一_临时核心协作组设计_v0.1.md)、[整体实验方向大纲](问题一_全量复盘与实验方向大纲_v1.0.md)。

> 历史草案状态：已合并，不再独立维护。合并时间：2026-09-25 00:27。算法设计已进入[整体大纲](问题一_全量复盘与实验方向大纲_v1.0.md)第 9.5～9.6 节，实施顺序、状态和验收统一见[固定实验计划](实验计划_问题一.md)。以下内容保留供追溯，存在顺序差异时以固定实验计划为准。

## 1. 结论与边界

采用资源表是合理方向。推荐设计是各资源模块负责自己的状态，由同一个事件循环统一推进模拟时间并提交更新；在一致的状态上，调度器查询资源、产生候选、比较完整方案。

不建议第一版让二至五个模拟核心分别运行线程、互相广播状态并自主抢任务。共享带宽和跨核依赖需要一致状态，小规模系统中的分布式同步可能比直接查表更贵。

借鉴路由表的三个思想：记录当前状态、变化时更新、查询时复用。不能照搬过期信息可暂时传播的机制；调度中的过期信息可能导致错误估时、重复分配或违反依赖。

题目最终接收固定的算子映射和每核顺序。因此这套机制运行在我们的方案生成与评估过程中，不能假设官方硬件额外执行一个在线查表调度器。

## 2. 两种提速应分别验证

第一种是求解器提速：避免重复构图、重复统计和扫描全部任务，使同一个方案的评估更快。相同方案的官方执行时间不因此改变。

第二种是方案质量改善：在相同求解预算内探索更多有效候选，或者利用更准确的资源状态避免拥挤，最终找到官方执行时间更短的方案。

必须先固定保存方案验证第一种，再做同预算搜索验证第二种。不能把查表耗时降低百分比直接当作神经网络执行加速百分比。

## 3. 当前代码已经具备什么

| 现有位置 | 已有机制 | 与新设计的差距 |
|---|---|---|
| `solver/model.py`（基础代价模型） | 整方案评估缓存、每核可用时刻、就绪堆、任务完成时间 | 仍是粗模型；整方案变化后不一定能复用局部工作 |
| `solver/scene_a_event.py`（局部事件精评） | 子图局部构图、访问/溢出统计、每核指针与前驱完成检查 | 每次调用会重新做许多局部工作，共享带宽时序仍为近似 |
| `solver/task_order_search.py`（固定切分调度搜索） | 固定切分时复用任务局部时间，廉价重放不同队列 | 尚非完整共享资源事件重放 |
| `solver/scene_a_fast.py`（快速官方语义副本） | 每任务剩余操作计数，替代反复扫描其全部操作是否完成 | 已验证的一项局部计数优化，不等于已有完整增量模拟系统 |
| 原官方场景 A 评估器 | 每核当前任务、队列指针、管道执行状态、前驱计数、外存剩余请求 | 语义依据；不能修改官方文件来制造评估收益 |

新工作应优先补局部模板缓存、依赖版本和一致的事件更新接口，不重复写一套已有状态却无验证收益的系统。

## 4. 资源表分为四类，另设局部代价缓存

### 4.1 核心表

每核记录当前任务、核序列位置、上一任务完成时刻、各管道的活动操作与队列、局部缓存状态、协作区域标识、状态版本。

核心的预计空闲时刻是预测，外存竞争变化可能使它改变，不能作为永久确定的预约时刻。

协作组标识只帮助搜索，不给予核心额外算力、共享缓存或带宽。一个任务结束后按场景 A 初始化下一个任务的私有缓存状态。

### 4.2 任务表

每任务记录所属子图、分配核心、未完成前驱数、前驱完成与依赖等待信息、任务状态、剩余操作数、局部模板标识与版本。

任务状态至少区分：依赖未完成、依赖完成但等待未结束、具备启动条件、执行中、完成。前驱数归零并不意味着立刻能执行，还要满足跨核等待、同核切换和核队首限制。

在固定方案评估模式下，不能从核心队列中跳过被阻塞的队首任务去运行后面的任务。在构造搜索模式下可以提出改变顺序的候选，但必须生成新计划并重新验证。

### 4.3 共享外存表

记录当前模拟时间、上次结算时间、所有活动请求的任务/操作身份、剩余独占服务量、带宽池版本和预计完成事件。

请求进入或离开时，先按旧活动集合把服务量推进到当前时刻，再修改集合、重新预测完成时间。不能先把新请求加入，再让它分享过去已经消耗的带宽。

活动请求数为 q，以独占周期表示的每个请求在 Δ 时间内得到 Δ/q 的服务量。周期取整、同时完成与浮点误差处理须与官方实现一致。总带宽始终为 60 字节/周期。

一个请求变化，可能改变其他核心请求的完成时间；此时必须使相关核心和后继预测失效，不能只更新发起变化的核心那一行。

### 4.4 区域/协作组表

记录区域成员、尚未完成的子图、候选/当前核心集合、已有核心释放事件，以及不同核心数量对应的完整候选计划评估。

“区域用三核需要多久”不是永远不变的常数：组外负载和共享带宽变化会改变时间。允许缓存区域分区与局部模板，不允许脱离上下文复用其全局完成时间。

组中一个核心完成当前任务后可参与其他区域，不添加人为全组同步。

### 4.5 局部模板和代价缓存

局部模板包含真实边界复制、官方对应步骤生成的操作依赖、内存补边、局部访问/溢出结果等。缓存键必须包含图与官方配置/源码摘要、精确成员和边界、会影响排序的编号及顺序。

当前局部构图会分配边界操作和张量编号。即使某子图成员不变，其他分区改变也可能影响其生成编号；第一版不能只按成员集合判断模板相同。先由一致的构图过程得到完整模板摘要，再复用昂贵局部评估；后续若要进一步跳过构图，需要单独证明生成编号和语义保持不变。

只改核心和核内任务顺序时，局部模板通常可以复用，完整全局时间仍需重放。切分改变时，至少重建全部受影响模板，并根据模板摘要决定其余缓存是否仍有效。

## 5. 更新机制：资源负责状态，事件循环保证一致

推荐单个候选评估使用一个确定性事件循环：

```text
读取下一个尚有效的事件时刻
将活动计算与外存服务推进到该时刻
按官方规则处理同刻完成、释放与依赖变化
更新任务计数、核心状态和外存请求集合
重复处理该时刻产生的零时延状态变化，直到稳定
在固定计划模式下，检查各核合法队首任务及内部管道发射条件
产生下一批预测事件
```

上面是结构说明，不替代官方同刻事件的具体顺序。实现必须逐项对齐官方；不能简单把“先完成再发射”的口号当作等价证明。

各模块拥有明确的状态写入接口，单个事件内的相关修改一起完成，再允许下一次调度查询。第一版不使用多线程写共享表，因此无须引入复杂分布式锁协议。

候选分支读取一份不可变基础快照，自己记录修改；多个分支不得共享可变资源表，否则一个候选试放任务可能污染另一个候选。

## 6. 版本和失效机制

核心、任务、局部模板、共享带宽池分别维护递增版本。预测事件至少关联任务/操作身份、候选分支标识和依赖资源版本。

当带宽变化后，旧的请求完成事件仍可能留在事件堆中。弹出时发现版本过期则丢弃，不把任务错误标为完成；同时确保相应的新完成事件已经生成，避免丢掉所有唤醒事件。

“丢弃”指内存中的过期事件，不涉及删除用户文件。

维护反向关联：某张量影响哪些子图，某任务解锁哪些后继，某带宽请求影响哪些活动预测。只更新确实受影响的项；对无法可靠确定影响范围的改动，采用完整重放。

## 7. 对一次候选修改怎样增量处理

### 7.1 只迁核或改顺序

复用未变化的局部图和溢出统计；重建受影响的核序列边；完整合并图判环；重新模拟全局时间。第一版到这里为止，先不冒险只重算两个核心的时间。

原因是两个核心上的搬运时刻变了，会改变其余核心的竞争，进而传播到全图。保持局部代价不变，不等于其余核心全局时间不变。

### 7.2 拆分、合并或移动边界

重建新旧边界涉及的子图，重新计算复制和溢出；按完整模板摘要查询缓存；更新全图依赖，然后完整重放。

### 7.3 第二阶段才做事件后缀重放

在已确认精确等价后，为完整状态保存检查点。候选发生变化时，只能从一个已证明此前事件前缀不变的检查点恢复；不能仅凭“修改的是靠后的任务”就认定前面的调度不受影响。

恢复状态包括全部活动外存请求及剩余工作、管道队列、任务计数、依赖计时和核序列位置。无法证明前缀一致，或者切分改变局部编号和发射顺序时，退回从头重放。

这一阶段可能进一步提速，也可能因为快照和失效传播成本过高而没有收益，需要独立测量。

## 8. 与临时协作组结合

上层区域调度查询：哪些核心可用、哪些任务即将解锁、共享外存是否拥挤、使用不同核心集合的候选完整计划如何。

下层任务调度使用同一份一致状态生成分核和顺序。任务结束事件更新区域完成计数，释放相关核心。整个机制仍生成正常的官方计划，资源表本身不是官方输出字段。

组外请求不能从共享外存表中消失；组的预计收益必须计入对其他区域的影响。不能让每个区域单独查自己的局部表后都声称拿到了全部带宽。

## 9. 实施顺序、文件和验收

第一阶段：为 `solver/scene_a_event.py`（局部精评）补完整模板摘要及局部结果缓存，在固定切分顺序搜索中复用；保持当前全局重放和官方复核。

第二阶段：按原整体大纲建立 `solver/scene_a_replay.py`（拟建共享带宽重放模块），将任务、核心和外存状态置于统一事件循环。已有快速官方副本继续作为对照，不能边改副本边把它当唯一真值。

第三阶段：增加版本事件和可恢复状态检查点，比较从头重放与后缀重放；检查点成本过高则保留第一、二阶段。

第四阶段：临时核心协作组和短窗口前瞻读取该状态，评估对方案质量的作用。第一版不部署分布式线程或消息广播。

验收分开进行：

1. 固定保存方案，对比未缓存/缓存、从头/增量的全部官方结果字段；事件差异仅在诊断样本详细比较，防止轨迹输出开销掩盖提速。
2. 专门覆盖同时完成、请求中途加入、过期事件、重复唤醒、跨核等待、队首阻塞、切分导致编号变化、分支状态污染等反例。
3. 报告局部构图、缓存查询、事件更新、完整评估耗时及峰值内存；缓存命中率高不代表总时间一定下降。
4. 比较同等求解时间下完成的候选数、有效候选数及最终官方耗时；另用固定候选列表证明评估提速没有改变结果。
5. 仅持久化可复用且版本匹配的结果，实验按任务记录进度和指纹，完成项可续跑。模拟内检查点不自动等同跨进程断点恢复，二者分开实现和表述。

资源表是已有事件调度思想的工程完善，是否有价值要靠重复工作减少和最终方案质量证明，不以“像路由协议”作为创新或性能依据。
