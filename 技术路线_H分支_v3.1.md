# H 分支当前技术路线：A/B 归档与第三问 C 全量官方核验

版本号：H-v3.1。更新时间：2026-09-26（Asia/Shanghai）。目标分支：H。
基线提交：33feb6141dab5570d65423739d9f377d8559be59；本轮另含 C 入口适配、亲和性分析占位和探针脚本的工作区改动，精确运行源码以 results/solve_log_problem3_full400.manifest.json 中的 SHA-256 为准。
负责模块：操作级候选、C 场景官方验证、B 方案初始化、不退化择优、统计与复现。

## 1. 当前成果与口径

A/B 既有 800 项结果保留不变，详细流程见 [H-v3.0](技术路线_H分支_v3.0.md)。本次新增第三问 C：case_001–case_100 × 2/3/4/5 核，共 400 项，全部取得本轮官方结果；不混入代理分数。

| 核数 | A 全 100 案例 | B 全 100 案例 | C 全 100 案例 | C 平均字节命中率 |
|---|---:|---:|---:|---:|
| 2 | 1.720799× | 2.267487× | 2.286691× | 9.8219% |
| 3 | 2.253361× | 3.065740× | 3.164093× | 16.6667% |
| 4 | 2.710850× | 3.677874× | 3.858122× | 17.9194% |
| 5 | 3.078658× | 4.225545× | 4.468436× | 23.1639% |

加速比为各案例 singlecore_makespan / official_makespan 的算术平均。命中率先按案例计算 hit_bytes / (hit_bytes + miss_bytes)，再对完整 100 案例取算术平均；零总访问量按桥接代码记 0。不等于按访问次数统计，也不等于合并全部字节后的加权命中率。

## 2. 官方第三问模型保持不变

使用附件 code/multicore_cut_evaluate_problem_3.py 的 evaluate_problem_3，经 solver/pipeline.py:real_evaluate 调用。C 在 B 模型上增加共享只读 FIFO Cache。

- DDR 带宽 60 B/cycle；L1 容量 524288 B；UB 容量 131072 B。
- 跨核拷贝延迟参数 500 cycles；Cache 容量 1048576 B（1 MiB）；Cache 带宽 250 B/cycle。
- 只有 COPY_IN 查询缓存；命中走独立 CACHE_READ 带宽池，未命中走 DDR。
- 发射时判断命中；完成 COPY_IN 时调用插入逻辑。已在缓存中的条目不改变顺序；容量不足从最早插入的条目开始淘汰；超过容量的张量不插入。
- 官方退休逻辑对每个 COPY_IN 完成都调用插入检查，包括读取期间已被淘汰的键，优化器以官方事件实现为准。
- 不改变原始图、流水线、内存约束、FIFO 策略或官方评估器源码。

## 3. 实际运行的求解流程

1. 读取原始输入、results/summary.csv 单核参照及同案例/核数的 B 最优方案。
2. C 输入优先查找对应 _C_ 文件；目录内没有 C 时读取 _B_ 文件。本轮输入目录只有 A/B。
3. 将输入方案重新送入官方 C 评估器，得到本轮 C 保底成绩，而不是沿用 B 分数。
4. 构造保留依赖的计算 DAG，用每核、每 Pipe 占用日历生成操作级列表候选。
5. 本轮 max_candidates=6：height/depth/fanout 三种优先级分别配合 delay=0/100；该 delay 是候选启发式参数，不替代官方跨核延迟 500。
6. 候选按规范化 JSON 签名去重，并逐个官方 C 核验。按 (makespan, added_copy_bytes) 字典序严格择优；耗时相同时允许新增搬运量下降。
7. 保存最终计划、签名、输入/候选记录、计算下界证书、汇总和启动指纹。makespan 胜平负按耗时单独统计。

本轮 400 次输入重评、1830 次候选评估，最终相对本轮 C 保底为 23 改善、377 持平、0 退化。该不退化结论只针对固定输入和官方评估模型，并不表示所有新策略零风险。

## 4. 已实现与尚未实现的边界

- 已实现：C 分支入口、B 方案回退加载、官方 C 验证、现有通用操作级候选、有效结果保底。
- _cache_affinity_traffic 当前返回值被赋给 affinity，但未传入 _schedule 或评分；该函数还通过字符串类型筛选消费者，整数节点 ID 不会被纳入。因此不能把当前收益归因于缓存亲和性、消费者窗口聚类或局部跨核迁移。
- 本轮运行不使用 solver/model.py 中的 C 近似模型筛选这组操作级候选；不能宣称本轮完成了严格 FIFO 代理校准。
- 尚未实现/验证：真正的缓存窗口调度、局部分核扰动、Pipe 对齐专项优化以及逐层消融。
- 计算下界证书省略拷贝、同步、DDR 竞争等因素，不是完整问题的最优性证明。

## 5. 运行与复现边界

6 个并发任务；每任务最多 6 个候选。单次隔离评估上限 360 秒、隔离生成上限 120 秒、候选生成及尝试预算 300 秒。

超过 12000 操作的图通过隔离子进程以放开 verify_cap 的官方调用评估，共 76 个大图配置；普通 C 任务进程内评估，预算在调用间检查，不是所有调用都具有可强制中断的端到端超时。

该入口无随机种子参数，候选构造确定；实际候选数量仍可能受墙钟预算影响。不覆盖已存在输出；没有断点续跑入口，需要新目录。

详见 [C400 实验记录](实验记录_H分支_第三问C400全量_v1.0.md) 和 [归档索引](results/README.md)。原始 manifest 中 scope、target_scope 仍是 A/B 历史说明文本，属于旧元数据；实际范围以 config.scenes=C、400 个 expected_keys、逐项 scene=C 及官方调用分支为准。保留原始文件，不改写历史证据。

## 6. 后续工作

先设计围绕现有 C 最优方案的小扰动，在依赖/内存合法性下控制跨核新增搬运；分别测试亲和性、拓扑窗口、Pipe 对齐，再以官方耗时和搬运量验收。不承诺新策略必然增益，不把当前结果描述为获奖门槛或全局最优。

本次仅发布 H 分支，不自动合并 main。
