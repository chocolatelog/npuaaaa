# 问题一技术路线

## 1. 目标

在官方问题一评估器约束下，最小化多核调度方案的 `makespan`，并在完成时间相近时减少 `added_copy_bytes` 和 `scheduled_copy_bytes`。本方案报告的是候选预算内的 best-known 解，不声称已经证明数学全局最优。

## 2. 建模

将原始 Op-Tensor 二部图收缩为非 COPY 操作的依赖 DAG。每个候选方案同时决定：

1. 操作到子图的划分；
2. 子图到处理器核的分配；
3. 每个核上的子图执行顺序。

划分后构造商图，并检查商图无环。所有输出方案必须覆盖全部非 COPY 操作，且 `core_schedules` 覆盖全部子图。

## 3. 候选生成

- Metis/图划分生成多个子图数量候选；
- 使用 `critical`、`id`、`level`、随机拓扑序产生不同初始解；
- HEFT 风格地利用官方 Step1/2/3 的子图工作量估计分配核；
- 以贪心方案作为稳定基线。

## 4. 搜索与筛选

候选先通过代理目标预筛选，再调用官方问题一评估器。局部搜索采用以下邻域：

- 高通信相邻子图合并；
- 大负载子图拆分；
- 边界操作迁移；
- 子图换核。

禁忌搜索允许有限幅度的劣化移动以跳出局部最优，但最终始终按官方三元组严格选择：

```text
(makespan, added_copy_bytes, scheduled_copy_bytes)
```

蚁群、模拟退火和 Pareto 归档用于扩展候选集；它们不能替代官方评估器。

## 5. 验证流程

```text
数据读取
  -> DAG/商图合法性检查
  -> 多初始候选生成
  -> 代理筛选
  -> 禁忌/蚁群/退火搜索
  -> 官方评估器真值验证
  -> 按官方指标排序并保存方案
```

全局测试采用断点续跑和分批时间预算。结果只在官方评估器通过后计入统计；大图单独记录运行时间，避免中断时丢失已完成结果。

## 6. 复现实验

```bash
python -m py_compile aco_scheduler.py greedy_sa_scheduler.py \
  problem1_heft.py problem1_select.py global_test.py

python -u global_test.py \
  --data-dir "/Users/bailansen/中文题目/A题/通用神经网络处理器下的多核调度问题  附件/data" \
  --output-dir /tmp/problem1_global_official_20260924 \
  --cores 2 3 4 5 --quick --resume --max-seconds 300
```

## 7. 当前边界

全局最优需要穷举全部划分、核分配和顺序，实际不可行。因此论文中应使用“官方评估器验证的候选预算内最优/当前 best-known”，并报告候选数量、随机种子、运行时间和失败案例。
