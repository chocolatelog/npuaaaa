# H 分支操作级 800 项全量实验：数据统计

实验日期：2026-09-25。以下结果从本轮原始日志重新计算，并核对最终计划的规范化 SHA-256。

## 1. 范围和指标

- 全量：100 个案例 × A/B 两个场景 × 2、3、4、5 核，共 800 项。
- 历史可比子集：每组 81 个案例，共 648 项。
- 其余大案例：每组 19 个案例，共 152 项；由运行程序按操作数大于 12,000 标记。
- 全部 800 项均保存了有效官方评估结果，无重复或缺漏任务，最终计划文件 800 份。
- 加速比为单核 makespan / 多核官方 makespan；分组统计取逐例加速比的算术平均，不是总运行时间之比。
- 改善/持平/退化按 makespan 比较。候选接受使用 (makespan, added_copy_bytes) 字典序，次要目标也不允许在 makespan 相等时变差。

## 2. 全量结果：每组 100 个案例

| 核数 | A 平均加速比 | B 平均加速比 |
| --- | ---: | ---: |
| 2 | 1.720799× | 2.267487× |
| 3 | 2.253361× | 3.065740× |
| 4 | 2.710850× | 3.677874× |
| 5 | 3.078658× | 4.225545× |

800 项混合算术平均为 2.875039×，中位数 2.881267×。混合均值包含不同核数和场景，不能作为某一固定核数的专项成绩。

## 3. 历史可比子集：每组 81 个案例

| 核数 | A 平均加速比 | B 平均加速比 |
| --- | ---: | ---: |
| 2 | 1.795990× | 2.308702× |
| 3 | 2.428981× | 3.104269× |
| 4 | 2.983310× | 3.710305× |
| 5 | 3.457662× | 4.284529× |

该范围由 `results/solve_log_guided_full.jsonl` 中具有有效官方结果的相同任务确定，不是随意选取最好的 81 个案例。

## 4. 新增大案例：每组 19 个案例

| 核数 | A 平均加速比 | B 平均加速比 |
| --- | ---: | ---: |
| 2 | 1.400245× | 2.091782× |
| 3 | 1.504669× | 2.901483× |
| 4 | 1.549314× | 3.539619× |
| 5 | 1.462900× | 3.974085× |

A/5 全量均值等于 `(81 × 3.4576623761 + 19 × 1.4629000825) / 100 = 3.0786575404`。
因此 81 例子集的 3.4577×不能作为全 100 例的成绩或保底。全量数值更低是范围扩大后的结果，不能据此认定相同案例性能退化。

## 5. 相对本轮输入组合的全量比较

输入组合包括 `plans_n5_oplist_full81`、`plans_n5_expanded_full` 和 `plans_guided_full`，并在本轮重新评估。

| 分组 | 输入平均加速比 | 最终平均加速比 | 改善 | 持平 | 退化 |
| --- | ---: | ---: | ---: | ---: | ---: |
| A/2 | 1.720799× | 1.720799× | 0 | 100 | 0 |
| A/3 | 2.242417× | 2.253361× | 4 | 96 | 0 |
| A/4 | 2.690343× | 2.710850× | 6 | 94 | 0 |
| A/5 | 3.058258× | 3.078658× | 5 | 95 | 0 |
| B/2 | 1.724636× | 2.267487× | 85 | 15 | 0 |
| B/3 | 2.272680× | 3.065740× | 80 | 20 | 0 |
| B/4 | 2.713527× | 3.677874× | 71 | 29 | 0 |
| B/5 | 3.753891× | 4.225545× | 15 | 85 | 0 |
| 合计 | — | — | 266 | 534 | 0 |

另一种比较是 648 项相对单一历史参考日志 `solve_log_guided_full.jsonl`：325 改善、323 持平、0 退化。两套胜平负使用不同基线，不应互相替换。

## 6. 搜索过程、错误与限制

- 记录的总墙钟耗时：11,881.969 秒，约 3.30 小时；并发任务数为 6。
- 输入官方调用：974 次；候选官方调用：6,534 次。
- 候选错误记录：98 次；生成器错误记录：33 项；候选预算耗尽：97 项。
- 上述计数可能重叠，不能直接相加为失败任务数，也不等于最终方案失效。最终 800 项的状态均为 `official`。
- 结果证明本轮已覆盖并评估全部任务；不代表搜索过程零错误、不代表每项都找到了全局最优，也不构成比赛奖项承诺。

## 7. 数据文件与复核方式

- 原始运行日志：`results/solve_log_oplist_full800.jsonl`。
- 运行配置和输入指纹：`results/solve_log_oplist_full800.manifest.json`。
- 运行结束时的原始汇总：`results/solve_log_oplist_full800.summary.json`。
- 可重算聚合统计：`results/oplist_full800.statistics.json`。
- 按案例、场景、核数排序的 800 项明细：`results/oplist_full800.tasks.json`。
- 最终调度计划：`results/plans_oplist_full800/`。
- 历史参考日志：`results/solve_log_guided_full.jsonl`。
- 单核参照值：`results/summary.csv` 中的 `singlecore_makespan`。

在仓库根目录运行：

```powershell
.venv/Scripts/python.exe solver/export_oplist_report.py
```

导出程序会先检查完整任务矩阵、任务唯一性、800 份输出计划及其规范化 SHA-256、800 项加速比公式、每组均值与胜平负、已有汇总一致性，再更新两个统计 JSON 文件。

本次归档复核没有重新运行完整官方评估器；官方有效性依据本轮原始评估日志，计划内容通过签名与日志绑定。统计 JSON 同时保存输入日志和摘要的文件 SHA-256，便于追溯数据来源。

归档前另外对启动 manifest 中的 1231 个源码、官方代码、输入与保底文件逐一计算原始字节 SHA-256：1231 项一致，0 缺失、0 不一致。该核验针对当前本地原始字节；统计导出脚本并未实现此项自动复算，Git 检出换行可能改变源码字节哈希。
