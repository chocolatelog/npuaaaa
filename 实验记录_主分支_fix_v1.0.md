# 主分支实验记录：fix_v1

版本：`main-fix-v1.0.0`

所属分支：`main`

更新时间：2026-09-24

记录人：主分支维护者

## 1. 实验目的

验证构造池重构、精化接入、多种子搜索、超大图时间预算和官方评估桥接是否修复上一版 `refined_v2` 的性能回退。

## 2. 实验命令

### 回归测试

```cmd
cd /d J:\数学建模\改进 && python -m pytest tests -q
```

结果：当前构造池和方案合法性回归测试通过。

### 全量代理/搜索实验

```cmd
cd /d J:\数学建模\改进 && python solver\run_all.py --cases 1-100 --cores 2,3,4,5 --scenes A --workers 4 --log-file results\solve_log_fixed_v1.jsonl --output-dir results\plans_fixed_v1
```

结果：400/400 条任务完成，日志和计划保存在本地 `results/`，原始日志按仓库规则不提交。

## 3. 结果口径

100 个算例中，81 个算例属于官方可比集合；19 个超大图只保留代理或受预算限制的结果。官方平均加速比只使用 81 个可比结果，不能把 19 个代理结果混入平均值。

| 核数 | 原始基线 | `refined_v2` | `fix_v1` | 相对 `refined_v2` |
|---:|---:|---:|---:|---:|
| 2 | 1.740 | 1.644 | **1.760** | +7.1% |
| 3 | 2.228 | 2.117 | **2.370** | +11.9% |
| 4 | 2.621 | 2.525 | **2.890** | +14.5% |
| 5 | 2.979 | 2.797 | **3.320** | +18.7% |

与 `refined_v2` 的 400 条场景 A 配对记录相比，`fix_v1` 的平均真实 makespan 在 2/3/4/5 核分别下降 5.17%、7.43%、8.49%、10.00%。但仍有少量退化样本，不能只根据平均值宣称所有案例均改善。

## 4. 本轮完成内容

1. 将构造池改为范式 × 粒度 × 场景 × 精化的分层入口。
2. 将 `ctx` 传入构造池，使 R1/R2/R3 能在正式流程运行。
3. 统一候选的 `spill_bytes` 兼容字段，保留构造元数据。
4. 增加 HEFT、STRIP、CHAIN、NETBENEFIT 四类构造范式。
5. 增加 coarse、balanced、fine 三种粒度配置，并限制最终子图规模。
6. R1 枚举换核，R2 沿真实块边界移动，R3 检查合法无依赖子图换核。
7. 主搜索保留最多 6 个结构多样的种子，不再只搜索一个代理最优种子。
8. 超大图按预算兜底，避免候选精化和官方评估吞掉全部运行时间。
9. 增加构造池验证和方案覆盖、商图无环、核编号回归测试。

## 5. 仍存在的退化和风险

重点退化样本：`case_063/N4`、`case_082/N5`、`case_044/N4`、`case_007/N4`、`case_049/N5`。其中 `case_049/N5` 的新增搬运下降但 makespan 上升，说明当前代理没有充分惩罚并行度损失、后继链变长和核空泡。

fixed_v1 的代理均值仍明显高于真实均值，尤其大图的代理 added copy 可能达到真实值的数量级以上。后续不能通过继续扩大构造池解决，应先修正生命周期、评分口径和后继前瞻。

## 6. 下一轮实验

先选择以下 12 个代表算例，不直接重跑 400 组：

```text
case_001、case_007、case_015、case_023、case_042、case_044、case_049、case_063、case_067、case_078、case_082、case_097
```

依次比较：

```text
fix_v1
生命周期修正版
生命周期 + H=2 后继前瞻
生命周期 + H=2 后继前瞻 + 固定切分顺序搜索
```

每组记录真实 makespan、added copy、spill、peak live bytes（峰值存活字节）、关键路径等待、核空泡、候选数、官方评估次数和运行时间。只有代表集无极端子图、退化样本至少一半恢复、代理排序方向明显改善时，才进入全量实验。
