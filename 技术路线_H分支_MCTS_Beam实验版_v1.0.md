# H 分支 MCTS + Beam 混合搜索实验路线

版本：`H-v2.5-target-3.8-wide-verification`

日期：2026-09-25

## 1. 定位

本路线在无 MCTS 正式流程之后追加两个受限候选生成器：核心分配 PUCT
搜索和结构 Beam Search。原禁忌搜索、模拟退火、蚁群、MOPSO、LRU 复排
及官方候选不减少，新增搜索只追加候选，最终仍由官方评估器和 warm-start
非回退规则选择方案。

## 2. 搜索流程

```text
无 MCTS 正式流程完整运行
  -> 固定当前最优分区作为两路搜索根节点
  -> PUCT：子图换核 / 子图核交换
  -> Beam：换核 / 核交换 / 关键边块移动 / 子图合并 / 子图拆分
  -> 两路独立归档和 top-K 候选池
  -> 原候选优先官方核验
  -> MCTS / Beam 候选使用独立追加配额
  -> warm-start 官方复核与非回退选择
```

## 3. 关键实现

- PUCT 价值为相对根节点的归一化代理收益，限制在 `[-1, 1]`。
- transposition table 共享重复状态统计，并用最大评估数和停滞阈值终止。
- Beam 在相同代理评估预算下保留负载/子图数量不同的多样化前沿。
- `A/3`、`A/4`、`A/5`、`B/4`、`B/5` 使用最多 192 次代理评估，
  其他组合使用最多 64 次；时间上限也可能提前终止搜索。
- 高收益且不超过 6000 op 的任务追加 8 个官方候选，其他任务追加 4 个；
  同时启用两路搜索时平均分配给 MCTS 和 Beam。
- 每个官方候选记录来源、代理 makespan、代理搬运量、官方 makespan、
  官方搬运量以及是否最终被选择。

核心文件：

- `solver/mcts_schedule.py`
- `solver/beam_schedule.py`
- `solver/pipeline.py`
- `solver/run_all.py`

## 4. 使用方式

```cmd
.venv\Scripts\python.exe -m solver.run_all --cases 1-100 --cores 2,3,4,5 --scenes A,B --workers 8 --seed 0 --verify-k 24 --mcts --beam --warm-start-dir results\plans_verify24_full --output-dir results\plans_guided_full --log-file results\solve_log_guided_full.jsonl
```

`--mcts` 和 `--beam` 默认关闭，因此不会改变既有正式路线。实验输出必须使用
独立目录，不能覆盖无 MCTS 全量方案。

## 5. 适用结论

96 项代表集达到 +0.75995%，但 648 项官方全量配对增量为 +0.27538%。
该路线确认结构搜索有效且在 warm-start 保护下 0 退化，但代表集明显高估
全量收益。因此当前应保留为可选增强模块，不直接替换无 MCTS 正式默认流程。

## 6. 新目标与当前基线

后续实验目标分层设置为：B/5 平均加速比达到 3.8x，A/5 先达到 3.6x 并继续向 3.8x 推进。
扩大候选配额前，81 个官方可比案例的完整实验均值为
A/5=`3.440593x`、B/5=`3.577514x`。扩大后的 B/5 单次完整实验为
`3.589868x`，相对上一轮 16 胜、65 平、0 负；加入 case_023 历史遗漏恢复后，
组合最佳为 `3.589922x`，不冒充单次完整实验。详细口径见实验记录第 10 节。
空核和负载失衡与低加速比相关，但依赖、搬运和缓存也影响官方耗时，
因此不强制使用全部核心，也不以负载均衡代理值代替官方接受条件。

## 7. 扩大验证范围及实现边界

- wavefront 目前仅实现整子图迁核；没有实现成对交换、重新分区或子图拆分。
- A/B 的 5 核 warm 起点最多从前 24 个可用候选解码，原上限为 8；
  每个起点最多返回 8 个代理改善候选。代理根最多返回 24 个候选。
- 重点任务 wavefront 独立官方核验配额提高到 16，原上限为 8。
  配额是去重后的核验尝试次数，错误候选也会消耗配额。
- base 核验仍有独立上限：不超过 6000 op 时最多 32，否则最多 24。
  因此命令中的 `--verify-k 48` 不代表实际核验 48 个 base 候选。
- 所有原始 warm 计划在后续逐一接受官方复核，最终按 makespan 优先、
  新增搬运量次优择优；这条保底不依赖 warm 邻域是否成功解码。
- 超过 12000 op 的 19 个案例不纳入本轮官方平均。

已完成 wide19：B/5 的 19 个案例相对 bfull 为 11 胜、8 平、0 负。
这是增加候选配额、覆盖范围和历史起点的组合实验，不是单机制消融结论。

### 当前限制

1. 候选顺序为代理根、各 warm 起点串接，核验前 16 个有效去重候选；
   尚未实现多起点轮转或公平分配，增加起点不保证每个起点被官方核验。
2. `max_actions` 限制返回的改善候选数，不是代理评估调用数或时间上限。
   当前不能把 `--budget 0.5` 解释为每个任务总耗时只有 0.5 秒。
3. `wavefront.warm_candidates` 为累计候选数量，不是起点数量。
4. 计划解码会重新生成核内顺序；当前未完整验证同一 model block 内的
   全部 op 是否映射到相同子图。原始 warm 保底仍直接使用原计划。
5. 显式添加空核集合通常与原 light 目标重复，不单独声明“空核填充”的增益。

下一步候选方向是有评估预算的多起点轮转及更严格的计划解码，
需单独门槛验证；已完成的 widefull 全量期间求解源码保持不变，
并已对 81 份官方输出重新评估，均与原日志一致。
